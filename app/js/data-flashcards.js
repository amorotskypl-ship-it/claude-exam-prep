// ===== Flashcard Data =====
const FLASHCARDS = [
  // Domain 1
  { id: "f1", domain: 1, front: "What are the two stop_reason values in an agentic loop and what do they mean?", back: "<b>\"tool_use\"</b> = continue the loop (execute the tool, append result, send next request)<br><br><b>\"end_turn\"</b> = stop the loop (model is done, present final response)" },
  { id: "f2", domain: 1, front: "What is the hub-and-spoke architecture in multi-agent systems?", back: "A <b>coordinator agent</b> manages ALL inter-subagent communication, error handling, and information routing. Subagents never communicate directly with each other. The coordinator handles task decomposition, delegation, and result aggregation." },
  { id: "f3", domain: 1, front: "Do subagents inherit the coordinator's conversation history?", back: "<b>No.</b> Subagents have completely isolated context. Context must be <b>explicitly passed</b> in the subagent's prompt. There is no automatic inheritance or shared memory between agents." },
  { id: "f4", domain: 1, front: "What tool is used to spawn subagents? What must be in allowedTools?", back: "The <b>Task tool</b> spawns subagents. The coordinator's <b>allowedTools must include \"Task\"</b> or it cannot invoke subagents at all." },
  { id: "f5", domain: 1, front: "How do you spawn parallel subagents?", back: "Emit <b>multiple Task tool calls in a single coordinator response</b> (not across separate turns). This allows subagents to run concurrently." },
  { id: "f6", domain: 1, front: "When should you use programmatic enforcement (hooks) vs prompt-based guidance?", back: "<b>Hooks</b> = when deterministic compliance is required (identity verification before refunds, financial operations, security-critical steps). Hooks provide <b>guaranteed compliance</b>.<br><br><b>Prompts</b> = probabilistic, non-zero failure rate. Only for non-critical guidance." },
  { id: "f7", domain: 1, front: "What do PostToolUse hooks do?", back: "They <b>intercept tool results for transformation</b> before the model processes them. Common use: normalizing heterogeneous data formats (Unix timestamps, ISO 8601, numeric status codes) from different MCP tools into a consistent format." },
  { id: "f8", domain: 1, front: "What are the three anti-patterns for agentic loop termination?", back: "1. <b>Parsing natural language</b> to determine loop end<br>2. <b>Arbitrary iteration caps</b> as the primary stopping mechanism<br>3. <b>Checking for assistant text content</b> as a completion indicator<br><br>Always use <b>stop_reason</b> values instead." },
  { id: "f9", domain: 1, front: "What is fork_session used for?", back: "Creating <b>independent branches from a shared analysis baseline</b> to explore divergent approaches (e.g., comparing two testing strategies or refactoring approaches from the same codebase analysis)." },
  { id: "f10", domain: 1, front: "When should you resume a session vs start fresh?", back: "<b>Resume (--resume)</b>: when prior context is mostly still valid.<br><br><b>Start fresh with injected summaries</b>: when prior tool results are stale. A structured summary is more reliable than resuming with outdated tool outputs." },

  // Domain 2
  { id: "f11", domain: 2, front: "What is the PRIMARY mechanism LLMs use for tool selection?", back: "<b>Tool descriptions.</b> Minimal descriptions lead to unreliable selection. Descriptions should include: input formats, example queries, edge cases, and boundaries explaining when to use this tool vs similar alternatives." },
  { id: "f12", domain: 2, front: "What are the four error categories for MCP structured error responses?", back: "1. <b>Transient</b> (timeouts, service unavailability) -- typically retryable<br>2. <b>Validation</b> (invalid input) -- not retryable without fixing input<br>3. <b>Business</b> (policy violations) -- not retryable<br>4. <b>Permission</b> (access denied) -- not retryable<br><br>Always include: errorCategory, isRetryable boolean, human-readable description" },
  { id: "f13", domain: 2, front: "What happens when an agent has access to too many tools?", back: "Selection reliability <b>degrades</b>. 18 tools is too many. Best practice: <b>4-5 tools per agent</b>, scoped to its role. Agents with tools outside their specialization tend to misuse them." },
  { id: "f14", domain: 2, front: "What are the three tool_choice options?", back: "<b>\"auto\"</b> = model may return text instead of calling a tool<br><b>\"any\"</b> = model MUST call a tool (but can choose which)<br><b>{type: \"tool\", name: \"...\"}</b> = model MUST call this specific tool<br><br>Use \"any\" for guaranteed structured output. Use forced for prerequisite steps." },
  { id: "f15", domain: 2, front: "Where do you configure shared MCP servers vs personal ones?", back: "<b>Shared (team):</b> .mcp.json in project root (version controlled)<br><b>Personal:</b> ~/.claude.json (user-level, not shared)<br><br>.mcp.json supports <b>${ENV_VAR}</b> expansion for credentials without committing secrets." },
  { id: "f16", domain: 2, front: "What's the difference between Grep and Glob?", back: "<b>Grep</b> = search file <b>contents</b> (find patterns, function names, error messages inside files)<br><br><b>Glob</b> = find files by <b>name/path patterns</b> (e.g., **/*.test.tsx, src/**/*.service.ts)" },
  { id: "f17", domain: 2, front: "What are MCP resources and why use them?", back: "MCP resources expose <b>content catalogs</b> (issue summaries, documentation hierarchies, database schemas) to give agents visibility into available data <b>without requiring exploratory tool calls</b>. They reduce unnecessary API calls." },
  { id: "f18", domain: 2, front: "What's the difference between access failures and valid empty results?", back: "<b>Access failure</b>: the query could not reach the data source (timeout, permission denied). May warrant retry.<br><br><b>Valid empty result</b>: the query succeeded but found zero matches. This is NOT an error. Marking it as an error causes wasteful retries." },

  // Domain 3
  { id: "f19", domain: 3, front: "What is the CLAUDE.md configuration hierarchy?", back: "1. <b>User-level:</b> ~/.claude/CLAUDE.md (personal, NOT shared via version control)<br>2. <b>Project-level:</b> .claude/CLAUDE.md or root CLAUDE.md (shared with team)<br>3. <b>Directory-level:</b> subdirectory CLAUDE.md files<br><br>Also: .claude/rules/ for topic-specific rule files, @import for modularity" },
  { id: "f20", domain: 3, front: "What SKILL.md frontmatter options exist?", back: "<b>context: fork</b> = run in isolated sub-agent context (prevents pollution of main conversation)<br><b>allowed-tools</b> = restrict which tools the skill can use<br><b>argument-hint</b> = prompt for required parameters when invoked without arguments" },
  { id: "f21", domain: 3, front: "When to use plan mode vs direct execution?", back: "<b>Plan mode:</b> complex tasks, large-scale changes, multiple valid approaches, architectural decisions, multi-file modifications, unfamiliar codebases<br><br><b>Direct execution:</b> simple, well-scoped changes (single-file bug fix, adding one validation check, clear stack trace fix)" },
  { id: "f22", domain: 3, front: "What is the -p flag for Claude Code?", back: "<b>-p (--print)</b> runs Claude Code in <b>non-interactive mode</b>. Essential for CI/CD pipelines. Without it, Claude Code waits for interactive input, causing pipeline hangs.<br><br>Also: --output-format json + --json-schema for structured CI output." },
  { id: "f23", domain: 3, front: "How do path-specific rules in .claude/rules/ work?", back: "Files in .claude/rules/ with <b>YAML frontmatter</b> containing <b>paths: [\"glob-pattern\"]</b> load only when editing matching files.<br><br>Example: paths: [\"**/*.test.tsx\"] applies test conventions to all test files regardless of directory.<br><br>Better than subdirectory CLAUDE.md for conventions spanning multiple directories." },
  { id: "f24", domain: 3, front: "What is the interview pattern?", back: "Having Claude <b>ask questions to surface considerations</b> the developer may not have anticipated <b>before implementing</b>. Useful in unfamiliar domains (e.g., cache invalidation strategies, failure modes). Leads to better designs than jumping straight to implementation." },
  { id: "f25", domain: 3, front: "Why is self-review less effective than independent review?", back: "The same Claude session retains <b>reasoning context from generation</b>, making it less likely to question its own decisions. An independent review instance (without prior reasoning context) is more effective at catching subtle issues." },

  // Domain 4
  { id: "f26", domain: 4, front: "Why does 'be conservative' fail as a prompt instruction?", back: "General instructions like 'be conservative' or 'only report high-confidence findings' <b>don't improve precision</b> compared to <b>specific categorical criteria</b>.<br><br>Instead: define which issue types to report (bugs, security) vs skip (minor style), with concrete code examples for each severity." },
  { id: "f27", domain: 4, front: "When are few-shot examples the right technique?", back: "When <b>detailed instructions alone produce inconsistent results</b>. Few-shot examples (2-4 targeted) are the <b>most effective technique</b> for:<br>- Consistent output format<br>- Ambiguous case handling<br>- Reducing hallucination in extraction<br>- Enabling generalization to novel patterns" },
  { id: "f28", domain: 4, front: "How does tool_use with JSON schemas prevent fabrication?", back: "Strict schemas eliminate <b>syntax errors</b> (malformed JSON) but NOT semantic errors (wrong values).<br><br>To prevent fabrication of missing data: make fields <b>optional/nullable</b> so the model returns null instead of inventing values.<br><br>Add \"unclear\" enum values for ambiguous cases." },
  { id: "f29", domain: 4, front: "When do retries work vs fail in extraction?", back: "<b>Retries WORK for:</b> format mismatches, structural output errors, field placement errors<br><br><b>Retries FAIL when:</b> the required information simply doesn't exist in the source document. No amount of retrying will create missing data." },
  { id: "f30", domain: 4, front: "What are the Message Batches API constraints?", back: "<b>50% cost savings</b> but up to <b>24-hour processing window</b>, no guaranteed latency SLA.<br><br><b>Good for:</b> overnight reports, weekly audits, nightly test generation<br><b>Bad for:</b> blocking pre-merge checks<br><br>Uses <b>custom_id</b> to correlate requests/responses. Does NOT support multi-turn tool calling." },

  // Domain 5
  { id: "f31", domain: 5, front: "What is the 'lost in the middle' effect?", back: "Models reliably process information at the <b>beginning and end</b> of long inputs but may <b>omit findings from middle sections</b>.<br><br>Mitigation: place key findings summaries at the <b>beginning</b> of aggregated inputs and use explicit section headers." },
  { id: "f32", domain: 5, front: "What are the three valid escalation triggers?", back: "1. <b>Customer explicitly requests a human</b> (escalate immediately, don't investigate first)<br>2. <b>Policy exceptions/gaps</b> (policy is ambiguous or silent on the request)<br>3. <b>Inability to make meaningful progress</b><br><br><b>NOT valid:</b> sentiment analysis, self-reported confidence scores (unreliable proxies)." },
  { id: "f33", domain: 5, front: "What are the two anti-patterns for error propagation in multi-agent systems?", back: "1. <b>Silently suppressing errors</b> (returning empty results as success) -- prevents any recovery<br>2. <b>Terminating the entire workflow</b> on a single failure -- too drastic when recovery is possible<br><br><b>Correct:</b> return structured error context (failure type, attempted query, partial results, alternatives) for intelligent coordinator recovery." },
  { id: "f34", domain: 5, front: "What is context degradation and how do you combat it?", back: "In extended sessions, models start giving <b>inconsistent answers</b> and referencing 'typical patterns' instead of specific discovered classes.<br><br><b>Solutions:</b><br>- <b>Scratchpad files</b> for persisting key findings<br>- <b>Subagent delegation</b> for verbose exploration<br>- <b>/compact</b> to reduce accumulated context<br>- Structured state exports for crash recovery" },
  { id: "f35", domain: 5, front: "Why shouldn't you trust aggregate accuracy metrics?", back: "<b>97% overall accuracy may mask poor performance</b> on specific document types or fields (e.g., 99% invoices, 95% contracts, 72% handwritten notes).<br><br>Always: validate accuracy <b>by document type AND field segment</b>. Use <b>stratified random sampling</b> for ongoing error measurement." },
  { id: "f36", domain: 5, front: "How should conflicting data from multiple sources be handled in synthesis?", back: "<b>Include both values with source attribution</b> and annotate the conflict. Never arbitrarily pick one value.<br><br>Structure reports to distinguish <b>well-established findings</b> from <b>contested ones</b>. Include publication dates to avoid temporal misinterpretation." },
  { id: "f37", domain: 5, front: "What is a 'case facts' block and when should you use it?", back: "A <b>persistent structured block</b> containing exact transactional values (dollar amounts, dates, order numbers, statuses) that is included in each prompt <b>outside summarized history</b>.<br><br>Use it in multi-issue or long sessions to prevent progressive summarization from losing exact numerical values." },
  { id: "f38", domain: 5, front: "What should a structured handoff summary include when escalating to a human?", back: "- Customer ID<br>- Root cause analysis<br>- Refund/action amount<br>- Recommended action<br>- Steps already taken<br><br>Humans receiving the handoff may <b>not have access</b> to the conversation transcript, so the summary must be self-contained." },

  // ===== Article Insights (from Tw93's deep-use engineering article) =====

  { id: "f39", domain: 5, front: "What is the real available context budget in Claude Code? How is 200K broken down?", back: "<b>~160-180K dynamic</b> out of 200K total.<br><br>Fixed overhead (~15-20K): system instructions (~2K), skill descriptors (~1-5K), <b>MCP tool definitions (~10-20K — hidden killer)</b>, LSP (~2-5K).<br><br>Semi-fixed (~5-10K): CLAUDE.md + Memory.<br><br>Use <code>/context</code> to see the actual breakdown." },

  { id: "f40", domain: 2, front: "How many tokens does one MCP server consume? What about 5 servers?", back: "One MCP server: 20-30 tool definitions × ~200 tokens each = <b>4-6K tokens</b>.<br><br>Five servers: ~25K tokens = <b>12.5% of total context budget</b> consumed before any work begins.<br><br>Use <code>/mcp</code> to disconnect idle servers and <code>/context</code> to monitor overhead." },

  { id: "f41", domain: 5, front: "What is the prompt cache architecture in Claude Code? What breaks the cache?", back: "Prefix-matching cache. Order: <b>System → Tools → History → Current input</b>.<br><br><b>Cache breaks:</b><br>- Timestamps in system prompt (changes every request)<br>- Reordering tool definitions<br>- Switching models mid-session (invalidates ALL cached tokens)<br>- Adding/removing MCP servers<br><br>Fix: use <code>defer_loading: true</code> for MCP tools to keep prefix stable." },

  { id: "f42", domain: 3, front: "What is the compaction trap? How do you fix it?", back: "Default compaction (<code>/compact</code>) <b>drops architecture decisions</b> because it prioritizes 're-readable' content (tool outputs, code) over reasoning.<br><br><b>Fix:</b> Add a <b>Compact Instructions</b> section to CLAUDE.md that explicitly lists what to preserve during compaction (e.g., 'Always preserve: architectural decisions, API design choices, failure modes discovered')." },

  { id: "f43", domain: 5, front: "What is the HANDOFF.md pattern?", back: "Before ending a session or when context is nearly full, ask Claude to write a <b>HANDOFF.md</b> file summarizing:<br>- Current progress<br>- Approaches tried (what worked/failed)<br>- Open issues<br>- Recommended next steps<br><br>The next session reads this file instead of relying on compaction. Much more reliable for session transitions." },

  { id: "f44", domain: 2, front: "What is defer_loading for MCP tools? Why use it?", back: "<code>defer_loading: true</code> creates <b>stub tool definitions</b> with only the description. Full schema is loaded only <b>after the model selects the tool</b> via ToolSearch.<br><br><b>Benefits:</b><br>- Dramatically reduces fixed token overhead from MCP<br>- Keeps prompt cache prefix stable<br>- Essential when connecting multiple MCP servers" },

  { id: "f45", domain: 3, front: "What are the three skill types and when to use each?", back: "1. <b>Checklist (quality gate)</b>: pre-flight verification — all items must pass (e.g., release-check)<br><br>2. <b>Workflow (standardized operation)</b>: high-risk operations with explicit rollback steps. Use <code>disable-model-invocation: true</code> (e.g., config-migration)<br><br>3. <b>Domain Expert (decision framework)</b>: structured evidence collection + decision matrix for diagnosis (e.g., runtime-diagnosis)" },

  { id: "f46", domain: 3, front: "How should you optimize skill descriptor length?", back: "Keep descriptors short — <b>~9 tokens</b>, not ~45 tokens.<br><br><b>Bad:</b> 'This skill helps review pull requests with a focus on code correctness and best practices for production systems'<br><br><b>Good:</b> 'Use for PR reviews with focus on correctness'<br><br>Every skill descriptor is <b>always in context</b> (fixed cost), so verbosity wastes tokens on every request." },

  { id: "f47", domain: 3, front: "What happens when you switch models mid-session?", back: "The prompt cache is <b>invalidated for ALL prior tokens</b>. The cache must be rebuilt from scratch for the new model.<br><br>This actually <b>costs MORE</b> than staying on one model because you lose all cached prefixes.<br><br><b>Fix:</b> Use a <b>subagent</b> with a different model for specific tasks, or start a fresh session." },

  { id: "f48", domain: 1, front: "Name the four hook points in Claude Code and when each fires.", back: "1. <b>PreToolUse</b>: before tool executes — block edits, enforce prerequisites, validate params<br>2. <b>PostToolUse</b>: after tool returns, before model sees result — auto-format, filter verbose output<br>3. <b>SessionStart</b>: when session begins — inject dynamic context (git branch, env vars)<br>4. <b>Notification</b>: when task completes — push notifications (e.g., macOS alert)" },

  { id: "f49", domain: 1, front: "What is the Six-Layer Architecture Model for Claude Code?", back: "1. <b>Context</b> — what the model sees (CLAUDE.md, rules, memory)<br>2. <b>Tools</b> — what the model can do (built-in, MCP, custom)<br>3. <b>Skills</b> — on-demand workflows (SKILL.md)<br>4. <b>Hooks</b> — deterministic governance (zero token cost)<br>5. <b>Subagents</b> — isolated execution (only summaries return)<br>6. <b>Verification</b> — proof of correctness (tests, lint, typecheck)" },

  { id: "f50", domain: 3, front: "What is Plan Mode? How do you enter it and how does it preserve cache?", back: "<b>Enter:</b> Shift+Tab × 2, or model calls <code>EnterPlanMode</code> tool.<br><br><b>Key insight:</b> EnterPlanMode is a tool call — it does NOT switch the tool set. This means the <b>prompt cache prefix is preserved</b> (tools don't change).<br><br>Use for: complex architectural tasks, exploring unfamiliar codebases, multi-file changes. <b>Don't</b> use for simple one-file fixes." },

  { id: "f51", domain: 5, front: "What is the context layering strategy? (5 layers from always-resident to outside-context)", back: "1. <b>Always resident</b> (CLAUDE.md) — project contract, hard constraints<br>2. <b>Path-loaded</b> (.claude/rules/) — language/file-type specific rules<br>3. <b>On-demand</b> (Skills) — loaded only when triggered<br>4. <b>Isolated</b> (Subagents) — bulk exploration, only summaries return<br>5. <b>Outside context</b> (Hooks) — deterministic scripts, <b>zero token cost</b>" },

  // ===== NotebookLM New Concepts =====

  { id: "f52", domain: 1, front: "What is compact_boundary? When is it emitted?", back: "A <b>SystemMessage subtype</b> emitted after context compaction (when the system summarizes older turns to free up space).<br><br>It manages window state, providing the model with a <b>summary of the deleted context</b> while preserving the most recent turns.<br><br>Superior to simple truncation because it preserves context through summarization." },

  { id: "f53", domain: 1, front: "What is a ResultMessage in the Agent SDK?", back: "The <b>final message yielded</b> by the Agent SDK when a task completes. Contains:<br>- Response text<br>- Total <b>token usage</b> (input/output)<br>- <b>Cost data</b><br><br>This is the standard message for tracking billing and token consumption." },

  { id: "f54", domain: 1, front: "What is the 'init' SystemMessage subtype?", back: "The <b>first message</b> in a session that establishes grounding and session metadata.<br><br>It appears at the very beginning of the conversation, before any user messages or tool calls." },

  { id: "f55", domain: 1, front: "What are Agent Teams vs Subagents?", back: "<b>Subagents:</b> Ephemeral, spawned by a coordinator for discrete subtasks. Isolated context, no shared state.<br><br><b>Agent Teams:</b> Coordinate <b>independent persistent instances</b> that share a task list. Each instance maintains its own context but collaborates through shared work queues.<br><br>Use subagents for short tasks; Agent Teams for sustained parallel work." },

  { id: "f56", domain: 1, front: "What is Dynamic Decomposition vs Prompt Chaining?", back: "<b>Prompt Chaining:</b> Fixed, sequential workflow — output of step N feeds step N+1. Pre-defined pipeline.<br><br><b>Dynamic Decomposition:</b> The agent builds and prioritizes an <b>adaptive plan on the fly</b>. Can reorder, skip, or add subtasks based on intermediate results.<br><br>Use chaining for well-understood pipelines; dynamic decomposition for open-ended investigations." },

  { id: "f57", domain: 2, front: "What are the three MCP primitives?", back: "1. <b>Tools:</b> Executable functions that change state or query dynamic data<br>2. <b>Resources:</b> Static/semi-static data (database schemas, log files, documentation)<br>3. <b>Prompts:</b> Templates that guide model interaction with specific datasets<br><br>Resources reduce exploratory tool calls. Prompts standardize complex interactions." },

  { id: "f58", domain: 2, front: "What is stdio transport for MCP? When to use it?", back: "<b>stdio</b> is the standard transport mechanism for <b>local, command-line based</b> MCP servers in the Claude ecosystem.<br><br>The model communicates with the MCP server via standard input/output streams.<br><br>Used for local integrations (e.g., database access, file system operations). HTTP/SSE are for remote servers." },

  { id: "f59", domain: 3, front: "What is managed-settings.json? Where is it located?", back: "<b>System-level IT admin configuration</b> with the <b>highest precedence</b> — cannot be overridden by user settings, project settings, or CLI arguments.<br><br><b>macOS:</b> /Library/Application Support/ClaudeCode/managed-settings.json<br><b>Windows:</b> C:\\Program Files\\ClaudeCode\\managed-settings.json (legacy ProgramData path deprecated)<br><br>Used for enterprise security policies." },

  { id: "f60", domain: 3, front: "What enterprise lockdown settings exist in managed-settings.json?", back: "- <b>allowManagedMcpServersOnly:</b> Block all MCP except admin allowlist<br>- <b>strictKnownMarketplaces:</b> Plugin source allowlist (exact-match)<br>- <b>hostPattern:</b> Regex matching for MCP server hosts<br>- <b>network.allowedDomains:</b> Sandbox network restriction<br>- <b>availableModels:</b> Restrict model picker options<br>- <b>--dangerously-skip-permissions:</b> Can be blocked at managed level" },

  { id: "f61", domain: 3, front: "What is the /status command? How is it different from /memory?", back: "<b>/status:</b> Shows active <b>configuration layers</b> and their origins — identifies if Managed settings are being applied, which settings files are loaded, and precedence order.<br><br><b>/memory:</b> Shows which <b>CLAUDE.md files and rules</b> are currently in the agent's context.<br><br>/status = config diagnostics. /memory = context diagnostics." },

  { id: "f62", domain: 3, front: "What are .claude/agents/ and what goes there?", back: "Project-scoped directory for <b>custom AI subagent definitions</b>.<br><br>Each agent is defined as a markdown file with specific tool restrictions, system prompt, and capabilities.<br><br>Example: .claude/agents/security-reviewer.md — a subagent that only has Read, Grep, Glob tools and focuses on security analysis." },

  { id: "f63", domain: 4, front: "What is the 'Best-of-N' pattern?", back: "Generate <b>N candidate solutions</b>, then use a <b>programmatic evaluator or separate agent</b> to select the optimal one.<br><br>Core workflow:<br>1. Run N parallel attempts at the task<br>2. Evaluate each using test suites, linting, or a judge agent<br>3. Select the best one based on objective criteria<br><br>Different from simple retry — all N candidates are generated independently." },

  { id: "f64", domain: 4, front: "What is the --append-system-prompt flag?", back: "Appends additional instructions <b>to the existing</b> Claude Code system prompt (does NOT replace it).<br><br>Useful for CI/CD pipelines to inject context-specific instructions without modifying CLAUDE.md.<br><br>Example: <code>claude -p --append-system-prompt 'Focus only on security issues'</code>" },

  // Domain 4 — Additional flashcards (f71-f82)
  { id: "f71", domain: 4, front: "What is prefilling (pre-fill) and when should you use it?", back: "Placing text at the <b>start of the assistant turn</b> to steer output format or behavior.<br><br><b>Use cases:</b><br>- Force JSON output: prefill with <code>{</code><br>- Force XML output: prefill with <code>&lt;analysis&gt;</code><br>- Skip preamble: prefill with the desired opening<br><br><b>Key constraint:</b> Only works with non-streaming Messages API. Extended thinking mode <b>does NOT support prefilling</b>." },
  { id: "f72", domain: 4, front: "What are the XML tag conventions for Claude prompts?", back: "Claude is <b>specifically trained</b> to recognize XML tags as structural delimiters.<br><br><b>Common tags:</b><br>- <code>&lt;instructions&gt;</code> — task directives<br>- <code>&lt;context&gt;</code> / <code>&lt;document&gt;</code> — reference material<br>- <code>&lt;example&gt;</code> — few-shot demonstrations<br>- <code>&lt;output_format&gt;</code> — expected response structure<br><br><b>Why XML over markdown?</b> XML tags are unambiguous delimiters that don't conflict with content formatting. Nesting is explicit." },
  { id: "f73", domain: 4, front: "How do you elicit chain-of-thought (CoT) reasoning?", back: "<b>Explicit:</b> Include 'Think step by step' or 'Show your reasoning in &lt;thinking&gt; tags before answering.'<br><br><b>Structured CoT:</b> Ask for reasoning in a specific format (numbered steps, pros/cons table).<br><br><b>Extended thinking:</b> Enable <code>thinking</code> parameter with a <code>budget_tokens</code> value. Claude reasons internally before responding.<br><br><b>Key insight:</b> CoT improves accuracy on complex tasks but increases token usage. Skip it for simple lookups." },
  { id: "f74", domain: 4, front: "What is the correct ordering for prompt caching? What breaks the cache?", back: "Cache uses <b>prefix matching</b>. Order content from <b>most stable to least stable:</b><br><br>1. System prompt (static instructions)<br>2. Tool definitions<br>3. Few-shot examples (with cache_control breakpoint)<br>4. Long reference documents (with cache_control breakpoint)<br>5. Conversation history<br>6. Current user message<br><br><b>Cache breaks when ANY earlier prefix element changes</b> (reordering tools, modifying system prompt, adding timestamps)." },
  { id: "f75", domain: 4, front: "What are the system prompt best practices for Claude?", back: "<b>Structure (order matters):</b><br>1. Role/identity definition<br>2. Core instructions and constraints<br>3. Output format requirements<br>4. Reference material / context<br>5. Examples<br><br><b>Key rules:</b><br>- Put the most important instructions at the <b>top and bottom</b> (primacy/recency effect)<br>- Use XML tags to separate sections<br>- Keep identity/role statements concise<br>- Avoid contradictory instructions (later ones may override earlier)" },
  { id: "f76", domain: 4, front: "How does extended thinking work? What are the key constraints?", back: "<b>Enable:</b> Set <code>thinking.type: 'enabled'</code> with <code>budget_tokens</code> in the API request.<br><br><b>Constraints:</b><br>- Thinking content is <b>NOT cached</b> by default (use clear_thinking strategy to manage)<br>- <b>No prefilling</b> allowed when thinking is enabled<br>- <b>No temperature</b> setting (fixed at 1.0)<br>- Minimum budget: 1024 tokens<br>- Thinking tokens count toward <b>output token limits</b><br>- Thinking is visible via API but <b>should not be shown to end users</b> in production" },
  { id: "f77", domain: 4, front: "What is the difference between temperature and top_k/top_p?", back: "<b>Temperature (0-1):</b> Controls randomness. 0 = deterministic (greedy), 1 = maximum creativity.<br>- Use 0 for classification, extraction, factual Q&A<br>- Use 0.5-0.7 for balanced generation<br>- Use 1.0 for creative writing, brainstorming<br><br><b>top_k:</b> Limits sampling to top K tokens<br><b>top_p:</b> Limits sampling to tokens covering P cumulative probability<br><br><b>Exam trap:</b> Extended thinking forces temperature = 1.0 (cannot be overridden)." },
  { id: "f78", domain: 4, front: "How do you count tokens before sending a request?", back: "Use the <b>Token Counting API</b> endpoint: <code>POST /v1/messages/count_tokens</code><br><br>Accepts the same payload as the Messages API (system, messages, tools) and returns the <b>exact input token count</b> without generating a response.<br><br><b>Use cases:</b><br>- Pre-flight budget checks before expensive calls<br>- Deciding when to trigger context compaction<br>- Cost estimation for batch processing<br><br>Counting is <b>free</b> — no charge for token count requests." },
  { id: "f79", domain: 4, front: "What is the multi-turn prompt design pattern for tool use?", back: "<b>Structure:</b><br>1. User message (request)<br>2. Assistant message with <code>tool_use</code> content block<br>3. User message with <code>tool_result</code> content block<br>4. Assistant final response<br><br><b>Key rules:</b><br>- <code>tool_use</code> and <code>tool_result</code> must have matching <code>tool_use_id</code><br>- Multiple tool calls can appear in a single assistant turn<br>- <code>tool_result</code> messages go in the <b>user role</b> (not assistant)<br>- Always check <code>stop_reason</code> to know if the model wants to call more tools" },
  { id: "f80", domain: 4, front: "What is streaming and when should you use server-sent events (SSE)?", back: "<b>Streaming</b> delivers response tokens as they're generated via <b>Server-Sent Events (SSE)</b>.<br><br><b>Event types:</b><br>- <code>message_start</code> — message metadata<br>- <code>content_block_delta</code> — incremental text/tool_use tokens<br>- <code>message_stop</code> — generation complete<br><br><b>Use when:</b> User-facing applications needing low time-to-first-token<br><b>Skip when:</b> Batch processing, CI/CD pipelines (use synchronous API instead)" },
  { id: "f81", domain: 4, front: "What is prompt caching and what are the minimum cacheable sizes?", back: "<b>Prompt caching</b> stores frequently-used prompt prefixes server-side. Cached reads cost <b>90% less</b> than uncached input tokens.<br><br><b>Minimum sizes to cache:</b><br>- Sonnet/Haiku: <b>1,024 tokens</b><br>- Opus: <b>2,048 tokens</b><br><br><b>Mark with:</b> <code>cache_control: {type: 'ephemeral'}</code> on the content block<br><br><b>TTL:</b> 5 minutes (refreshed on each cache hit). Max 4 cache breakpoints per request." },
  { id: "f82", domain: 4, front: "What are the key differences between the Messages API and the Text Completions API?", back: "The <b>Text Completions API is legacy</b> and deprecated.<br><br><b>Messages API advantages:</b><br>- Structured turn-based format (user/assistant roles)<br>- Native tool use support<br>- System prompt as a dedicated parameter<br>- Vision (image) support<br>- Extended thinking support<br>- Prompt caching support<br>- Context editing support<br><br><b>Exam trap:</b> Any answer referencing the Completions API format (<code>\\n\\nHuman: / \\n\\nAssistant:</code>) is likely wrong for modern Claude usage." },

  { id: "f65", domain: 5, front: "What is the 'Lost in the Middle' effect and how to mitigate it?", back: "Models suffer <b>attention degradation in the center</b> of long context windows. Recall is highest at the <b>beginning and end</b>.<br><br><b>Mitigations:</b><br>- Place key instructions and summaries at the top<br>- Use explicit section headers<br>- Trim verbose tool outputs before they accumulate in the middle<br>- Place persistent 'Case Facts' blocks outside summarized history" },

  { id: "f66", domain: 5, front: "What are subscription tier multipliers vs Free?", back: "<b>Pro:</b> 20x<br><b>Max:</b> 25x (some sources say higher)<br><b>Team Standard:</b> 25-30x<br><b>Team Premium:</b> 150x<br><br>Active developer benchmark: ~<b>$6/day</b> on Claude Code tokens.<br><br>Use these for ROI calculations and capacity planning." },

  // Context Editing (from platform docs)
  { id: "f67", domain: 5, front: "What are the two server-side context editing strategies and their beta header?", back: "<b>1. clear_tool_uses_20250919</b> &mdash; clears old tool results (and optionally tool calls) when context grows beyond a threshold.<br><br><b>2. clear_thinking_20251015</b> &mdash; manages thinking blocks in extended thinking conversations.<br><br><b>Beta header:</b> <code>context-management-2025-06-27</code><br><br>Both are applied <b>server-side</b> before the prompt reaches Claude. The client keeps the full unmodified history." },
  { id: "f68", domain: 5, front: "What are the 5 configuration options for clear_tool_uses_20250919?", back: "<b>trigger:</b> When to activate (default: 100K input tokens)<br><b>keep:</b> How many recent tool uses to preserve (default: 3)<br><b>clear_at_least:</b> Minimum tokens to clear (prevents cache-breaking for small savings)<br><b>exclude_tools:</b> Tool names whose results are never cleared<br><b>clear_tool_inputs:</b> Also clear tool call parameters (default: false, only results cleared)" },
  { id: "f69", domain: 5, front: "What is the default thinking block clearing behavior, and how to maximize cache hits?", back: "<b>Default</b> (even without configuring the strategy): keeps only thinking blocks from the <b>last 1 assistant turn</b>.<br><br><b>Maximize cache hits:</b> set <code>keep: 'all'</code> to preserve all thinking blocks, maintaining the prompt cache prefix.<br><br><b>Ordering rule:</b> When combining strategies, <code>clear_thinking</code> MUST be listed <b>first</b> in the edits array." },
  { id: "f70", domain: 5, front: "Context editing vs. compaction: what's the difference?", back: "<b>Context editing</b> (server-side): Selectively <b>clears</b> specific content (tool results, thinking blocks). Client keeps full history. Fine-grained control.<br><br><b>Compaction</b> (server-side preferred, also client SDK): Generates a <b>summary</b> and replaces the full conversation history. Broader but lossy.<br><br><b>Key insight:</b> Compaction is the primary strategy for most use cases. Context editing is for specific scenarios needing fine-grained control over what gets removed." },
];
