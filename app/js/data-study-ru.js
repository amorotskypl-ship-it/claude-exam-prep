// ===== Study Guide Data (Russian) =====
const STUDY_DATA_RU = [
  {
    id: "d1",
    domain: 1,
    title: "Агентная архитектура и оркестрация",
    weight: 27,
    tagClass: "tag-d1",
    tasks: [
      {
        id: "t1.1",
        title: "1.1: Проектирование и реализация агентных циклов",
        knowledge: [
          "Жизненный цикл агентного цикла: отправить запрос Claude -> проверить stop_reason ('tool_use' vs 'end_turn') -> выполнить запрошенные инструменты -> вернуть результаты для следующей итерации",
          "Результаты инструментов добавляются в историю беседы, чтобы модель могла рассуждать о следующем действии",
          "Принятие решений моделью: Claude рассуждает о том, какой инструмент вызвать следующим на основе контекста, а НЕ по заранее настроенным деревьям решений или последовательностям инструментов"
        ],
        skills: [
          "Реализовать управление потоком цикла: продолжать при stop_reason 'tool_use', завершать при stop_reason 'end_turn'",
          "Добавлять результаты инструментов в контекст беседы между итерациями, чтобы модель учитывала новую информацию",
          "Избегать антипаттернов: парсинг естественного языка для определения конца цикла, произвольные ограничения итераций как основной механизм остановки, проверка текста ассистента как индикатора завершения"
        ],
        keyPoint: "Агентный цикл управляется значениями stop_reason, а не парсингом текста или произвольными лимитами. 'tool_use' = продолжать, 'end_turn' = остановить.",
        details: `
          <h4>Как работает агентный цикл</h4>
          <p>Агентный цикл — это основной паттерн выполнения для автономных агентов на базе Claude. Концепция проста, но критически важна для правильной реализации:</p>
          <pre><code>while True:
    response = claude.send(messages)

    if response.stop_reason == "end_turn":
        break   # Claude завершил — вернуть финальный ответ

    if response.stop_reason == "tool_use":
        tool_result = execute_tool(response.tool_call)
        messages.append(response)          # сообщение ассистента
        messages.append(tool_result)       # результат инструмента
        # цикл продолжается — Claude проверит результат</code></pre>
          <p>Ключевое понимание: <strong>Claude решает</strong>, что делать дальше. Вы не программируете дерево решений заранее. Модель проверяет результат инструмента, рассуждает о текущем состоянии и либо вызывает другой инструмент, либо возвращает финальный ответ.</p>

          <h4>Два значения stop_reason, которые нужно знать</h4>
          <ul>
            <li><code>stop_reason: "tool_use"</code> — Claude хочет вызвать инструмент. Выполните его, добавьте результат в беседу и продолжите цикл.</li>
            <li><code>stop_reason: "end_turn"</code> — Claude завершил работу. Выйдите из цикла и верните ответ.</li>
          </ul>
          <p>Вот и всё. Никакого парсинга текста на предмет «выглядит ли он завершённым». Никакого подсчёта итераций. stop_reason — это авторитетный сигнал.</p>

          <h4>Критические антипаттерны (любимые на экзамене)</h4>
          <div class="wrong-answer">
            <strong>Антипаттерн 1: Парсинг естественного языка для обнаружения завершения.</strong>
            Не проверяйте, говорит ли текст Claude «Я закончил» или «Вот результаты». Это хрупко и ненадёжно. Используйте <code>stop_reason</code>.
          </div>
          <div class="wrong-answer">
            <strong>Антипаттерн 2: Произвольные ограничения итераций как ОСНОВНОЙ механизм остановки.</strong>
            Установка <code>max_iterations = 5</code> как основного способа остановки цикла — неправильно. Модель должна останавливаться естественно через <code>end_turn</code>. Защитные ограничения допустимы как страховка, но не должны быть основным механизмом.
          </div>
          <div class="wrong-answer">
            <strong>Антипаттерн 3: Проверка текстового содержимого ассистента как индикатора завершения.</strong>
            Наличие текста в ответе ассистента НЕ означает, что он закончил. Claude может вернуть текст И вызов инструмента в одном ответе.
          </div>

          <h4>Почему результаты инструментов попадают в историю беседы</h4>
          <p>После выполнения инструмента вы должны добавить в <code>messages</code> и сообщение ассистента (с вызовом инструмента), и результат инструмента. Так Claude «видит» результат в следующей итерации. Без этого Claude не будет знать, что вернул инструмент, и будет рассуждать вслепую.</p>

          <div class="exam-tip">
            <strong>Совет к экзамену:</strong> Если вопрос спрашивает «что определяет, когда агентный цикл завершается», ответ всегда <code>stop_reason</code>. Если отвлекающие варианты упоминают «проверку, вернул ли ассистент текст», «подсчёт итераций цикла» или «парсинг ответа на фразы завершения» — все они неверны.
          </div>

          <h4>Связи со сценариями</h4>
          <p><span class="scenario-tag">Сценарий 1: Поддержка клиентов</span> Агент поддержки циклит: get_customer → lookup_order → process_refund → end_turn. Каждый результат инструмента информирует следующее решение.</p>
          <p><span class="scenario-tag">Сценарий 3: Мультиагентное исследование</span> Агентный цикл координатора делегирует субагентам через Task tool, собирая результаты до завершения исследования.</p>

          <h4>Глубокое понимание: Типы сообщений SDK в агентном цикле</h4>
          <p>Agent SDK использует специфические типы сообщений в жизненном цикле агентного цикла:</p>
          <ul>
            <li><strong>SystemMessage (init)</strong> — Самое первое сообщение в сессии, устанавливающее основу и метаданные. Появляется до любых пользовательских сообщений.</li>
            <li><strong>SystemMessage (compact_boundary)</strong> — Отправляется после сжатия контекста (когда система суммирует старые ходы для экономии места). Управляет состоянием окна с резюме удалённого контекста.</li>
            <li><strong>ResultMessage</strong> — <em>Финальное</em> сообщение SDK при завершении работы агента. Содержит текст ответа, общее использование токенов (вход/выход) и данные о стоимости. Используется для биллинга и отслеживания использования.</li>
          </ul>
          <p>Помимо <code>end_turn</code> и <code>tool_use</code>, знайте также <code>max_tokens</code> (вывод обрезан — добавьте частичное содержимое и продолжите) и <code>stop_sequence</code> (сработал пользовательский стоп-паттерн).</p>
        `
      },
      {
        id: "t1.2",
        title: "1.2: Оркестрация мультиагентных систем (координатор-субагент)",
        knowledge: [
          "Архитектура «звезда» (hub-and-spoke): координатор управляет ВСЕЙ межагентной коммуникацией, обработкой ошибок и маршрутизацией информации",
          "Субагенты имеют изолированный контекст — они НЕ наследуют историю беседы координатора автоматически",
          "Координатор обрабатывает: декомпозицию задач, делегирование, агрегацию результатов и решение о том, каких субагентов вызвать на основе сложности запроса",
          "Риск: слишком узкая декомпозиция задач приводит к неполному охвату широких тем"
        ],
        skills: [
          "Проектировать координаторов, которые анализируют требования запроса и динамически выбирают субагентов, а не всегда маршрутизируют через полный пайплайн",
          "Распределять область исследования между субагентами для минимизации дублирования (например, отдельные подтемы на агента)",
          "Реализовать циклы итеративного уточнения: координатор оценивает вывод синтеза на пробелы, повторно делегирует с целевыми запросами",
          "Маршрутизировать ВСЮ коммуникацию субагентов через координатор для наблюдаемости и согласованной обработки ошибок"
        ],
        keyPoint: "Субагенты изолированы. Координатор — единая точка контроля для всей коммуникации, делегирования и обработки ошибок.",
        details: `
          <h4>Архитектура «звезда» (Hub-and-Spoke)</h4>
          <p>Мультиагентные системы в Claude Agent SDK следуют паттерну <strong>«звезда»</strong> (также называемому координатор-субагент). Координатор — центральный узел; субагенты — лучи.</p>
          <pre><code>         ┌─────────────┐
         │ Координатор │  ← Центральный узел
         └──────┬──────┘
           ┌────┼────┐
           ▼    ▼    ▼
        [Веб] [Доки] [Синтез]  ← Субагенты (лучи)</code></pre>
          <p>Координатор обрабатывает: <strong>декомпозицию задач</strong> (разбиение запроса на части), <strong>делегирование</strong> (назначение частей субагентам), <strong>агрегацию результатов</strong> (объединение выходов) и <strong>обработку ошибок</strong> (восстановление после сбоев).</p>

          <h4>Принцип изоляции</h4>
          <p>Это наиболее тестируемая концепция в этом задании: <strong>субагенты имеют изолированный контекст</strong>. Они НЕ наследуют автоматически:</p>
          <ul>
            <li>Историю беседы координатора</li>
            <li>Результаты других субагентов</li>
            <li>Предыдущие результаты вызовов инструментов координатора</li>
            <li>Любую «общую память» — неявного общего состояния не существует</li>
          </ul>
          <p>Если субагенту синтеза нужны результаты субагента веб-поиска, координатор должен <em>явно включить</em> эти результаты в промпт субагента синтеза.</p>

          <h4>Динамический выбор субагентов</h4>
          <p>Хорошо спроектированный координатор не всегда вызывает каждого субагента. Он анализирует запрос и выбирает только релевантных:</p>
          <ul>
            <li>Простой фактический запрос → возможно, только субагент веб-поиска</li>
            <li>Сложная исследовательская тема → веб-поиск + анализ документов + синтез</li>
            <li>Неоднозначный запрос → запросить уточнение у пользователя перед делегированием</li>
          </ul>
          <div class="wrong-answer">
            <strong>Антипаттерн: Всегда маршрутизировать через полный пайплайн.</strong>
            Если координатор вызывает каждого субагента для каждого запроса независимо от сложности, это расточительно и может давать результаты более низкого качества. Координатор должен принимать решения о динамической маршрутизации.
          </div>

          <h4>Итеративное уточнение</h4>
          <p>После синтеза координатор должен оценить вывод на пробелы. Если пробелы найдены, он повторно делегирует с целевыми запросами:</p>
          <pre><code>1. Координатор делегирует исследование субагентам
2. Координатор получает результаты, вызывает субагент синтеза
3. Координатор оценивает синтез → находит пробел в «данных о ценах»
4. Координатор повторно делегирует веб-поиску: «Найти цены на X»
5. Координатор повторно вызывает синтез с новыми данными
6. Цикл до достаточного покрытия</code></pre>

          <h4>Маршрутизация коммуникации</h4>
          <p>ВСЯ коммуникация субагентов проходит через координатор. Субагенты никогда не общаются друг с другом напрямую. Это обеспечивает:</p>
          <ul>
            <li><strong>Наблюдаемость</strong> — координатор видит всё</li>
            <li><strong>Обработку ошибок</strong> — координатор может повторить, перенаправить или эскалировать</li>
            <li><strong>Контроль потока информации</strong> — координатор фильтрует и маршрутизирует релевантную информацию</li>
          </ul>

          <div class="exam-tip">
            <strong>Совет к экзамену:</strong> Если вопрос описывает субагентов, общающихся напрямую друг с другом или использующих общее хранилище памяти, это неправильный ответ. Всё проходит через координатор.
          </div>

          <h4>Связи со сценариями</h4>
          <p><span class="scenario-tag">Сценарий 3: Мультиагентное исследование</span> Это основной сценарий. Координатор делегирует субагентам веб-поиска, анализа документов, синтеза и генерации отчётов.</p>
          <p><span class="scenario-tag">Сценарий 1: Поддержка клиентов</span> Даже одноагентная система поддержки может использовать субагентов для конкретных задач, таких как поиск в базе знаний или запросы биллинга.</p>

          <h4>Глубокое понимание: Паттерны инъекции контекста координатором</h4>
          <p>Экзамен проверяет <em>как</em> контекст передаётся от координатора субагенту. Есть три паттерна, и только один правильный:</p>
          <pre><code>// НЕВЕРНО — неявное наследование (не существует)
task("Синтезировать исследование")
// Субагент НЕ имеет доступа к результатам веб-поиска

// НЕВЕРНО — ссылка на общую память
task("Прочитать находки из shared_state.research_results")
// Общей памяти между агентами не существует

// ВЕРНО — явная инъекция контекста
task("Синтезировать эти находки в отчёт:
  Источник 1 (Bloomberg): Выручка выросла на 23% г/г...
  Источник 2 (Reuters): Доля рынка достигла 34%...
  Источник 3 (SEC Filing): Операционная маржа 18.5%...")
// Полные находки встроены непосредственно в промпт</code></pre>
          <p><strong>Ловушка экзамена:</strong> Отвлекающие варианты часто описывают «общие хранилища контекста», «межагентные очереди сообщений» или «глобальные объекты состояния». Ничего из этого не существует в Agent SDK. Координатор вручную копирует релевантные данные в промпт каждого субагента.</p>
          <p>Более тонкая ловушка: координатор НЕ должен передавать <em>всю</em> историю беседы субагенту. Он должен передавать только <strong>релевантные находки</strong> — передача всего тратит токены контекста и размывает фокус субагента.</p>
        `
      },
      {
        id: "t1.3",
        title: "1.3: Настройка вызова субагентов, передачи контекста и порождения",
        knowledge: [
          "Task tool — механизм порождения субагентов; allowedTools должен включать 'Task' для координаторов",
          "Контекст субагента должен быть явно предоставлен в промпте — нет автоматического наследования или общей памяти",
          "Конфигурация AgentDefinition включает описания, системные промпты и ограничения инструментов для каждого субагента",
          "fork_session для исследования расходящихся подходов от общей базовой линии"
        ],
        skills: [
          "Включать полные находки предыдущих агентов непосредственно в промпт субагента",
          "Использовать структурированные форматы данных для разделения контента и метаданных (URL источников, имена документов, номера страниц) для атрибуции",
          "Порождать параллельных субагентов, отправляя несколько вызовов Task tool в одном ответе координатора",
          "Проектировать промпты координатора с целями исследования и критериями качества, а не пошаговыми инструкциями"
        ],
        keyPoint: "Контекст должен быть ЯВНО передан субагентам. Параллельное порождение = несколько вызовов Task в ОДНОМ ответе.",
        details: `
          <h4>Task Tool</h4>
          <p>В Agent SDK <strong>Task tool</strong> — это способ порождения субагентов координатором. Чтобы координатор мог делегировать, его <code>allowedTools</code> должен включать <code>"Task"</code>.</p>
          <pre><code>// AgentDefinition для координатора
{
  name: "coordinator",
  description: "Управляет рабочим процессом исследования",
  allowedTools: ["Task", "Read", "Write"],  // Task = может порождать субагентов
  systemPrompt: "Вы координируете исследование, делегируя субагентам..."
}</code></pre>

          <h4>Явная передача контекста (золотое правило)</h4>
          <p>Субагенты имеют <strong>нулевой автоматический контекст</strong>. Всё, что им нужно, должно быть в промпте. Это означает:</p>
          <ul>
            <li>Если субагенту синтеза нужны результаты веб-поиска, включите полные результаты в его промпт</li>
            <li>Если субагенту нужно знать о предыдущих находках, вставьте эти находки в промпт</li>
            <li>Метаданные (URL источников, имена документов, номера страниц) должны быть явно включены для атрибуции</li>
          </ul>
          <pre><code>// ПЛОХО — предполагает, что у субагента есть контекст
coordinator: "Task: Синтезировать результаты исследования"

// ХОРОШО — включает всё, что нужно субагенту
coordinator: "Task: Синтезировать эти находки в отчёт.
  Результаты веб-поиска: [полные результаты здесь]
  Анализ документов: [полный анализ здесь]
  Требуемый формат: Резюме + детальные разделы
  Включить атрибуцию источника для каждого утверждения."</code></pre>

          <h4>Структурированные данные для атрибуции</h4>
          <p>При передаче контекста между агентами отделяйте <strong>контент</strong> от <strong>метаданных</strong>:</p>
          <pre><code>{
  "finding": "Выручка выросла на 23% г/г",
  "source_url": "https://example.com/report",
  "document": "Финансовый отчёт Q4",
  "page": 12,
  "date": "2024-01-15"
}</code></pre>
          <p>Эта структура обеспечивает сохранение атрибуции на этапе синтеза. Без неё субагент синтеза может создавать утверждения без источников.</p>

          <h4>Параллельное порождение субагентов</h4>
          <p>Для параллельного порождения субагентов координатор отправляет <strong>несколько вызовов Task tool в одном ответе</strong>:</p>
          <pre><code>// Один ответ координатора содержит:
Tool call 1: Task("Искать в вебе тему A")
Tool call 2: Task("Искать в вебе тему B")
Tool call 3: Task("Проанализировать загруженный документ")
// Все три запускаются параллельно</code></pre>
          <div class="wrong-answer">
            <strong>Неверный подход:</strong> Порождение субагентов по одному в отдельных ходах. Это последовательно и медленно. Параллельное порождение означает несколько вызовов Task в ОДНОМ ответе.
          </div>

          <h4>fork_session</h4>
          <p><code>fork_session</code> создаёт независимую ветку от общей базовой линии. Используйте, когда хотите исследовать расходящиеся подходы, не загрязняя исходную сессию:</p>
          <ul>
            <li>Сравнить две стратегии рефакторинга на основе одного анализа кодовой базы</li>
            <li>Попробовать разные подходы к тестированию на основе общего понимания кода</li>
            <li>Исследовать сценарии «что если» без потери основной беседы</li>
          </ul>

          <h4>Проектирование промпта координатора</h4>
          <p>Промпты координатора должны указывать <strong>цели и критерии качества</strong>, а не пошаговые инструкции:</p>
          <div class="wrong-answer">
            <strong>Неверно:</strong> «Шаг 1: Вызвать web_search. Шаг 2: Вызвать analyze_document. Шаг 3: Вызвать synthesize.» — Это предварительно настроенный пайплайн, а не агентное поведение.
          </div>
          <p><strong>Верно:</strong> «Исследовать [тему]. Обеспечить охват последних разработок, конкурирующих точек зрения и количественных данных. Делегировать соответствующим субагентам. Доисследовать любые пробелы перед синтезом.»</p>

          <div class="exam-tip">
            <strong>Совет к экзамену:</strong> Вопросы о контексте субагентов всегда проверяют, знаете ли вы, что контекст должен быть ЯВНЫМ. Любой ответ, предполагающий «субагенты наследуют контекст координатора» или «субагенты делят память» — неверен.
          </div>

          <h4>Глубокое понимание: Команды агентов vs эфемерные субагенты</h4>
          <p>Экзамен различает два мультиагентных паттерна:</p>
          <table class="ref-table">
            <thead><tr><th>Аспект</th><th>Эфемерные субагенты (Task tool)</th><th>Команды агентов</th></tr></thead>
            <tbody>
              <tr><td><strong>Жизненный цикл</strong></td><td>Создаются для задачи, уничтожаются после возврата</td><td>Независимые долгоживущие экземпляры</td></tr>
              <tr><td><strong>Координация</strong></td><td>Hub-and-spoke: координатор порождает и собирает</td><td>Общий список задач / очередь сообщений</td></tr>
              <tr><td><strong>Обмен контекстом</strong></td><td>Явно в промпте (нулевое наследование)</td><td>Через общие артефакты (файлы, записи БД)</td></tr>
              <tr><td><strong>Лучше для</strong></td><td>Параллелизуемые подзадачи в одной сессии</td><td>Долгоживущие рабочие процессы между сессиями</td></tr>
            </tbody>
          </table>
          <p><strong>Ключевое различие:</strong> Субагенты — это <em>эфемерные потомки</em> координатора. Команды агентов — <em>независимые равноправные участники</em>, координирующиеся через общую рабочую очередь — ни один координатор ими не владеет.</p>
          <div class="wrong-answer">
            <strong>Внимание:</strong> Не путайте команды агентов с веерным порождением субагентов. Если экзамен описывает «независимые экземпляры, координирующиеся через общий список задач» — это команды агентов. Если говорит «координатор порождает параллельных работников» — это веерное порождение субагентов.
          </div>

          <h4>Глубокое понимание: Динамическая декомпозиция vs цепочка промптов</h4>
          <p>Два паттерна рабочих процессов, которые тестирует экзамен:</p>
          <ul>
            <li><strong>Цепочка промптов (фиксированный пайплайн):</strong> Предопределённые последовательные шаги. Шаг 1 → Шаг 2 → Шаг 3. Последовательность никогда не меняется независимо от входных данных. Подходит для детерминированных, хорошо понятных рабочих процессов (например, извлечение → валидация → преобразование → загрузка).</li>
            <li><strong>Динамическая декомпозиция (адаптивное планирование):</strong> Агент анализирует задачу, создаёт план, выполняет его и <em>перепланирует</em>, если промежуточные результаты выявляют новые подзадачи. Последовательность адаптируется к входным данным.</li>
          </ul>
          <pre><code>// Цепочка промптов (фиксированная):
Шаг 1: Извлечь сущности
Шаг 2: Валидировать сущности
Шаг 3: Сгенерировать отчёт
// Всегда эти 3 шага, всегда в этом порядке

// Динамическая декомпозиция (адаптивная):
Агент анализирует задачу → решает, что нужно 5 подзадач
Агент завершает подзадачу 3 → обнаруживает новую зависимость
Агент перепланирует → добавляет подзадачу 3б
Агент продолжает с пересмотренным планом</code></pre>
          <div class="exam-tip">
            <strong>Совет к экзамену:</strong> Если сценарий говорит «рабочий процесс должен адаптироваться на основе промежуточных результатов» или «агент должен перепланировать при обнаружении новой информации» — ответ динамическая декомпозиция. Если говорит «чётко определённые последовательные шаги, которые не меняются» — это цепочка промптов. Ключевое слово — <strong>адаптивный</strong>.
          </div>
        `
      },
      {
        id: "t1.4",
        title: "1.4: Многошаговые рабочие процессы с обеспечением соблюдения и передачей",
        knowledge: [
          "Программное обеспечение соблюдения (хуки, шлюзы предварительных условий) vs руководство на основе промптов для упорядочивания рабочих процессов",
          "Когда требуется детерминированное соблюдение (верификация личности перед финансовыми операциями), промпты одни имеют ненулевой процент сбоев",
          "Структурированные протоколы передачи для эскалации: данные клиента, анализ первопричин, рекомендуемые действия"
        ],
        skills: [
          "Реализовать программные предварительные условия, блокирующие нижестоящие инструменты до завершения предварительных (например, блокировать process_refund до получения подтверждённого ID от get_customer)",
          "Декомпозировать мультипроблемные запросы на отдельные элементы, исследовать каждый параллельно, затем синтезировать единое решение",
          "Составлять структурированные резюме передачи (ID клиента, первопричина, сумма, рекомендуемое действие) при эскалации людям"
        ],
        keyPoint: "Для КРИТИЧЕСКИХ бизнес-правил используйте программное обеспечение соблюдения (хуки), а не промпт-инструкции. Промпты вероятностны; хуки детерминированы.",
        details: `
          <h4>Программное vs промпт-основанное обеспечение соблюдения</h4>
          <p>Это одно из важнейших различий на экзамене. Есть два способа обеспечить порядок рабочего процесса:</p>
          <ul>
            <li><strong>На основе промпта:</strong> Сказать Claude в системном промпте «всегда верифицируй личность перед обработкой возвратов». Это <em>вероятностно</em> — работает большую часть времени, но имеет ненулевой процент сбоев.</li>
            <li><strong>Программное:</strong> Использовать хуки или шлюзы предварительных условий в коде для <em>блокировки</em> нижестоящих инструментов до завершения предварительных. Это <em>детерминировано</em> — физически не может быть обойдено.</li>
          </ul>
          <pre><code>// Программный шлюз предварительных условий
function beforeToolCall(toolName, args) {
  if (toolName === "process_refund") {
    if (!state.customerVerified) {
      return { blocked: true, reason: "Сначала нужно верифицировать клиента" };
    }
  }
  return { blocked: false };
}</code></pre>

          <h4>Когда какой подход уместен</h4>
          <ul>
            <li><strong>Программное обеспечение:</strong> Финансовые операции, верификация личности, критичные для безопасности шлюзы, требования комплаенса — всё, где сбой имеет реальные последствия.</li>
            <li><strong>Руководство на основе промпта:</strong> Мягкие предпочтения, стилистические рекомендации, рекомендуемый (но не обязательный) порядок.</li>
          </ul>

          <h4>Декомпозиция мультипроблемных запросов</h4>
          <p>Когда клиент поднимает несколько вопросов в одном сообщении («Мне нужен возврат за заказ #123 И мой адрес доставки неверен в заказе #456»), агент должен:</p>
          <ol>
            <li>Декомпозировать запрос на отдельные элементы</li>
            <li>Исследовать каждый параллельно с использованием общего контекста</li>
            <li>Синтезировать единое решение, адресующее все проблемы</li>
          </ol>

          <h4>Структурированные резюме передачи</h4>
          <p>При эскалации живому агенту составьте структурированное резюме, поскольку человек обычно <strong>не видит транскрипт беседы</strong>:</p>
          <pre><code>{
  "customer_id": "C-12345",
  "root_cause": "Возврат превышает лимит политики $500",
  "order_id": "ORD-67890",
  "refund_amount": "$723.50",
  "recommended_action": "Одобрить исключение — лояльный клиент, 3 года истории",
  "steps_completed": ["Личность верифицирована", "Заказ подтверждён", "Возврат инициирован"],
  "customer_sentiment": "Разочарован, но сотрудничает"
}</code></pre>
          <div class="wrong-answer">
            <strong>Неверный подход:</strong> Эскалация с «Клиенту нужна помощь с возвратом». У живого агента нет контекста, и ему придётся начинать сначала.
          </div>

          <div class="exam-tip">
            <strong>Совет к экзамену:</strong> Когда вопрос спрашивает об обеспечении того, чтобы верификация личности всегда происходила перед финансовыми операциями, правильный ответ включает программное обеспечение (хуки/шлюзы), а НЕ промпт-инструкции. Промпты вероятностны и имеют ненулевой процент сбоев для критических операций.
          </div>

          <h4>Связи со сценариями</h4>
          <p><span class="scenario-tag">Сценарий 1: Поддержка клиентов</span> Это основной сценарий. Агент должен верифицировать личность через <code>get_customer</code> перед вызовом <code>process_refund</code>. Программные шлюзы обеспечивают это.</p>

          <h4>Глубокое понимание: Матрица решений хук vs промпт</h4>
          <p>Экзамен любит проверять, выбираете ли вы хуки или промпты для данного сценария. Используйте эту матрицу:</p>
          <pre><code>Используйте ХУКИ (программные), когда:
  ✅ Финансовые операции (возвраты, платежи, переводы)
  ✅ Предварительные условия верификации личности
  ✅ Критичные для безопасности шлюзы (проверки разрешений)
  ✅ Требования комплаенса (порядок, требуемый аудитом)
  ✅ Сбой имеет юридические/финансовые последствия
  ✅ Нужно 100% гарантированное соблюдение

Используйте ПРОМПТЫ (руководство), когда:
  ✅ Предпочтения тона/стиля («будь эмпатичным»)
  ✅ Предложения по формату ответа
  ✅ Мягкие рекомендации по рабочему процессу
  ✅ Некритичные предпочтения порядка
  ✅ Сбой неудобен, но не вреден</code></pre>
          <p><strong>Ловушка 12%:</strong> Экзамен часто описывает сценарии, где промпт-основанное обеспечение работает «большую часть времени» (88-95%), но сбоит в критических крайних случаях. Ключевой инсайт: для финансовых/секьюрити операций даже 5% процент сбоев неприемлем. Правильный ответ — всегда хуки для критической бизнес-логики, независимо от того, насколько хорошо промпты «обычно» работают.</p>
          <p><strong>Тонкость декомпозиции мультипроблем:</strong> Когда клиент поднимает 3 проблемы, исследуйте все 3 параллельно с общим контекстом (не последовательно). Но синтезируйте в ОДИН единый ответ, адресующий все проблемы. Экзамен проверяет, понимаете ли вы параллельное исследование с единым решением.</p>
        `
      },
      {
        id: "t1.5",
        title: "1.5: Хуки Agent SDK для перехвата и нормализации",
        knowledge: [
          "PostToolUse хуки перехватывают результаты инструментов для преобразования до того, как модель их обработает",
          "Хуки перехвата вызовов инструментов обеспечивают соблюдение правил комплаенса (например, блокировка возвратов выше порога)",
          "Хуки обеспечивают детерминированные гарантии; промпт-инструкции обеспечивают лишь вероятностное соблюдение"
        ],
        skills: [
          "Реализовать PostToolUse хуки для нормализации форматов данных (Unix timestamps, ISO 8601, коды статусов) от различных MCP-инструментов",
          "Реализовать перехват вызовов инструментов для блокировки действий, нарушающих политику, и перенаправления на альтернативные рабочие процессы",
          "Выбирать хуки вместо промпт-основанного обеспечения, когда бизнес-правила требуют гарантированного соблюдения"
        ],
        keyPoint: "Хуки = гарантированное соблюдение. Промпты = соблюдение по мере возможности. Для финансовых/безопасностных операций всегда используйте хуки.",
        details: `
          <h4>Что такое хуки Agent SDK?</h4>
          <p>Хуки — это перехватчики на уровне кода, которые выполняются в определённых точках жизненного цикла агента. Они дают вам <strong>детерминированный контроль</strong> над тем, что агент может делать, независимо от того, что говорит промпт.</p>

          <h4>Ключевые типы хуков</h4>
          <ul>
            <li><strong>PostToolUse хуки:</strong> Выполняются ПОСЛЕ того, как инструмент вернул результат, но ДО того, как Claude его обработает. Используйте для нормализации и преобразования данных.</li>
            <li><strong>Хуки перехвата вызовов инструментов:</strong> Выполняются ДО выполнения инструмента. Используйте для блокировки действий, нарушающих политику.</li>
          </ul>

          <h4>PostToolUse: Нормализация данных</h4>
          <p>Когда ваш агент вызывает несколько MCP-инструментов, возвращающих данные в разных форматах, PostToolUse хуки нормализуют всё до того, как Claude увидит:</p>
          <pre><code>// PostToolUse hook — normalize dates
function postToolUse(toolName, result) {
  if (result.timestamp) {
    // Convert Unix timestamp → ISO 8601
    result.timestamp = new Date(result.timestamp * 1000).toISOString();
  }
  if (result.status_code) {
    // Convert numeric status → human-readable
    result.status = STATUS_MAP[result.status_code];
  }
  return result;  // Claude sees the normalized version
}</code></pre>
          <p>Почему это важно: если один MCP-инструмент возвращает <code>1703980800</code> (Unix timestamp), а другой возвращает <code>"2024-01-01T00:00:00Z"</code> (ISO 8601), Claude может запутаться. Хук нормализует оба в единый формат.</p>

          <h4>Перехват вызовов инструментов: Обеспечение политик</h4>
          <p>Хуки перехвата блокируют вызовы инструментов, нарушающие бизнес-правила, ДО их выполнения:</p>
          <pre><code>// Tool call interception — block large refunds
function beforeToolCall(toolName, args) {
  if (toolName === "process_refund" && args.amount > 500) {
    return {
      blocked: true,
      redirect: "escalate_to_human",
      reason: "Refunds over $500 require human approval"
    };
  }
  return { blocked: false };
}</code></pre>
          <p>Возврат физически не может пройти. Не важно, что говорит промпт или как Claude рассуждает — хук его блокирует.</p>

          <h4>Хуки vs Промпты: Ключевое различие</h4>
          <ul>
            <li><strong>Хуки:</strong> Детерминированные. 100% соблюдение. Не могут быть обойдены моделью. Используйте для финансовых лимитов, правил безопасности, требований комплаенса.</li>
            <li><strong>Промпты:</strong> Вероятностные. Высокое, но не идеальное соблюдение. Модель может иногда игнорировать инструкции. Используйте для мягких рекомендаций и предпочтений.</li>
          </ul>

          <div class="wrong-answer">
            <strong>Распространённый неправильный ответ:</strong> «Добавить системную промпт-инструкцию, запрещающую Claude обрабатывать возвраты свыше $500.» Это вероятностно — Claude обычно подчинится, но это не гарантировано. Для финансовых операций используйте хуки.
          </div>

          <div class="exam-tip">
            <strong>Совет к экзамену:</strong> Экзамен любит спрашивать «как вы ГАРАНТИРУЕТЕ соблюдение [бизнес-правила]?» Ответ всегда хуки, никогда не промпт-инструкции. Если вы видите «гарантировано» или «детерминировано» в вопросе, думайте хуки.
          </div>

          <h4>Связи со сценариями</h4>
          <p><span class="scenario-tag">Сценарий 1: Поддержка клиентов</span> PostToolUse нормализует данные клиентов из разных бэкенд-систем. Хуки перехвата блокируют возвраты выше порога и перенаправляют на эскалацию к человеку.</p>

          <h4>Глубокое понимание: Полный жизненный цикл хуков</h4>
          <p>Claude Code поддерживает четыре точки хуков, каждая срабатывающая на разной стадии:</p>
          <ul>
            <li><strong>PreToolUse:</strong> До выполнения инструмента — блокировка редактирования защищённых файлов, обеспечение предусловий, валидация параметров</li>
            <li><strong>PostToolUse:</strong> После возврата инструмента, до обработки моделью — автоформатирование/линтинг после Edit, нормализация данных, фильтрация многословного вывода (например, <code>cargo test</code> сокращён до pass/fail)</li>
            <li><strong>SessionStart:</strong> При начале сессии — инъекция динамического контекста: git branch, переменные окружения, состояние проекта</li>
            <li><strong>Notification:</strong> При завершении задачи — push-уведомление (например, macOS-уведомление о завершении задачи)</li>
          </ul>
          <p><strong>Ключевое преимущество:</strong> Хуки выполняются <strong>вне контекста</strong> — они стоят ноль токенов. Это делает их идеальным слоем для детерминированного управления, аудит-логирования и фильтрации вывода.</p>
        `
      },
      {
        id: "t1.6",
        title: "1.6: Стратегии декомпозиции задач",
        knowledge: [
          "Фиксированные последовательные конвейеры (цепочки промптов) vs динамическая адаптивная декомпозиция на основе промежуточных находок",
          "Цепочки промптов: разбить ревью на последовательные шаги (по-файлово, затем кросс-файловый интеграционный проход)",
          "Адаптивные планы генерируют подзадачи на основе того, что обнаружено на каждом шаге"
        ],
        skills: [
          "Выбирать цепочки промптов для предсказуемых многоаспектных ревью; динамическую декомпозицию для открытых исследований",
          "Разделять большие код-ревью на по-файловые локальные проходы + отдельный кросс-файловый интеграционный проход для избежания размывания внимания",
          "Декомпозировать открытые задачи: сначала картирование структуры, выявление зон высокого влияния, затем создание приоритизированного адаптивного плана"
        ],
        keyPoint: "По-файловый анализ + кросс-файловый интеграционный проход предотвращает размывание внимания и противоречивые выводы в больших ревью.",
        details: `
          <h4>Две стратегии декомпозиции</h4>
          <p>Экзамен проверяет вашу способность выбрать правильную стратегию декомпозиции для ситуации:</p>
          <ul>
            <li><strong>Цепочка промптов (фиксированный последовательный конвейер):</strong> Предсказуемые, упорядоченные шаги, где вывод каждого шага подаётся на следующий. Хорошо для структурированных, повторяемых рабочих процессов.</li>
            <li><strong>Динамическая адаптивная декомпозиция:</strong> План эволюционирует на основе того, что обнаружено на каждом шаге. Хорошо для открытых задач исследования.</li>
          </ul>

          <h4>Пример цепочки промптов: Код-ревью</h4>
          <pre><code>Step 1: Per-file analysis (each file independently)
  ├── file_a.py → local issues (bugs, style, complexity)
  ├── file_b.py → local issues
  └── file_c.py → local issues

Step 2: Cross-file integration pass
  └── Analyze data flow ACROSS files
      (API contracts, shared state, import chains)</code></pre>
          <p>Почему разделять? <strong>Размывание внимания.</strong> Если попросить Claude ревьюить 10 файлов одновременно, он может пропустить проблемы в средних файлах (эффект «потерянного в середине») или выдать противоречивые выводы. По-файловые проходы фокусируют внимание; интеграционный проход ловит кросс-файловые проблемы.</p>

          <h4>Пример динамической декомпозиции: Тестирование легаси-кодовой базы</h4>
          <pre><code>1. Map the codebase structure (discover modules, dependencies)
2. Identify high-impact areas (most changed, most depended on)
3. Create prioritized test plan based on findings
4. Start writing tests — adapt plan as you discover:
   - Hidden dependencies you didn't expect
   - Code paths that need refactoring before testing
   - Integration points that need mock setup</code></pre>
          <p>План на шаге 4 выглядит иначе, чем то, что вы планировали бы на шаге 1, потому что вы узнали новое по ходу.</p>

          <h4>Когда использовать каждую стратегию</h4>
          <ul>
            <li><strong>Цепочка промптов:</strong> Код-ревью, конвейеры обработки документов, многоаспектные оценки, извлечение данных → преобразование → валидация</li>
            <li><strong>Динамическая декомпозиция:</strong> «Добавить комплексные тесты», «исследовать этот баг», «исследовать эту кодовую базу», исследовательские задачи</li>
          </ul>

          <div class="wrong-answer">
            <strong>Неправильный подход:</strong> Использование фиксированного конвейера для открытого исследования («Шаг 1: Прочитать все файлы. Шаг 2: Написать все тесты.»). Открытые задачи требуют адаптивных планов, реагирующих на находки.
          </div>

          <div class="exam-tip">
            <strong>Совет к экзамену:</strong> Если вопрос упоминает большое код-ревью с множеством файлов, правильный ответ почти всегда включает разделение на по-файловые проходы + кросс-файловый интеграционный проход. Это каноничный паттерн для избежания размывания внимания.
          </div>

          <h4>Связи со сценариями</h4>
          <p><span class="scenario-tag">Сценарий 5: CI/CD код-ревью</span> Автоматизированные код-ревью в CI должны использовать по-файловый анализ + кросс-файловые интеграционные проходы.</p>
          <p><span class="scenario-tag">Сценарий 4: Продуктивность разработчика</span> Исследование незнакомых кодовых баз использует динамическую декомпозицию — адаптация плана по мере обнаружения структуры.</p>

          <h4>Глубокое понимание: Порог размывания внимания</h4>
          <p>Экзамен проверяет <em>границу</em> между тем, когда нужна однопроходная и многопроходная декомпозиция:</p>
          <pre><code>Single-pass review (no decomposition needed):
  ✅ 1-3 files, < 500 lines total
  ✅ All files in the same module/directory
  ✅ Changes are self-contained

Multi-pass decomposition REQUIRED:
  ✅ 4+ files OR > 1000 lines total
  ✅ Files span multiple modules/services
  ✅ Cross-cutting changes (API contract + client + tests)
  ✅ "Lost in the middle" risk is high</code></pre>
          <p><strong>Ловушка экзамена — цепочка промптов vs динамическая декомпозиция:</strong> Если вопрос описывает задачу с известными, предсказуемыми шагами (код-ревью, конвейер данных, обработка документов), ответ — <strong>цепочка промптов</strong>. Если вопрос описывает исследование, расследование или отладку с неизвестным объёмом, ответ — <strong>динамическая декомпозиция</strong>. Ловушка: использование фиксированного конвейера для открытого исследования блокирует вас в плане, который становится неправильным по мере обнаружения новой информации.</p>
          <p><strong>Кросс-файловый интеграционный проход</strong> специально ищет: несоответствия API-контрактов между вызывающим и вызываемым, мутации общего состояния между модулями, корректность цепочек импортов и несоответствия типов данных на границах. По-файловые проходы не могут это обнаружить, потому что видят только один файл.</p>
        `
      },
      {
        id: "t1.7",
        title: "1.7: Состояние сессии, возобновление и ветвление",
        knowledge: [
          "--resume <session-name> продолжает конкретную предыдущую беседу",
          "fork_session создаёт независимые ветки от общей аналитической базы",
          "Информируйте агента об изменениях в ранее проанализированных файлах при возобновлении",
          "Начать заново со структурированным резюме надёжнее, чем возобновлять с устаревшими результатами инструментов"
        ],
        skills: [
          "Использовать --resume с именами сессий для продолжения именованных исследовательских сессий",
          "Использовать fork_session для сравнения подходов (например, две стратегии тестирования от общего анализа кодовой базы)",
          "Выбирать возобновление, когда предыдущий контекст в основном валиден; новый старт с резюме, когда результаты инструментов устарели",
          "Информировать возобновлённые сессии о конкретных изменениях файлов для целевого повторного анализа"
        ],
        keyPoint: "Возобновляйте, когда контекст ещё валиден. Начинайте заново с инъецированными резюме, когда старые результаты инструментов устарели.",
        details: `
          <h4>Варианты управления сессиями</h4>
          <p>Claude Code предоставляет три стратегии управления сессиями:</p>
          <ul>
            <li><code>--resume &lt;session-name&gt;</code> — Продолжить существующую именованную сессию с полной историей</li>
            <li><code>fork_session</code> — Ответвиться от общей базы для исследования расходящихся подходов</li>
            <li><strong>Новый старт с резюме</strong> — Начать новую сессию, но инъецировать ключевые находки из предыдущей</li>
          </ul>

          <h4>Когда возобновлять vs начинать заново</h4>
          <pre><code>Resume (--resume):
  ✅ Prior context is mostly still valid
  ✅ Files haven't changed much since last session
  ✅ You want to continue exactly where you left off
  ✅ Named session: --resume my-investigation

Start Fresh with Summaries:
  ✅ Old tool results are stale (files changed, data updated)
  ✅ Context window was near exhaustion
  ✅ You want a clean slate but need key findings
  ✅ More reliable than resuming with outdated info</code></pre>
          <p>Ключевой компромисс: возобновление сохраняет полный контекст, но рискует устаревшими данными. Новый старт избегает устаревших данных, но теряет детальную историю (что компенсируется структурированными резюме).</p>

          <h4>fork_session: Исследование расходящихся подходов</h4>
          <p><code>fork_session</code> создаёт независимую ветку от текущего состояния сессии. И оригинал, и форк продолжают независимо:</p>
          <pre><code>Session: "analyze-codebase"
  │
  ├── fork: "approach-A" (try refactoring with strategy A)
  └── fork: "approach-B" (try refactoring with strategy B)
  
Both forks share the same codebase analysis as their starting point,
but they diverge from there.</code></pre>
          <p>Случаи использования: сравнение стратегий тестирования, оценка различных подходов к рефакторингу, исследования «что если».</p>

          <h4>Информирование возобновлённых сессий об изменениях</h4>
          <p>При возобновлении после модификации кода сообщите Claude, что изменилось:</p>
          <pre><code>// Good: specific about changes
"Since our last session, I've modified auth.py to add 
rate limiting (lines 45-60) and updated the user model 
in models.py. Please re-analyze these files."

// Bad: vague
"Some things changed, please look again."</code></pre>
          <p>Конкретные описания изменений обеспечивают <strong>целевой повторный анализ</strong> вместо необходимости полного повторного исследования всей кодовой базы.</p>

          <div class="wrong-answer">
            <strong>Неправильный подход:</strong> Всегда возобновлять старые сессии, даже когда файлы были сильно изменены. Устаревшие результаты инструментов (например, содержимое файлов, которое больше не совпадает) приводят к некорректным рассуждениям и рекомендациям.
          </div>

          <div class="exam-tip">
            <strong>Совет к экзамену:</strong> Экзамен проверяет компромисс между возобновлением и новым стартом. Если вопрос упоминает «файлы были изменены с последней сессии» или «результаты инструментов устарели», правильный ответ обычно «начать заново со структурированными резюме», а не «возобновить старую сессию».
          </div>

          <h4>Связи со сценариями</h4>
          <p><span class="scenario-tag">Сценарий 4: Продуктивность разработчика</span> Длительные сессии исследования кодовой базы выигрывают от именованных сессий (<code>--resume</code>) и fork_session для сравнения подходов.</p>
          <p><span class="scenario-tag">Сценарий 2: Генерация кода</span> Многодневная разработка может требовать возобновления сессий или нового старта с резюме в зависимости от того, сколько кода изменилось за ночь.</p>

          <h4>Глубокое понимание: HANDOFF.md и стратегии непрерывности сессий</h4>
          <p>Экзамен проверяет, понимаете ли вы <strong>трёхуровневую иерархию</strong> стратегий непрерывности сессий, от простейшей до самой надёжной:</p>
          <pre><code>Strategy 1: --resume (simplest)
  When: same day, files unchanged, context not exhausted
  Risk: stale tool outputs if files changed

Strategy 2: HANDOFF.md pattern (recommended for multi-day)
  When: ending a session, context near limit, switching days
  Before ending: Claude writes HANDOFF.md with:
    - Current progress and findings
    - Architectural decisions made (and WHY)
    - What was tried and failed
    - Open issues and recommended next steps
  Next session: "Read HANDOFF.md and continue"

Strategy 3: Compact Instructions in CLAUDE.md (for ongoing)
  When: project-level decisions that must survive ALL sessions
  Add to CLAUDE.md: "Always preserve during compaction:
    - Auth uses JWT with 30min expiry
    - Database is PostgreSQL via Prisma
    - API follows REST conventions in /api/v2/"</code></pre>
          <p><strong>Ловушка экзамена — тайминг fork_session:</strong> Форк можно сделать только от <em>текущего</em> состояния сессии. Нельзя сделать форк из исторической точки. Если нужно сравнить подходы, делайте форк ДО начала реализации, а не после. После реализации подхода A форк будет включать все изменения подхода A.</p>
          <p><strong>Флаг --worktree:</strong> При ветвлении сессий, включающих модификацию файлов, <code>--worktree</code> создаёт отдельное git worktree, чтобы файловые изменения форкнутой сессии не конфликтовали с основной сессией. Используйте <code>sparsePaths</code> для ограничения файлов, которые worktree выгружает.</p>
        `
      }
    ]
  },
  {
    id: "d2",
    domain: 2,
    title: "Проектирование инструментов и интеграция MCP",
    weight: 18,
    tagClass: "tag-d2",
    tasks: [
      {
        id: "t2.1",
        title: "2.1: Проектирование эффективных интерфейсов инструментов",
        knowledge: [
          "Описания инструментов — ОСНОВНОЙ механизм, который LLM используют для выбора инструментов — минимальные описания ведут к ненадёжному выбору",
          "Включайте форматы входных данных, примеры запросов, граничные случаи и объяснения границ в описаниях",
          "Неоднозначные или перекрывающиеся описания вызывают ошибки маршрутизации (например, analyze_content vs analyze_document)",
          "Формулировки системного промпта с чувствительными к ключевым словам инструкциями могут создавать непреднамеренные ассоциации с инструментами"
        ],
        skills: [
          "Писать описания, различающие назначение каждого инструмента, входные/выходные данные и когда использовать его vs альтернативы",
          "Переименовывать инструменты и обновлять описания для устранения функционального перекрытия",
          "Разделять универсальные инструменты на целевые с определёнными контрактами ввода/вывода",
          "Проверять системные промпты на чувствительные к ключевым словам инструкции, переопределяющие описания инструментов"
        ],
        keyPoint: "Когда инструменты выбираются неправильно, ПЕРВОЕ, что нужно исправить — это описания инструментов, а не слои маршрутизации или консолидация.",
        details: `
          <h4>Почему описания инструментов так важны</h4>
          <p>Описания инструментов — <strong>основной механизм</strong>, который Claude использует для выбора инструмента. Думайте об описаниях как о «резюме» инструмента — Claude их читает и решает, какой инструмент лучше подходит для задачи. Если описания размыты, Claude делает плохой выбор.</p>

          <h4>Анатомия хорошего описания инструмента</h4>
          <pre><code>// BAD — minimal description
{
  name: "analyze_content",
  description: "Analyzes content"
}

// GOOD — comprehensive description
{
  name: "extract_web_results",
  description: "Extracts structured data from web search results. 
    Input: raw HTML or search API response. 
    Output: array of {title, url, snippet, date}.
    Use this for web search results ONLY. 
    For uploaded documents, use extract_document_data instead.
    Handles: Google, Bing, and DuckDuckGo result formats."
}</code></pre>

          <h4>Распространённые проблемы с описаниями инструментов</h4>
          <ul>
            <li><strong>Перекрытие:</strong> <code>analyze_content</code> vs <code>analyze_document</code> с почти идентичными описаниями → Claude выбирает случайно</li>
            <li><strong>Слишком минимально:</strong> <code>"Analyzes data"</code> → Claude не может различить, когда использовать это vs альтернативы</li>
            <li><strong>Нет границ:</strong> Нет указания, когда НЕ использовать инструмент → Claude использует его для всего</li>
            <li><strong>Нет спецификации ввода/вывода:</strong> Claude отправляет неправильный формат или не знает, чего ожидать в ответ</li>
          </ul>

          <h4>Исправление проблем с выбором инструментов (порядок приоритетов)</h4>
          <ol>
            <li><strong>Первое:</strong> Улучшить описания инструментов — добавить назначение, входы, выходы, границы, примеры</li>
            <li><strong>Второе:</strong> Переименовать инструменты для устранения неоднозначности (например, <code>analyze_content</code> → <code>extract_web_results</code>)</li>
            <li><strong>Третье:</strong> Разделить универсальные инструменты на целевые с чёткими контрактами ввода/вывода</li>
            <li><strong>Четвёртое:</strong> Проверить системный промпт на чувствительные к ключевым словам инструкции, создающие непреднамеренные ассоциации</li>
          </ol>
          <div class="wrong-answer">
            <strong>Неправильный подход:</strong> Когда инструменты маршрутизируются неправильно, добавление слоя маршрутизации или консолидация инструментов в один мега-инструмент. Исправьте описания ПЕРВЫМ ДЕЛОМ — это почти всегда корневая причина.
          </div>

          <h4>Чувствительность системного промпта к ключевым словам</h4>
          <p>Формулировки системного промпта могут случайно создать ассоциации с инструментами:</p>
          <pre><code>// System prompt says: "When the user asks to analyze anything, 
// provide a thorough analysis."
// Problem: The word "analyze" makes Claude prefer any tool 
// with "analyze" in its name, overriding better tool matches.</code></pre>
          <p>Проверяйте системные промпты на ключевые слова, совпадающие с именами инструментов — они могут переопределять хорошо написанные описания.</p>

          <div class="exam-tip">
            <strong>Совет к экзамену:</strong> Если сценарий описывает неправильный выбор инструментов и спрашивает, что исправить первым, ответ ВСЕГДА — описания инструментов. Не логику маршрутизации, не консолидацию инструментов, не добавление новых инструментов. Сначала описания.
          </div>

          <h4>Связи со сценариями</h4>
          <p><span class="scenario-tag">Сценарий 3: Мультиагентное исследование</span> Инструмент веб-поиска vs инструмент анализа документов должны иметь чёткие описания, чтобы координатор маршрутизировал правильно.</p>
          <p><span class="scenario-tag">Сценарий 4: Продуктивность разработчика</span> Встроенные инструменты (Grep vs Glob vs Read) уже имеют хорошие описания, но пользовательские MCP-инструменты нуждаются в столь же чётких.</p>

          <h4>Глубокое понимание: Три примитива MCP</h4>
          <p>MCP определяет три различных примитива — экзамен может проверить, какой использовать:</p>
          <table class="ref-table">
            <thead><tr><th>Примитив</th><th>Направление</th><th>Назначение</th><th>Пример</th></tr></thead>
            <tbody>
              <tr><td><strong>Tools</strong></td><td>Вызывается моделью</td><td>Действия с побочными эффектами или динамическое извлечение данных</td><td><code>search_orders</code>, <code>process_refund</code></td></tr>
              <tr><td><strong>Resources</strong></td><td>Предоставляется приложением</td><td>Статические или полустатические данные (контекст только для чтения)</td><td>Документы политик компании, спецификации API, файлы конфигурации</td></tr>
              <tr><td><strong>Prompts</strong></td><td>Пользовательские шаблоны</td><td>Предварительно созданные паттерны взаимодействия, направляющие поведение модели</td><td>"Summarize this document", "Review this PR"</td></tr>
            </tbody>
          </table>
          <p><strong>Ключевые различия:</strong></p>
          <ul>
            <li><strong>Tools</strong> <em>вызываются моделью</em> — Claude решает, когда их вызывать. Они выполняют действия или извлекают динамические данные.</li>
            <li><strong>Resources</strong> <em>контролируются приложением</em> — хост-приложение решает, когда инъецировать их в контекст. Они предоставляют статические справочные данные (документы политик, схемы, конфиги). Модель не «вызывает» ресурс; он инъецируется.</li>
            <li><strong>Prompts</strong> — <em>пользовательские шаблоны</em> — они структурируют, как пользователь взаимодействует с моделью. Думайте о них как о переиспользуемых шаблонах промптов, предоставляемых MCP-сервером.</li>
          </ul>
          <div class="exam-tip">
            <strong>Совет к экзамену:</strong> Если сценарий спрашивает «как предоставить агенту документы политик только для чтения», ответ — MCP <strong>Resources</strong>, а не Tools. Resources для инъекции статического контекста. Tools для действий. Если спрашивают «как дать пользователям предварительно созданные паттерны взаимодействия» — это MCP <strong>Prompts</strong>.
          </div>
          <div class="wrong-answer">
            <strong>Частая ошибка:</strong> Использование Tools для всего. Если данные статические (политики компании, схемы API), это должен быть Resource, а не Tool. Tools добавляют к бюджету токенов определений инструментов (~200 токенов каждый); Resources инъецируются по запросу без этих накладных расходов.
          </div>
        `
      },
      {
        id: "t2.2",
        title: "2.2: Структурированные ответы об ошибках для MCP-инструментов",
        knowledge: [
          "Паттерн MCP isError флага для коммуникации сбоев агенту",
          "Типы ошибок: транзиентные (таймауты), валидационные (невалидный ввод), бизнес (нарушения политик), разрешения",
          "Однотипные ошибки 'Operation failed' не позволяют агенту принимать решения о восстановлении",
          "Различайте повторяемые vs неповторяемые ошибки со структурированными метаданными"
        ],
        skills: [
          "Возвращать структурированные метаданные: errorCategory, булевый isRetryable, человекочитаемые описания",
          "Включать retriable: false и клиентопонятные объяснения для нарушений бизнес-правил",
          "Реализовать локальное восстановление в субагентах для транзиентных сбоев; передавать только неразрешимые ошибки с частичными результатами",
          "Различать сбои доступа (требующие повтора) от валидных пустых результатов (успешный запрос, нет совпадений)"
        ],
        keyPoint: "Никогда не возвращайте универсальные ошибки. Никогда не подавляйте ошибки молча. Никогда не завершайте весь рабочий процесс из-за одного сбоя. Всегда возвращайте структурированный контекст ошибки.",
        details: `
          <h4>Флаг MCP isError</h4>
          <p>MCP-инструменты сообщают о сбоях с помощью флага <code>isError</code> в ответе. Когда инструмент возвращает <code>isError: true</code>, агент знает, что что-то пошло не так, и может решить, как реагировать.</p>

          <h4>Четыре категории ошибок</h4>
          <ul>
            <li><strong>Транзиентные:</strong> Таймауты, недоступность сервиса. <em>Обычно можно повторить.</em></li>
            <li><strong>Валидационные:</strong> Невалидный формат ввода, отсутствие обязательных полей. <em>Исправить ввод и повторить.</em></li>
            <li><strong>Бизнес:</strong> Нарушения политик (возврат превышает лимит, аккаунт заблокирован). <em>Не повторяемые — нужен альтернативный рабочий процесс.</em></li>
            <li><strong>Разрешения:</strong> Недостаточные права доступа. <em>Не повторяемые — нужна эскалация.</em></li>
          </ul>

          <h4>Паттерн структурированного ответа об ошибке</h4>
          <pre><code>// GOOD — structured error with actionable metadata
{
  isError: true,
  content: "Refund exceeds policy limit",
  metadata: {
    errorCategory: "business",
    isRetryable: false,
    attempted: "process_refund($723.50)",
    limit: "$500.00",
    alternative: "escalate_to_human",
    customerMessage: "This refund requires manager approval. 
                      I'll connect you with a supervisor."
  }
}

// BAD — generic error
{
  isError: true,
  content: "Operation failed"
}</code></pre>

          <h4>Почему универсальные ошибки вредны</h4>
          <p>Когда инструмент возвращает "Operation failed" без деталей, агент не может:</p>
          <ul>
            <li>Решить, стоит ли повторить (это транзиентная? или постоянная?)</li>
            <li>Попробовать альтернативный подход (какие альтернативы существуют?)</li>
            <li>Осмысленно сообщить пользователю (что пошло не так?)</li>
            <li>Правильно маршрутизировать ошибку (эскалировать? повторить? отказаться?)</li>
          </ul>

          <h4>Сбой доступа vs валидный пустой результат</h4>
          <p>Это различие активно тестируется:</p>
          <pre><code>// Access failure — the search COULDN'T execute
{ isError: true, errorCategory: "transient", 
  message: "Database timeout" }
// → Agent should RETRY

// Valid empty result — the search executed but found nothing
{ isError: false, results: [], 
  message: "No orders found matching criteria" }
// → Agent should tell user "no results found" (NOT retry)</code></pre>
          <div class="wrong-answer">
            <strong>Неправильный подход:</strong> Возвращение пустых результатов (<code>[]</code>) и для сбоев, и для подлинных «нет совпадений». Агент не может отличить одно от другого и может сказать пользователю «заказов не найдено», когда на самом деле база данных была недоступна.
          </div>

          <h4>Восстановление после ошибок в мультиагентных системах</h4>
          <ul>
            <li><strong>Субагенты обрабатывают локальное восстановление</strong> для транзиентных ошибок (повтор 2-3 раза)</li>
            <li><strong>Передавать координатору</strong> только ошибки, которые не удаётся разрешить локально</li>
            <li>Всегда включать <strong>частичные результаты</strong> и <strong>что было предпринято</strong> при передаче</li>
          </ul>

          <div class="exam-tip">
            <strong>Совет к экзамену:</strong> Три вещи, которые НИКОГДА нельзя делать с ошибками: (1) возвращать универсальное «Operation failed», (2) молча подавлять ошибки, возвращая пустые результаты как успех, (3) завершать весь рабочий процесс из-за сбоя одного инструмента. Правильный паттерн — всегда структурированные метаданные ошибки с действенным контекстом.
          </div>

          <h4>Связи со сценариями</h4>
          <p><span class="scenario-tag">Сценарий 1: Поддержка клиентов</span> <code>process_refund</code> может дать сбой по бизнес-причинам (сумма слишком велика) или по транзиентным причинам (таймаут платёжной системы). Агент должен различать их для правильного реагирования.</p>
          <p><span class="scenario-tag">Сценарий 3: Мультиагентное исследование</span> Если субагент веб-поиска получил таймаут, он должен повторить локально. Если всё равно не удаётся, передать ошибку с частичными результатами координатору.</p>

          <h4>Глубокое понимание: Тест полноты ответа об ошибке</h4>
          <p>Экзамен проверяет, содержит ли ответ об ошибке достаточно информации для восстановления. Используйте этот чеклист:</p>
          <pre><code>Complete error response (all 6 fields):
  1. isError: true
  2. errorCategory: "transient" | "validation" | "business" | "permission"
  3. isRetryable: true | false
  4. attempted: "what operation was tried"
  5. context: { limit, threshold, alternative }
  6. customerMessage: "human-readable explanation"

Incomplete responses (exam traps):
  ❌ { isError: true, content: "Failed" }  — no category, no retry info
  ❌ { results: [] }  — suppresses error as empty success
  ❌ throw new Error("timeout")  — kills entire pipeline</code></pre>
          <p><strong>Ловушка «пустых результатов» — тестируемый паттерн №1:</strong> Субагент, возвращающий <code>[]</code> и для «результатов не найдено», и для «база данных не отвечает», делает интеллектуальное восстановление невозможным. Координатор не может отличить «эти данные не существуют» от «мы не смогли связаться с базой данных». Экзамен представит оба варианта, и правильный ответ всегда различает их.</p>
          <p><strong>Тонкость бизнес-ошибок:</strong> Бизнес-ошибки (возврат превышает лимит политики) должны включать поле <code>customerMessage</code> с объяснением, подходящим для конечного пользователя, И поле <code>alternative</code>, предлагающее следующий шаг (например, "escalate_to_human"). Это позволяет агенту и объяснить ситуацию клиенту, и предпринять правильное следующее действие.</p>
        `
      },
      {
        id: "t2.3",
        title: "2.3: Распределение инструментов между агентами и настройка tool_choice",
        knowledge: [
          "Слишком много инструментов (например, 18 вместо 4-5) снижает надёжность выбора",
          "Агенты с инструментами вне их специализации склонны использовать их неправильно",
          "Скопированный доступ к инструментам: только инструменты, нужные для роли, с ограниченными кросс-ролевыми инструментами для высокочастотных потребностей",
          "tool_choice: 'auto' (может вернуть текст), 'any' (должен вызвать инструмент), принудительный выбор (должен вызвать конкретный инструмент)"
        ],
        skills: [
          "Ограничивать набор инструментов каждого субагента теми, которые релевантны его роли",
          "Заменять универсальные инструменты ограниченными альтернативами (например, fetch_url → load_document)",
          "Предоставлять скопированные кросс-ролевые инструменты для высокочастотных нужд (например, verify_fact для синтез-агента)",
          "Использовать принудительный выбор инструмента для обеспечения запуска конкретного инструмента первым, затем обрабатывать последующие шаги в следующих ходах",
          "Устанавливать tool_choice: 'any' для гарантии вызова инструмента вместо возврата текста"
        ],
        keyPoint: "4-5 хорошо скопированных инструментов на агента. 'auto' = может пропустить инструменты. 'any' = должен использовать инструмент. Принудительный = должен использовать ЭТОТ инструмент.",
        details: `
          <h4>Проблема количества инструментов</h4>
          <p>Предоставление агенту слишком многих инструментов снижает надёжность выбора. С 18 инструментами Claude должен прочитать 18 описаний и принять сложное решение. С 4-5 сфокусированными инструментами решение намного проще и надёжнее.</p>
          <pre><code>// BAD — synthesis agent with too many tools
synthesisAgent.tools = [
  "web_search", "fetch_url", "analyze_document", 
  "extract_data", "summarize", "translate", 
  "write_report", "send_email", "query_database",
  "verify_fact", "calculate", "format_table",
  "create_chart", "upload_file", "notify_user",
  "log_event", "cache_result", "compress_data"
]  // 18 tools — synthesis agent might try to web_search!

// GOOD — focused tool set for synthesis
synthesisAgent.tools = [
  "read_findings",    // Read research findings
  "write_report",     // Write final report
  "verify_fact",      // Quick fact verification (scoped cross-role tool)
  "format_table"      // Format data as tables
]  // 4 tools — clear and focused</code></pre>

          <h4>Скопированный доступ к инструментам</h4>
          <p>Каждый субагент получает только нужные для его роли инструменты:</p>
          <ul>
            <li><strong>Агент веб-поиска:</strong> web_search, fetch_url, extract_content</li>
            <li><strong>Агент анализа документов:</strong> read_document, extract_data, summarize</li>
            <li><strong>Агент синтеза:</strong> read_findings, write_report, verify_fact, format_table</li>
          </ul>
          <p><strong>Скопированные кросс-ролевые инструменты:</strong> Иногда агенту нужен периодический доступ к инструментам вне его основной роли. Например, синтез-агент может нуждаться в <code>verify_fact</code> для быстрых проверок. Предоставляйте это как скопированную, ограниченную версию, а не давайте полный доступ ко всем инструментам поиска.</p>

          <h4>Конфигурация tool_choice</h4>
          <p>Это <strong>высокочастотная тема экзамена</strong>. Знайте три варианта:</p>
          <pre><code>// "auto" — Claude may call a tool OR return text
tool_choice: "auto"
// Use when: you want Claude to decide if a tool is needed

// "any" — Claude MUST call a tool (can choose which one)
tool_choice: "any"
// Use when: you always want structured output, not text

// Forced — Claude MUST call THIS specific tool
tool_choice: { type: "tool", name: "extract_metadata" }
// Use when: a specific tool must run first (prerequisites)</code></pre>

          <h4>Когда использовать каждый tool_choice</h4>
          <ul>
            <li><code>"auto"</code>: Общая беседа, где инструменты необязательны. Риск: Claude может вернуть текст, когда вы хотели вызов инструмента.</li>
            <li><code>"any"</code>: У вас несколько схем извлечения, и вы не знаете, какая подходит. Гарантирует вызов инструмента, но Claude выбирает какого.</li>
            <li><strong>Принудительный:</strong> Обязательный инструмент должен запуститься первым (например, <code>extract_metadata</code> перед обогащением). Затем переключитесь на "auto" для следующих шагов.</li>
          </ul>

          <div class="wrong-answer">
            <strong>Паттерн неправильного ответа:</strong> Использование <code>tool_choice: "auto"</code>, когда нужен гарантированный структурированный вывод. "Auto" означает, что Claude может просто вернуть текст вместо вызова инструмента. Используйте <code>"any"</code> или принудительный выбор для гарантии вызова инструмента.
          </div>

          <h4>Замена универсальных инструментов</h4>
          <p>Заменяйте слишком универсальные инструменты ограниченными альтернативами:</p>
          <pre><code>// Generic — agent could fetch anything
{ name: "fetch_url", description: "Fetches any URL" }

// Constrained — validates document URLs only
{ name: "load_document", 
  description: "Loads a document from a verified document URL. 
  Validates URL is from approved document sources. 
  Use for PDF, DOCX, HTML documents only." }</code></pre>

          <div class="exam-tip">
            <strong>Совет к экзамену:</strong> Запомните три значения tool_choice и их гарантии: "auto" = НЕТ гарантии использования инструмента. "any" = ГАРАНТИРОВАННОЕ использование инструмента, модель выбирает какого. Принудительный = ГАРАНТИРОВАННЫЙ конкретный инструмент. Вопросы часто проверяют, знаете ли вы, что "auto" не гарантирует вызов инструмента.
          </div>

          <h4>Связи со сценариями</h4>
          <p><span class="scenario-tag">Сценарий 3: Мультиагентное исследование</span> Каждый субагент (веб, документы, синтез) получает только свои релевантные инструменты. У координатора есть Task tool для делегирования.</p>
          <p><span class="scenario-tag">Сценарий 6: Извлечение данных</span> Используйте принудительный выбор инструмента, чтобы <code>extract_metadata</code> запустился до инструментов обогащения.</p>

          <h4>Глубокое понимание: Паттерн конечного автомата tool_choice</h4>
          <p>Экзамен тестирует критический многоходовый паттерн: изменение <code>tool_choice</code> между ходами для обеспечения последовательности рабочего процесса:</p>
          <pre><code>Turn 1: tool_choice = { type: "tool", name: "classify_document" }
  → Forces classification first. Model MUST classify.

Turn 2: tool_choice = "any"
  → Model picks the right extraction tool based on classification.

Turn 3: tool_choice = "auto"
  → Model may call validation tool OR return final result.</code></pre>
          <p>Это создаёт <strong>конечный автомат</strong>, где tool_choice каждого хода ограничивает, что может произойти. Экзамен специально проверяет, понимаете ли вы, что принудительный выбор применяется к ОДНОМУ ходу — вы должны изменить его для следующих ходов.</p>
          <p><strong>Число «18 инструментов» — сигнал экзамена:</strong> Всякий раз, когда вопрос упоминает агента с множеством инструментов (15-20+) и низкой точностью выбора, ответ ВСЕГДА — сократить количество инструментов до 4-5 сфокусированных. Дистракторы предложат «улучшить описания инструментов» или «добавить логику маршрутизации» — хотя описания важны, сокращение количества инструментов — основное исправление, когда инструментов слишком много.</p>
          <p><strong>Ловушка скопированных кросс-ролевых инструментов:</strong> Синтез-агент нуждается в <code>verify_fact</code> для быстрых проверок. Неправильный ответ даёт ему полный доступ к веб-поиску. Правильный ответ даёт ему ограниченный <code>verify_fact</code>, обрабатывающий только простые проверки, а сложное исследование по-прежнему идёт через координатора. Это <strong>принцип минимальных привилегий</strong>, применённый к доступу к инструментам.</p>
        `
      },
      {
        id: "t2.4",
        title: "2.4: Интеграция MCP-серверов в Claude Code и рабочие процессы агентов",
        knowledge: [
          "Уровень проекта: .mcp.json (общий через систему контроля версий) vs Уровень пользователя: ~/.claude.json (личный)",
          "Подстановка переменных окружения в .mcp.json (например, ${GITHUB_TOKEN}) для управления учётными данными",
          "Инструменты со всех настроенных MCP-серверов обнаруживаются при подключении и доступны одновременно",
          "MCP-ресурсы предоставляют каталоги контента (резюме issues, схемы) для сокращения поисковых вызовов инструментов"
        ],
        skills: [
          "Настраивать общие MCP-серверы в .mcp.json с подстановкой переменных окружения",
          "Настраивать личные/экспериментальные серверы в ~/.claude.json",
          "Улучшать описания MCP-инструментов, чтобы агент не предпочитал встроенные инструменты более функциональным MCP-инструментам",
          "Выбирать общедоступные MCP-серверы вместо пользовательских реализаций для стандартных интеграций",
          "Предоставлять каталоги контента как MCP-ресурсы для видимости агента без поисковых вызовов"
        ],
        keyPoint: ".mcp.json = общие командные инструменты (под контролем версий). ~/.claude.json = личные инструменты. Используйте ${ENV_VARS} для секретов.",
        details: `
          <h4>Два уровня MCP-серверов</h4>
          <p>MCP-серверы можно настроить на двух уровнях:</p>
          <ul>
            <li><strong>Уровень проекта: <code>.mcp.json</code></strong> — Общий через систему контроля версий. Каждый член команды получает одни и те же инструменты. Используйте для командных стандартных интеграций (GitHub, Jira, база данных).</li>
            <li><strong>Уровень пользователя: <code>~/.claude.json</code></strong> — Только личный. Не под контролем версий. Используйте для экспериментальных серверов, личных API-ключей, тестируемых инструментов.</li>
          </ul>
          <pre><code>// .mcp.json (project-level, committed to repo)
{
  "mcpServers": {
    "github": {
      "command": "mcp-server-github",
      "env": {
        "GITHUB_TOKEN": "\${GITHUB_TOKEN}"  // env var expansion
      }
    },
    "jira": {
      "command": "mcp-server-jira",
      "env": {
        "JIRA_API_KEY": "\${JIRA_API_KEY}"
      }
    }
  }
}

// ~/.claude.json (user-level, NOT committed)
{
  "mcpServers": {
    "my-experimental-server": {
      "command": "node",
      "args": ["./my-server.js"]
    }
  }
}</code></pre>

          <h4>Подстановка переменных окружения</h4>
          <p>Используйте синтаксис <code>\${ENV_VAR}</code> в <code>.mcp.json</code> для учётных данных. Таким образом файл конфигурации безопасен для коммита — секреты приходят из окружения каждого разработчика, а не из файла.</p>
          <div class="wrong-answer">
            <strong>Неправильный подход:</strong> Хардкод API-ключей в <code>.mcp.json</code>. Поскольку этот файл под контролем версий, секреты будут закоммичены в репозиторий. Всегда используйте подстановку <code>\${ENV_VAR}</code>.
          </div>

          <h4>Обнаружение инструментов при подключении</h4>
          <p>Инструменты со ВСЕХ настроенных MCP-серверов обнаруживаются при подключении Claude Code и доступны одновременно. Не нужно «активировать» конкретные серверы — они все активны по умолчанию.</p>

          <h4>MCP-ресурсы</h4>
          <p>MCP-ресурсы предоставляют <strong>каталоги контента</strong> — данные только для чтения, дающие агенту видимость без необходимости поисковых вызовов инструментов:</p>
          <ul>
            <li>Резюме issues и структура проектов из Jira/GitHub</li>
            <li>Схемы баз данных</li>
            <li>Иерархии документации</li>
            <li>Каталоги API-эндпоинтов</li>
          </ul>
          <p>Без ресурсов агенту пришлось бы делать множество вызовов инструментов только чтобы узнать, что доступно. Ресурсы предоставляют это сразу.</p>

          <h4>Общедоступные vs пользовательские MCP-серверы</h4>
          <p>Для стандартных интеграций (GitHub, Jira, Slack, базы данных) предпочитайте <strong>общедоступные MCP-серверы</strong> вместо создания пользовательских. Оставляйте пользовательские реализации для специфических командных рабочих процессов, не имеющих готовых решений.</p>

          <h4>Улучшение описаний MCP-инструментов</h4>
          <p>Если Claude продолжает предпочитать встроенные инструменты (как Grep) более функциональным MCP-инструментам, исправление — <strong>улучшить описание MCP-инструмента</strong>, объяснив его возможности и почему он лучше для определённых задач:</p>
          <pre><code>// Enhanced MCP tool description
"search_codebase: Searches the indexed codebase using semantic 
understanding. Returns contextually relevant results with 
dependency analysis. MORE EFFECTIVE than grep for understanding 
code relationships, finding all callers of a function including 
through wrapper modules. Use this instead of Grep when you need 
to understand code flow, not just find text matches."</code></pre>

          <div class="exam-tip">
            <strong>Совет к экзамену:</strong> Вопрос об области видимости часто встречается: «Новый член команды не видит командных MCP-инструментов.» Ответ почти всегда — инструменты настроены на уровне пользователя (<code>~/.claude.json</code>) вместо уровня проекта (<code>.mcp.json</code>). Уровень проекта = общий. Уровень пользователя = личный.
          </div>

          <h4>Связи со сценариями</h4>
          <p><span class="scenario-tag">Сценарий 4: Продуктивность разработчика</span> Команды настраивают общие MCP-серверы (GitHub, CI-инструменты) в <code>.mcp.json</code>, чтобы все разработчики имели одинаковые инструменты.</p>
          <p><span class="scenario-tag">Сценарий 2: Генерация кода</span> Claude Code с MCP-интеграциями для управления проектом, фреймворков тестирования и инструментов деплоя.</p>

          <h4>Глубокое понимание: Токенные накладные расходы MCP и defer_loading</h4>
          <p><strong>Скрытый убийца:</strong> Каждый MCP-сервер добавляет 20-30 определений инструментов x ~200 токенов каждое = <strong>4-6K токенов на сервер</strong>. С 5 подключёнными серверами это ~25K токенов (12.5% общего бюджета), потребляемых до начала работы. Используйте <code>/context</code> для просмотра разбивки и <code>/mcp</code> для отключения простаивающих серверов.</p>
          <p><strong>Паттерн defer_loading:</strong> Установите <code>defer_loading: true</code> на определениях MCP-инструментов для создания заглушек только с описанием. Полные схемы загружаются по запросу только после того, как модель выберет инструмент через ToolSearch. Это радикально снижает фиксированные накладные расходы и поддерживает стабильность префикса кэша промптов.</p>
          <p><strong>Затопление вывода инструментов:</strong> Команды вроде <code>cargo test</code> (1000+ строк), <code>git log</code> или <code>find</code> могут затопить контекст многословным выводом. Используйте PostToolUse хуки для фильтрации вывода до того, как Claude его увидит — например, пропустить через <code>head -30</code> или сократить вывод тестов до pass/fail (паттерн RTK).</p>
        `
      },
      {
        id: "t2.5",
        title: "2.5: Выбор и эффективное применение встроенных инструментов",
        knowledge: [
          "Grep: поиск по СОДЕРЖИМОМУ файлов (паттерны, имена функций, сообщения об ошибках, импорты)",
          "Glob: поиск файлов по паттернам ИМЕНИ/ПУТИ (например, **/*.test.tsx)",
          "Read/Write: полные файловые операции. Edit: целевые модификации с использованием уникального текстового совпадения",
          "Когда Edit не работает (неуникальное совпадение): используйте Read + Write как запасной вариант"
        ],
        skills: [
          "Выбирать Grep для поиска по содержимому кода в кодовой базе",
          "Выбирать Glob для поиска файлов по паттернам именования",
          "Использовать Read затем Write, когда Edit не может найти уникальный якорный текст",
          "Строить понимание инкрементально: Grep для поиска точек входа → Read для отслеживания потоков (не читать все файлы заранее)",
          "Отслеживать использование функций через обёртки: сначала определить экспортируемые имена, затем искать каждое"
        ],
        keyPoint: "Grep = ЧТО внутри файлов. Glob = ГДЕ файлы находятся. Стройте понимание инкрементально, не читайте всё заранее.",
        details: `
          <h4>Матрица выбора встроенных инструментов</h4>
          <p>Знание того, какой инструмент использовать для какой задачи — ключевой навык экзамена:</p>
          <pre><code>┌──────────┬─────────────────────────────────────────────┐
│ Tool     │ When to Use                                 │
├──────────┼─────────────────────────────────────────────┤
│ Grep     │ Search file CONTENTS                        │
│          │ "Find all files that call processRefund()"  │
│          │ "Find error messages containing 'timeout'"  │
│          │ "Find all import statements for auth module" │
├──────────┼─────────────────────────────────────────────┤
│ Glob     │ Find files by NAME/PATH patterns            │
│          │ "Find all test files: **/*.test.tsx"         │
│          │ "Find all config files: **/config.*"         │
│          │ "Find all files in src/auth/"                │
├──────────┼─────────────────────────────────────────────┤
│ Read     │ Load full file contents into context         │
│          │ Read a specific file after finding it        │
├──────────┼─────────────────────────────────────────────┤
│ Write    │ Create or overwrite entire files             │
├──────────┼─────────────────────────────────────────────┤
│ Edit     │ Targeted modification using unique text      │
│          │ Replaces a specific string in a file         │
│          │ Fails if match text isn't unique!            │
├──────────┼─────────────────────────────────────────────┤
│ Bash     │ Run shell commands (git, npm, docker, etc.) │
└──────────┴─────────────────────────────────────────────┘</code></pre>

          <h4>Ключевое различие: Grep vs Glob</h4>
          <p>Это различие тестируется чаще всего в этом утверждении задачи:</p>
          <ul>
            <li><strong>Grep</strong> = ЧТО внутри файлов (поиск по содержимому). «Какие файлы содержат строку <code>processRefund</code>?»</li>
            <li><strong>Glob</strong> = ГДЕ файлы (сопоставление по имени/пути). «Какие файлы называются <code>*.test.tsx</code>?»</li>
          </ul>
          <p>Мнемоника: <strong>Grep = содержимое. Glob = расположение.</strong></p>

          <h4>Когда Edit не работает: запасной вариант Read + Write</h4>
          <p>Edit работает, находя уникальное текстовое совпадение в файле и заменяя его. Если текст встречается несколько раз (не уникален), Edit не срабатывает. Запасной вариант:</p>
          <pre><code>1. Read the entire file
2. Modify the content in memory (with full context to disambiguate)
3. Write the entire file back</code></pre>

          <h4>Инкрементальное понимание (критический паттерн)</h4>
          <p>Не читайте каждый файл заранее. Стройте понимание шаг за шагом:</p>
          <pre><code>1. Grep to find entry points
   → "Where is processRefund called?" → finds 3 files

2. Read the most relevant file
   → Discover it calls validateOrder() from order.js

3. Grep for validateOrder
   → Finds it's defined in services/order.js

4. Read services/order.js
   → Now you understand the full flow</code></pre>
          <div class="wrong-answer">
            <strong>Неправильный подход:</strong> Чтение всех файлов проекта заранее. Это тратит токены контекстного окна и включает массу нерелевантного кода. Стройте понимание инкрементально, следуя по цепочке.
          </div>

          <h4>Отслеживание использования функций через обёртки</h4>
          <p>Функции часто реэкспортируются через модули-обёртки. Чтобы отследить все использования:</p>
          <pre><code>1. Find where the function is defined (Grep for "function processRefund")
2. Find all exported names from that module (Read the export statements)
3. Grep for EACH exported name across the codebase
4. If it's re-exported from a wrapper, grep for the wrapper's export too</code></pre>

          <div class="exam-tip">
            <strong>Совет к экзамену:</strong> Если вопрос спрашивает «какой инструмент использовать для поиска всех файлов, вызывающих функцию X?» — ответ Grep (поиск по содержимому). Если спрашивает «какой инструмент использовать для поиска всех тестовых файлов?» — ответ Glob (сопоставление по паттернам имён файлов).
          </div>

          <h4>Связи со сценариями</h4>
          <p><span class="scenario-tag">Сценарий 4: Продуктивность разработчика</span> Это основной сценарий. Исследование незнакомых кодовых баз требует мастерства выбора встроенных инструментов: Grep для отслеживания потока кода, Glob для поиска тестовых файлов и конфигов, Read для понимания конкретных файлов.</p>
          <p><span class="scenario-tag">Сценарий 2: Генерация кода</span> Перед генерацией кода Claude должен использовать Grep/Glob для понимания существующих паттернов в кодовой базе.</p>

          <h4>Глубокое понимание: Антипаттерн инкрементального обнаружения</h4>
          <p>Экзамен активно проверяет, понимаете ли вы <strong>инкрементальное обнаружение vs предварительную загрузку</strong>. Вот матрица решений:</p>
          <pre><code>Incremental (CORRECT for exploration):
  Grep "processRefund" → found in 3 files
  Read most relevant file → discover it calls validateOrder()
  Grep "validateOrder" → trace to services/order.js
  Read services/order.js → now understand full flow
  Total: 4 tool calls, ~200 lines of relevant context

Upfront loading (WRONG for exploration):
  Read src/*.ts → 50 files, 15,000 lines
  Context window: overwhelmed with irrelevant code
  "Lost in the middle" effect: key functions missed</code></pre>
          <p><strong>Исключение:</strong> Предварительная загрузка ПРАВИЛЬНА, когда вы точно знаете, какие 2-3 файла нужны. Антипаттерн — чтение «всех файлов» или «всего в директории» при исследовании.</p>
          <p><strong>Паттерн запасного варианта Edit:</strong> Когда <code>Edit</code> не работает из-за неуникального целевого текста, экзамен ожидает использования <code>Read</code> + <code>Write</code> как запасного варианта (прочитать весь файл, модифицировать в памяти с полным контекстом для устранения неоднозначности, записать обратно). Ответ-ловушка — «использовать более конкретную строку поиска» — хотя иногда возможно, Read+Write — надёжный универсальный запасной вариант.</p>
          <p><strong>Быстрое решение Grep vs Glob:</strong> Если вопрос спрашивает «найти файлы, которые <em>содержат</em> X» → Grep. Если вопрос спрашивает «найти файлы, которые <em>соответствуют паттерну</em> X» → Glob. Слово «содержат» всегда означает Grep (поиск по содержимому). Слово «соответствуют» или «названы» всегда означает Glob (паттерн пути).</p>
        `
      }
    ]
  },
  {
    id: "d3",
    domain: 3,
    title: "Конфигурация Claude Code и рабочие процессы",
    weight: 20,
    tagClass: "tag-d3",
    tasks: [
      {
        id: "t3.1",
        title: "3.1: Настройка иерархии CLAUDE.md и модульная организация",
        knowledge: [
          "Иерархия: уровень пользователя (~/.claude/CLAUDE.md), уровень проекта (.claude/CLAUDE.md или корень), уровень директории (поддиректория CLAUDE.md)",
          "Настройки уровня пользователя НЕ передаются коллегам через систему контроля версий",
          "Синтаксис @import ссылается на внешние файлы для модульности CLAUDE.md",
          "Директория .claude/rules/ для тематических файлов правил как альтернатива монолитному CLAUDE.md"
        ],
        skills: [
          "Диагностировать проблемы иерархии конфигурации (например, новый член команды не получает инструкции, потому что они на уровне пользователя, а не проекта)",
          "Использовать @import для выборочного включения релевантных файлов стандартов в CLAUDE.md каждого пакета",
          "Разделять большой CLAUDE.md на сфокусированные файлы в .claude/rules/ (testing.md, api-conventions.md, deployment.md)",
          "Использовать команду /memory для проверки загруженных файлов памяти"
        ],
        keyPoint: "Новый член команды не получает инструкции? Проверьте, находятся ли правила на уровне пользователя (~/) вместо уровня проекта (.claude/).",
        details: `
          <h4>Иерархия CLAUDE.md</h4>
          <p>Claude Code загружает конфигурацию с трёх уровней, каждый с различной областью действия:</p>
          <pre><code>Level 1: User-level     ~/.claude/CLAUDE.md
         → Personal preferences (YOUR machine only)
         → NOT shared via version control
         → e.g., "I prefer verbose explanations"

Level 2: Project-level  .claude/CLAUDE.md  or  ./CLAUDE.md
         → Shared team standards (committed to repo)
         → Every team member gets these
         → e.g., "Use TypeScript strict mode"

Level 3: Directory-level  src/api/CLAUDE.md
         → Conventions for specific subdirectories
         → Loaded when editing files in that directory
         → e.g., "API routes use express middleware pattern"</code></pre>

          <h4>Самый распространённый баг иерархии</h4>
          <p>Это <strong>топовый сценарий экзамена</strong>: «Новый член команды присоединяется, и Claude Code не следует командным соглашениям. Что пошло не так?»</p>
          <p>Ответ: Соглашения находятся в <code>~/.claude/CLAUDE.md</code> (уровень пользователя) вместо <code>.claude/CLAUDE.md</code> (уровень проекта). Файлы уровня пользователя живут на машине оригинального разработчика и НЕ передаются через систему контроля версий.</p>

          <h4>@import для модульной организации</h4>
          <p>Большие монолитные CLAUDE.md файлы становятся неуправляемыми. Используйте <code>@import</code> для разделения:</p>
          <pre><code># .claude/CLAUDE.md (root)
@import ./standards/testing.md
@import ./standards/api-conventions.md
@import ./standards/deployment.md

# Each package can import only what's relevant:
# packages/frontend/.claude/CLAUDE.md
@import ../../standards/testing.md
@import ../../standards/frontend-patterns.md</code></pre>
          <p>Таким образом каждый пакет загружает только релевантные стандарты, а не всё.</p>

          <h4>Директория .claude/rules/</h4>
          <p>Альтернатива @import: поместите тематические файлы правил в <code>.claude/rules/</code>:</p>
          <pre><code>.claude/
  rules/
    testing.md          ← Testing conventions
    api-conventions.md  ← API patterns  
    deployment.md       ← Deployment rules
    security.md         ← Security requirements</code></pre>
          <p>Эти файлы также могут иметь YAML frontmatter для привязки к путям (рассмотрено в t3.3).</p>

          <h4>Команда /memory</h4>
          <p>Используйте <code>/memory</code> для просмотра загруженных файлов памяти. Это ваш диагностический инструмент, когда Claude игнорирует инструкции — проверьте, действительно ли ожидаемые файлы загружены.</p>

          <div class="exam-tip">
            <strong>Совет к экзамену:</strong> Вопросы об иерархии почти всегда проверяют различие уровня пользователя vs уровня проекта. Если кто-то говорит «у моего коллеги другое поведение Claude» — ответ всегда об области видимости: файлы уровня пользователя не разделяются. Переместите их на уровень проекта.
          </div>

          <h4>Связи со сценариями</h4>
          <p><span class="scenario-tag">Сценарий 2: Генерация кода</span> Командные стандарты кодирования идут в CLAUDE.md уровня проекта. Личные предпочтения (стиль редактора, многословность) — на уровень пользователя.</p>
          <p><span class="scenario-tag">Сценарий 5: CI/CD</span> Claude Code, вызванный из CI, читает CLAUDE.md уровня проекта для стандартов тестирования, критериев ревью и соглашений о фикстурах.</p>

          <h4>Глубокое понимание: Размер CLAUDE.md и Compact Instructions</h4>
          <p><strong>Ориентир:</strong> CLAUDE.md самой Anthropic составляет ~2.5K токенов. Используйте это как целевой размер. CLAUDE.md — это <strong>полуфиксированный контекст</strong> — он загружается при каждом запросе, поэтому каждый токен здесь стоит вам при каждом API-вызове.</p>
          <p><strong>Паттерн Compact Instructions:</strong> Добавьте секцию в CLAUDE.md под названием «Compact Instructions», явно указывающую компактору, что сохранять при <code>/compact</code>. Без этого компакция сбрасывает архитектурные решения и сохраняет многословный вывод инструментов. Пример:</p>
          <pre><code># Compact Instructions
When compacting, always preserve:
- Architectural decisions and rationale
- API design choices
- Failure modes discovered
- File paths and function names identified
- Any constraints or invariants established</code></pre>
          <p><strong>Выносите детали:</strong> Если CLAUDE.md становится длинным, перенесите правила для конкретных языков в <code>.claude/rules/</code> (загружаются по пути, не всегда резидентны), а доменные знания — в Skills (по запросу). Оставляйте в CLAUDE.md только общепроектные контракты.</p>
        `
      },
      {
        id: "t3.2",
        title: "3.2: Пользовательские слэш-команды и навыки",
        knowledge: [
          "Команды уровня проекта: .claude/commands/ (передаются через систему контроля версий)",
          "Команды уровня пользователя: ~/.claude/commands/ (личные)",
          "Skills: .claude/skills/ с SKILL.md, поддерживающим frontmatter: context: fork, allowed-tools, argument-hint",
          "context: fork запускает навыки в изолированном контексте субагента, предотвращая загрязнение основной беседы"
        ],
        skills: [
          "Создавать команды уровня проекта в .claude/commands/ для командной доступности",
          "Использовать context: fork для изоляции многословных или исследовательских навыков от основной сессии",
          "Настраивать allowed-tools в SKILL.md для ограничения доступа к инструментам (например, только запись файлов)",
          "Использовать argument-hint для запроса обязательных параметров при вызове без аргументов",
          "Выбирать между навыками (по запросу для конкретных задач) и CLAUDE.md (всегда загруженные универсальные стандарты)"
        ],
        keyPoint: "context: fork изолирует вывод навыка. allowed-tools ограничивает возможности навыка. Skills = по запросу; CLAUDE.md = всегда активен.",
        details: `
          <h4>Пользовательские слэш-команды</h4>
          <p>Слэш-команды — это markdown-файлы, определяющие переиспользуемые рабочие процессы, запускаемые через <code>/command-name</code>:</p>
          <pre><code>Two scopes:
  .claude/commands/    ← Project-scoped (shared via version control)
  ~/.claude/commands/  ← User-scoped (personal only)

Example: .claude/commands/review.md
─────────────────────────────────────
Review this file for security vulnerabilities:
- Check for SQL injection
- Check for XSS vulnerabilities  
- Check for authentication bypasses
Provide findings as a numbered list with severity ratings.

Usage: /review</code></pre>

          <h4>Навыки (продвинутые команды)</h4>
          <p>Навыки живут в <code>.claude/skills/</code> и имеют файл <code>SKILL.md</code> с мощными опциями frontmatter:</p>
          <pre><code>.claude/skills/
  analyze-codebase/
    SKILL.md     ← Skill definition with frontmatter</code></pre>
          <pre><code># .claude/skills/analyze-codebase/SKILL.md
---
context: fork
allowed-tools:
  - Read
  - Glob
  - Grep
argument-hint: "path to analyze"
---

Analyze the codebase structure at the given path.
Map all modules, dependencies, and entry points.
Return a structured summary of the architecture.</code></pre>

          <h4>Ключевые опции Frontmatter</h4>
          <ul>
            <li><strong><code>context: fork</code></strong> — Запускает навык в <strong>изолированном контексте субагента</strong>. Многословный вывод навыка не загрязняет основную беседу. Субагент возвращает резюме.</li>
            <li><strong><code>allowed-tools</code></strong> — Ограничивает, какие инструменты может использовать навык. Для безопасности: ограничьте навык записи файлов только <code>Write</code>, предотвращая выполнение команд <code>Bash</code>.</li>
            <li><strong><code>argument-hint</code></strong> — Запрашивает у пользователя обязательный параметр, если навык вызван без аргументов.</li>
          </ul>

          <h4>context: fork — Почему это важно</h4>
          <p>Без <code>context: fork</code> навык, читающий 50 файлов и производящий детальный анализ, сбрасывает весь вывод в контекст основной беседы. Это может:</p>
          <ul>
            <li>Быстро исчерпать контекстное окно</li>
            <li>Размыть беседу нерелевантными деталями</li>
            <li>Сделать последующие взаимодействия медленнее и менее сфокусированными</li>
          </ul>
          <p>С <code>context: fork</code> навык работает изолированно и возвращает только резюме в основную сессию.</p>

          <h4>Skills vs CLAUDE.md — когда использовать каждый</h4>
          <pre><code>CLAUDE.md:
  ✅ Always-loaded universal standards
  ✅ Coding conventions, testing rules, style guides
  ✅ Applies to EVERY interaction automatically

Skills:
  ✅ On-demand task-specific workflows
  ✅ Invoked explicitly when needed
  ✅ Codebase analysis, review workflows, scaffolding
  ✅ Can be isolated with context: fork</code></pre>
          <div class="wrong-answer">
            <strong>Неправильный подход:</strong> Помещение задачеспецифических рабочих процессов (как «анализ кодовой базы») в CLAUDE.md. Это загружает ненужный контекст при каждом взаимодействии. Используйте навыки для задач по запросу; CLAUDE.md для универсальных стандартов.
          </div>

          <h4>Личные варианты навыков</h4>
          <p>Если хотите личный вариант командного навыка, создайте его в <code>~/.claude/skills/</code> с другим именем. Это избегает переопределения командной версии, давая вам кастомизацию.</p>

          <div class="exam-tip">
            <strong>Совет к экзамену:</strong> Когда вопрос спрашивает об изоляции многословного исследования от основной сессии, ответ — <code>context: fork</code>. Когда спрашивают об ограничении возможностей навыка — <code>allowed-tools</code>. Эти две опции frontmatter — наиболее тестируемые концепции навыков.
          </div>

          <h4>Связи со сценариями</h4>
          <p><span class="scenario-tag">Сценарий 2: Генерация кода</span> Командные слэш-команды (<code>.claude/commands/</code>) для код-ревью, генерации тестов и шаблонов скаффолдинга.</p>
          <p><span class="scenario-tag">Сценарий 4: Продуктивность разработчика</span> Навыки с <code>context: fork</code> для исследования кодовой базы — изоляция многословного вывода и возврат резюме.</p>

          <h4>Глубокое понимание: Три типа навыков (паттерны проектирования)</h4>
          <p>Навыки делятся на три категории, каждая с отличными паттернами проектирования:</p>
          <ul>
            <li><strong>Чеклист (качественный шлюз):</strong> Предполётная верификация — все пункты должны пройти. Пример: <code>release-check</code>. Короткий, детерминированный, pass/fail по каждому пункту.</li>
            <li><strong>Рабочий процесс (стандартизированная операция):</strong> Высокорисковые операции с явными шагами отката. Пример: <code>config-migration</code>. Установите <code>disable-model-invocation: true</code>, чтобы запускалось только явно.</li>
            <li><strong>Доменный эксперт (фреймворк решений):</strong> Структурированный сбор доказательств + матрица решений. Пример: <code>runtime-diagnosis</code>. Собирает доказательства по фиксированному пути, а не угадыванием.</li>
          </ul>
          <p><strong>Оптимизация дескрипторов:</strong> Держите дескрипторы навыков короткими (~9 токенов, не ~45). Каждый дескриптор <strong>всегда в контексте</strong> (фиксированная стоимость), поэтому многословность тратит токены при каждом запросе. Плохо: "This skill helps review pull requests with a focus on code correctness and best practices for production systems." Хорошо: "Use for PR reviews with focus on correctness."</p>
          <div class="wrong-answer">
            <strong>Антипаттерн:</strong> Один навык, покрывающий всё (ревью + деплой + дебаг + документация). Это слишком широко, срабатывает некорректно и тратит контекст. Разделяйте на сфокусированные однозадачные навыки.
          </div>

          <h4>Глубокое понимание: Корпоративная конфигурация — managed-settings.json</h4>
          <p>В корпоративных средах IT-администраторы могут принудительно устанавливать настройки Claude Code через <code>managed-settings.json</code> — конфигурационный слой с <strong>наивысшим приоритетом</strong>:</p>
          <pre><code>Settings Precedence (highest → lowest):
1. managed-settings.json (IT admin, system-level) ← CANNOT be overridden
2. Enterprise policy / Organization settings
3. Project .claude/settings.json
4. User ~/.claude/settings.json
5. Command-line flags</code></pre>
          <p><strong>Ключевые настройки корпоративной блокировки:</strong></p>
          <ul>
            <li><code>allowManagedMcpServersOnly</code> — Можно использовать только одобренные IT MCP-серверы</li>
            <li><code>strictKnownMarketplaces</code> — Ограничение установки инструментов одобренными маркетплейсами</li>
            <li><code>hostPattern</code> — Ограничение хостов/доменов, к которым могут подключаться MCP-серверы</li>
            <li><code>network.allowedDomains</code> — Белый список разрешённых сетевых доменов</li>
            <li><code>availableModels</code> — Ограничение доступных моделей</li>
            <li><code>respectGitignore</code> — Принудительное соблюдение .gitignore</li>
          </ul>
          <div class="exam-tip">
            <strong>Совет к экзамену:</strong> Если сценарий описывает организацию, желающую ограничить MCP-серверы разработчиков, ответ — <code>managed-settings.json</code> с <code>allowManagedMcpServersOnly: true</code>. Это ЕДИНСТВЕННЫЙ способ обеспечить это — настройки проекта могут быть переопределены пользователями.
          </div>

          <h4>Глубокое понимание: Директория .claude/agents/</h4>
          <p>Директория <code>.claude/agents/</code> — это место на уровне проекта для <strong>определений пользовательских AI-субагентов</strong>. Это переиспользуемые конфигурации агентов, которые можно разделять через систему контроля версий:</p>
          <pre><code>.claude/
  agents/
    researcher.md     ← Research-focused agent definition
    reviewer.md       ← Code review agent
    architect.md      ← Architecture analysis agent</code></pre>
          <p>В отличие от навыков (задачеспецифические рабочие процессы), агенты в этой директории определяют <strong>постоянные роли</strong>, которые может вызывать координатор. Они дополняют директории <code>.claude/commands/</code> и <code>.claude/skills/</code> в структуре конфигурации проекта.</p>

          <h4>Глубокое понимание: Команда /status</h4>
          <p>Используйте <code>/status</code> для просмотра активных конфигурационных слоёв и их источников. Это ваш диагностический инструмент, когда настройки ведут себя не так, как ожидается — он показывает, откуда пришла настройка: из managed-settings.json, настроек проекта, настроек пользователя или флагов командной строки.</p>
        `
      },
      {
        id: "t3.3",
        title: "3.3: Правила привязки к путям для условной загрузки соглашений",
        knowledge: [
          "Файлы .claude/rules/ с YAML frontmatter полем paths, содержащим glob-паттерны",
          "Привязанные к путям правила загружаются только при редактировании соответствующих файлов, уменьшая нерелевантный контекст и токены",
          "Правила с glob-паттернами лучше директорийного CLAUDE.md для соглашений, охватывающих несколько директорий"
        ],
        skills: [
          "Создавать файлы .claude/rules/ с YAML frontmatter (например, paths: ['terraform/**/*'])",
          "Использовать glob-паттерны для соглашений по типу файлов независимо от расположения (например, **/*.test.tsx)",
          "Выбирать правила привязки к путям вместо поддиректорийных CLAUDE.md, когда соглашения охватывают всю кодовую базу"
        ],
        keyPoint: "Тестовые соглашения в файлах **/*.test.tsx по многим директориям? Используйте .claude/rules/ с glob-паттернами, НЕ файлы CLAUDE.md в поддиректориях.",
        details: `
          <h4>Правила привязки к путям</h4>
          <p>Файлы в <code>.claude/rules/</code> могут иметь YAML frontmatter с полем <code>paths</code>. Правила загружаются только при редактировании файлов, соответствующих glob-паттернам:</p>
          <pre><code># .claude/rules/terraform.md
---
paths:
  - "terraform/**/*"
---

When editing Terraform files:
- Use modules for reusable infrastructure
- Always include tags for cost tracking
- Use variables.tf for all input variables
- Run terraform fmt before committing</code></pre>
          <p>Эти правила загружаются только при редактировании файлов в <code>terraform/</code>. Они не потребляют токены при редактировании React-компонентов или Python-скриптов.</p>

          <h4>Glob-паттерны для кросс-директорийных соглашений</h4>
          <p>Убийственный случай использования: соглашения, применяемые к <em>типу</em> файла независимо от расположения:</p>
          <pre><code># .claude/rules/testing.md
---
paths:
  - "**/*.test.tsx"
  - "**/*.test.ts"
  - "**/*.spec.ts"
---

When writing tests:
- Use describe/it blocks with clear descriptions
- Mock external dependencies, not internal modules
- Test behavior, not implementation details
- Include edge cases for null, undefined, empty arrays</code></pre>
          <p>Это применяется к тестовым файлам в <code>src/components/</code>, <code>src/utils/</code>, <code>src/api/</code> — везде. Не нужен CLAUDE.md в каждой директории.</p>

          <h4>Правила путей vs директорийный CLAUDE.md</h4>
          <pre><code>Directory CLAUDE.md (src/api/CLAUDE.md):
  ✅ Good for: rules specific to ONE directory
  ❌ Bad for: rules that span many directories

Path-specific rules (.claude/rules/ with globs):
  ✅ Good for: rules by file TYPE across the codebase
  ✅ Good for: infrastructure files (terraform/**)
  ✅ Good for: test files (**/*.test.tsx)
  ✅ Reduces token usage — only loaded when relevant</code></pre>

          <div class="wrong-answer">
            <strong>Неправильный подход:</strong> Создание CLAUDE.md файла в каждой директории, содержащей тестовые файлы, просто для применения тестовых соглашений. Если у вас тестовые файлы в 20 директориях — это 20 CLAUDE.md файлов для поддержки. Используйте ОДНО правило привязки к путям с <code>**/*.test.tsx</code>.
          </div>

          <h4>Эффективность токенов</h4>
          <p>Привязанные к путям правила уменьшают нерелевантный контекст. Без привязки к путям все правила загружаются для каждого редактируемого файла. С привязкой:</p>
          <ul>
            <li>Редактирование <code>main.py</code> → загружаются только правила Python</li>
            <li>Редактирование <code>infra/main.tf</code> → загружаются только правила Terraform</li>
            <li>Редактирование <code>src/App.test.tsx</code> → загружаются только правила тестирования</li>
          </ul>
          <p>Меньше нерелевантного контекста = лучшая работа Claude и меньший расход токенов.</p>

          <div class="exam-tip">
            <strong>Совет к экзамену:</strong> Когда вопрос описывает соглашения, которые должны применяться к файлам, разбросанным по многим директориям (как тестовые файлы), правильный ответ — .claude/rules/ с glob-паттернами. Создание CLAUDE.md файлов в каждой поддиректории — неправильный ответ, это неподдерживаемо.
          </div>

          <h4>Связи со сценариями</h4>
          <p><span class="scenario-tag">Сценарий 2: Генерация кода</span> Правила привязки к путям обеспечивают следование Claude различным соглашениям для разных типов файлов: React-компоненты, API-маршруты, тестовые файлы, конфиг-файлы.</p>
          <p><span class="scenario-tag">Сценарий 5: CI/CD</span> Промпты CI-ревью могут ссылаться на правила привязки к путям, чтобы критерии ревью соответствовали типу ревьюируемого файла.</p>

          <h4>Глубокое понимание: Механика загрузки правил и стоимость токенов</h4>
          <p>Понимание <em>когда</em> правила загружаются критично для экзамена:</p>
          <pre><code>Always loaded (fixed cost):
  ✅ Root CLAUDE.md — loaded for EVERY request
  ✅ User-level ~/.claude/CLAUDE.md — loaded for EVERY request
  ✅ Skill descriptors — always in context (keep ~9 tokens each!)

Conditionally loaded (path-scoped):
  ✅ .claude/rules/ with paths: [...] — only when editing matching files
  ✅ Directory-level CLAUDE.md — only when editing files in that directory

Never loaded automatically:
  ✅ Skills (SKILL.md) — only when explicitly invoked
  ✅ Subagent definitions (.claude/agents/) — only when spawned</code></pre>
          <p><strong>Ловушка экзамена — «когда использовать директорийный CLAUDE.md»:</strong> Единственный правильный случай использования поддиректорийного CLAUDE.md — правила, применяемые ТОЛЬКО к этой одной директории И нигде больше. Как только соглашение охватывает несколько директорий (как тестовые соглашения), переходите на <code>.claude/rules/</code> с glob-паттернами. Экзамен представит сценарии с 15+ директориями, нуждающимися в одном соглашении — ответ всегда один файл правил с glob, никогда 15 CLAUDE.md файлов.</p>
          <p><strong>Связь с @import:</strong> Если корневой CLAUDE.md становится слишком большим (вредя эффективности кэша), используйте <code>@import</code> для разделения на модули. Но помните: @import-ированные файлы по-прежнему загружаются при каждом запросе. @import помогает организации, а не условной загрузке. Только <code>.claude/rules/</code> обеспечивает истинную условную загрузку.</p>
        `
      },
      {
        id: "t3.4",
        title: "3.4: Режим планирования vs прямое выполнение",
        knowledge: [
          "Режим планирования: сложные задачи, масштабные изменения, множество допустимых подходов, архитектурные решения, модификация нескольких файлов",
          "Прямое выполнение: простые, чётко ограниченные изменения (одна проверка валидации, понятный баг-фикс)",
          "Режим планирования позволяет безопасное исследование до фиксации изменений, предотвращая дорогую переделку",
          "Субагент Explore изолирует многословный вывод обнаружения и возвращает резюме для сохранения основного контекста"
        ],
        skills: [
          "Выбирать режим планирования для архитектурных последствий (реструктуризация микросервисов, миграция библиотек, решения с несколькими подходами)",
          "Выбирать прямое выполнение для понятных изменений с чёткой областью (баг-фикс в одном файле, одна проверка валидации)",
          "Использовать субагент Explore для многословного обнаружения для предотвращения исчерпания контекста",
          "Комбинировать режим планирования для исследования с прямым выполнением для реализации"
        ],
        keyPoint: "Если задача имеет несколько допустимых подходов или затрагивает много файлов → режим планирования. Если это чёткий фикс в одном файле → прямое выполнение.",
        details: `
          <h4>Режим планирования vs прямое выполнение</h4>
          <p>Это о выборе правильной стратегии выполнения до внесения изменений:</p>
          <pre><code>Plan Mode:                          Direct Execution:
  ✅ Complex tasks                    ✅ Simple, clear tasks
  ✅ Multiple valid approaches        ✅ One obvious approach
  ✅ Large-scale changes              ✅ Single-file changes
  ✅ Architectural decisions          ✅ Bug fix with clear stack trace
  ✅ Multi-file modifications         ✅ Adding a validation check
  ✅ Library migrations               ✅ Fixing a typo
  ✅ Unknown codebase areas           ✅ Well-understood code</code></pre>

          <h4>Режим планирования в деталях</h4>
          <p>Режим планирования позволяет безопасное исследование ДО фиксации изменений. Это как архитектурное проектирование перед строительством:</p>
          <pre><code>Plan Mode Workflow:
1. Explore the codebase (read files, understand structure)
2. Identify affected areas and dependencies
3. Evaluate multiple approaches (tradeoffs, risks)
4. Present a plan for review
5. Execute the approved plan

Direct Execution Workflow:
1. Understand the change needed
2. Make the change
3. Done</code></pre>

          <h4>Субагент Explore</h4>
          <p>Субагент Explore создан специально для фазы обнаружения в режиме планирования. Он:</p>
          <ul>
            <li>Работает в <strong>изолированном контексте</strong> — многословный вывод обнаружения остаётся отдельно</li>
            <li>Возвращает <strong>резюме</strong> в основную беседу</li>
            <li>Предотвращает исчерпание контекстного окна при многоэтапных задачах</li>
          </ul>
          <p>Без субагента Explore чтение 30 файлов во время обнаружения заполнило бы основное контекстное окно, не оставляя места для реализации.</p>

          <h4>Комбинирование планирования + прямое выполнение</h4>
          <p>Самый мощный паттерн: режим планирования для исследования, затем прямое выполнение для реализации:</p>
          <pre><code>Phase 1 (Plan Mode):
  "Investigate how to migrate from library A to library B.
   Identify all affected files, breaking changes, and 
   suggest an approach."

Phase 2 (Direct Execution):
  "Implement the migration plan: update imports in these 
   5 files, modify the config, update tests."</code></pre>

          <h4>Фреймворк принятия решений</h4>
          <p>Задайте эти вопросы для выбора:</p>
          <ol>
            <li>Есть ли более одного допустимого подхода? → Режим планирования</li>
            <li>Затрагивает ли это более 2-3 файлов? → Режим планирования</li>
            <li>Есть ли архитектурные последствия? → Режим планирования</li>
            <li>Фикс очевиден из ошибки? → Прямое выполнение</li>
            <li>Это изменение одной функции/файла? → Прямое выполнение</li>
          </ol>

          <div class="wrong-answer">
            <strong>Неправильный подход:</strong> Использование режима планирования для «добавить проверку null на строке 42 в auth.js». Это чёткий фикс в одном файле — режим планирования добавляет ненужные накладные расходы. Просто сделайте это.
          </div>

          <div class="exam-tip">
            <strong>Совет к экзамену:</strong> Вопросы описывают сценарий и спрашивают, использовать режим планирования или прямое выполнение. Ключевые сигналы: «реструктуризация микросервисов», «миграция библиотек», «45+ файлов», «несколько подходов» → режим планирования. «Одна проверка валидации», «чёткий баг-фикс», «одна функция» → прямое выполнение.
          </div>

          <h4>Связи со сценариями</h4>
          <p><span class="scenario-tag">Сценарий 2: Генерация кода</span> Режим планирования для крупных задач рефакторинга; прямое выполнение для быстрых баг-фиксов и изменений одной функции.</p>
          <p><span class="scenario-tag">Сценарий 4: Продуктивность разработчика</span> Субагент Explore помогает разработчикам понять незнакомые кодовые базы без исчерпания контекста.</p>

          <h4>Глубокое понимание: Внутренности режима планирования</h4>
          <p><strong>EnterPlanMode — это вызов инструмента</strong>, а не переключение режима. Когда Claude входит в режим планирования (через Shift+Tab x2 или автономно), он вызывает инструмент <code>EnterPlanMode</code>. Критично: это НЕ меняет доступный набор инструментов — что значит <strong>префикс кэша промптов сохраняется</strong>. Это по дизайну: режим планирования добавляет исследование только для чтения без инвалидации дорогостоящего кэшированного префикса (система + инструменты + история).</p>
          <p><strong>Следствие для кэша:</strong> Если бы режим планирования менял набор инструментов, каждый вход/выход инвалидировал бы кэш промптов для всех предшествующих токенов. Сохраняя его как вызов инструмента, Anthropic избегает этой стоимости.</p>
        `
      },
      {
        id: "t3.5",
        title: "3.5: Техники итеративного улучшения",
        knowledge: [
          "Конкретные примеры ввода/вывода — самый эффективный способ коммуникации ожидаемых преобразований",
          "Итерация через тесты: сначала пишите тесты, итерируйте, сообщая о проваленных тестах для направления улучшений",
          "Паттерн интервью: Claude задаёт вопросы для выявления соображений перед реализацией",
          "Взаимосвязанные проблемы → одно сообщение. Независимые проблемы → исправлять последовательно."
        ],
        skills: [
          "Предоставить 2-3 конкретных примера ввода/вывода, когда текстовые описания дают непоследовательные результаты",
          "Сначала написать набор тестов (поведение, граничные случаи, производительность), затем итерировать, сообщая о провалах",
          "Использовать паттерн интервью в незнакомых доменах (например, стратегии кэширования, режимы сбоев)",
          "Предоставлять конкретные тест-кейсы с примером ввода и ожидаемым выводом для исправления граничных случаев",
          "Решать взаимосвязанные проблемы вместе; независимые — последовательно"
        ],
        keyPoint: "Непоследовательный вывод? Дайте конкретные примеры ввода/вывода. Незнакомый домен? Используйте паттерн интервью.",
        details: `
          <h4>Четыре техники итеративного улучшения</h4>
          <p>Техники для прогрессивного улучшения качества вывода Claude:</p>

          <h4>1. Конкретные примеры ввода/вывода</h4>
          <p><strong>Самая эффективная</strong> техника, когда текстовые описания дают непоследовательные результаты:</p>
          <pre><code>// Instead of: "Convert dates to ISO format"
// Provide examples:

Input:  "Jan 5, 2024"      → Output: "2024-01-05"
Input:  "5/1/24"            → Output: "2024-01-05"  
Input:  "January 5th 2024"  → Output: "2024-01-05"
Input:  "2024.01.05"        → Output: "2024-01-05"</code></pre>
          <p>2-3 примера, показывающих преобразование, устраняют неоднозначность, которую параграфы инструкций не могут разрешить.</p>

          <h4>2. Итерация через тесты</h4>
          <p>Сначала пишите тесты, ЗАТЕМ итерируйте, сообщая о провалах:</p>
          <pre><code>Step 1: Write test suite
  - Expected behavior tests
  - Edge case tests (null, empty, boundary values)
  - Performance requirements

Step 2: Claude implements
  → 3 tests fail

Step 3: Share SPECIFIC failures
  "Test 'handles null input' failed:
   Expected: return empty array
   Got: TypeError: Cannot read property 'map' of null"

Step 4: Claude fixes → repeat until all tests pass</code></pre>
          <p>Сообщение конкретных провалов тестов даёт Claude точную, действенную обратную связь для улучшения.</p>

          <h4>3. Паттерн интервью</h4>
          <p>Для незнакомых доменов попросите Claude задать вопросы ДО реализации:</p>
          <pre><code>You: "Build a caching system for our API"

Claude (interview mode):
  "Before I implement, let me ask a few questions:
   1. What's your cache invalidation strategy? (TTL, event-based, manual)
   2. What failure mode is acceptable? (stale data vs cache miss)
   3. What's the expected cache hit ratio target?
   4. Do you need distributed caching or single-node?
   5. What data formats need caching? (JSON, binary, streams)"</code></pre>
          <p>Это выявляет соображения, о которых вы могли не подумать. Используйте, когда ВЫ (разработчик) тоже не знакомы с доменом.</p>

          <h4>4. Стратегия группировки проблем</h4>
          <pre><code>Interacting issues (fix together in ONE message):
  "The auth middleware is both skipping validation 
   AND not setting the user context, causing downstream 
   permission errors."
  → These issues interact, so fix them together.

Independent issues (fix sequentially):
  "The login page has a CSS bug AND the API has a typo 
   in the error message."
  → Unrelated issues, fix one at a time for cleaner iterations.</code></pre>

          <div class="wrong-answer">
            <strong>Неправильный подход:</strong> Отправка размытой обратной связи вроде «вывод неправильный» или «попробуй ещё». Предоставьте КОНКРЕТНЫЕ примеры ввода/вывода, провалы тестов или конкретные критерии того, что нужно изменить.
          </div>

          <div class="exam-tip">
            <strong>Совет к экзамену:</strong> Если вопрос говорит «Claude выдаёт непоследовательный вывод несмотря на подробные инструкции» — ответ конкретные примеры ввода/вывода (а не больше инструкций). Если вопрос говорит «реализация в незнакомом домене» — ответ паттерн интервью.
          </div>

          <h4>Связи со сценариями</h4>
          <p><span class="scenario-tag">Сценарий 2: Генерация кода</span> Итерация через тесты: сначала напишите тесты, затем Claude итерирует реализацию, получая информацию о провалах тестов.</p>
          <p><span class="scenario-tag">Сценарий 6: Извлечение данных</span> Конкретные примеры ввода/вывода критичны для обучения Claude извлечению данных из различных форматов документов.</p>

          <h4>Глубокое понимание: Выбор правильной техники улучшения</h4>
          <p>Экзамен проверяет выбор техники на основе <em>симптома</em>. Запомните это соответствие:</p>
          <pre><code>Symptom → Technique:

"Inconsistent output format"
  → Few-shot examples (2-4 showing exact format)

"Works sometimes but not for edge cases"
  → Concrete input/output examples for the failing edges

"Don't know what to ask for"
  → Interview pattern (Claude asks clarifying questions first)

"Implementation has bugs"
  → Test-driven iteration (share specific test failures)

"Multiple issues in the code"
  → Interacting issues = single message
  → Independent issues = sequential messages

"Vague feedback isn't helping"
  → Provide SPECIFIC test cases with input + expected output</code></pre>
          <p><strong>Ловушка «больше инструкций»:</strong> Когда вопрос говорит «Claude выдаёт непоследовательный вывод несмотря на подробные инструкции», неправильный ответ ВСЕГДА «добавить больше инструкций» или «сделать инструкции подробнее». Правильный ответ — переключиться с инструкций на <strong>конкретные примеры</strong>. Если инструкции уже не помогли, больше инструкций не поможет — проблема в неоднозначности, которую могут разрешить только примеры.</p>
          <p><strong>Тонкость паттерна интервью:</strong> Паттерн интервью НЕ для случаев, когда Claude не понимает задачу. Он для случаев, когда <em>разработчик</em> не продумал все требования. Claude выявляет соображения, которые разработчик пропустил (стратегии инвалидации кэша, режимы сбоев, граничные случаи). Это <strong>техника проектирования</strong>, а не техника отладки.</p>
        `
      },
      {
        id: "t3.6",
        title: "3.6: Интеграция Claude Code в CI/CD конвейеры",
        knowledge: [
          "Флаг -p / --print запускает Claude Code в неинтерактивном режиме (предотвращает зависание CI!)",
          "--output-format json и --json-schema обеспечивают структурированный вывод в CI",
          "CLAUDE.md предоставляет контекст проекта (стандарты тестирования, фикстуры, критерии ревью) для вызванного из CI Claude Code",
          "Та же сессия, что генерировала код, менее эффективна при ревью своих изменений vs независимый экземпляр"
        ],
        skills: [
          "Запускать Claude Code в CI с флагом -p для предотвращения зависания при интерактивном вводе",
          "Использовать --output-format json + --json-schema для машиночитаемых структурированных результатов",
          "Включать предыдущие результаты ревью при повторном запуске для избежания дублирующихся комментариев",
          "Предоставлять существующие тестовые файлы в контексте, чтобы генерация тестов не дублировала сценарии",
          "Документировать стандарты тестирования и доступные фикстуры в CLAUDE.md для качественной генерации тестов"
        ],
        keyPoint: "CI/CD = используйте флаг -p. Саморевью слабо; используйте второй независимый экземпляр. Включайте предыдущие результаты для избежания дублирующихся комментариев.",
        details: `
          <h4>Флаг -p (неинтерактивный режим)</h4>
          <p>Это <strong>вещь №1</strong> для интеграции с CI/CD:</p>
          <pre><code># WRONG — hangs in CI waiting for user input
claude "Review this PR"

# RIGHT — non-interactive mode
claude -p "Review this PR"
# Also: claude --print "Review this PR"</code></pre>
          <p>Флаг <code>-p</code> (или <code>--print</code>) запускает Claude Code в неинтерактивном режиме. Без него Claude Code ждёт интерактивного ввода и ваш CI-конвейер зависает навсегда.</p>

          <h4>Структурированный вывод для CI</h4>
          <p>CI-системы нуждаются в машиночитаемом выводе, а не в разговорном тексте:</p>
          <pre><code># JSON output with schema enforcement
claude -p \\
  --output-format json \\
  --json-schema '{"type":"object","properties":{
    "findings": {"type":"array","items":{
      "type":"object","properties":{
        "file":{"type":"string"},
        "line":{"type":"number"},
        "severity":{"type":"string","enum":["critical","warning","info"]},
        "message":{"type":"string"},
        "suggested_fix":{"type":"string"}
      }
    }},
    "summary":{"type":"string"}
  }}' \\
  "Review this PR for bugs and security issues"</code></pre>
          <p>Этот вывод может быть автоматически разобран и опубликован как inline PR-комментарии.</p>

          <h4>Ограничение саморевью</h4>
          <p>Та же сессия Claude, что генерировала код, <strong>менее эффективна при его ревью</strong>. Почему? Она сохраняет контекст рассуждений от генерации — она «помнит», почему приняла каждое решение, что делает её менее склонной подвергать эти решения сомнению.</p>
          <pre><code>// Weak: same session generates and reviews
Session A: "Write auth middleware" → generates code
Session A: "Now review your code" → biased review

// Strong: separate session reviews independently
Session A: "Write auth middleware" → generates code
Session B: "Review this code" → unbiased review
           (Session B has no knowledge of Session A's reasoning)</code></pre>

          <h4>Избежание дублирующихся комментариев</h4>
          <p>При повторном запуске CI-ревью после новых коммитов включайте предыдущие результаты в контекст:</p>
          <pre><code>claude -p "Review this PR. 
Previous review findings (already addressed or acknowledged):
- Line 45: Missing null check (FIXED in commit abc123)
- Line 89: SQL injection risk (acknowledged, using prepared statements)

Report only NEW or still-unaddressed issues."</code></pre>
          <p>Без этого Claude будет помечать одни и те же проблемы при каждом запуске, создавая шум.</p>

          <h4>CLAUDE.md для контекста CI</h4>
          <p>CLAUDE.md предоставляет контекст проекта для вызванного из CI Claude Code:</p>
          <ul>
            <li>Стандарты и фреймворки тестирования</li>
            <li>Доступные фикстуры и утилиты тестирования</li>
            <li>Критерии ревью и определения серьёзности</li>
            <li>Соглашения о стиле кода</li>
          </ul>
          <p>Это предотвращает генерацию Claude тестов, дублирующих существующее покрытие, или использование паттернов, несовместимых с проектом.</p>

          <h4>Генерация тестов в CI</h4>
          <pre><code># Provide existing tests so Claude doesn't generate duplicates
claude -p "Generate tests for src/auth.ts. 
  Existing test file: $(cat src/auth.test.ts)
  Available fixtures: $(cat test/fixtures/README.md)
  Testing standards are in CLAUDE.md."</code></pre>

          <div class="wrong-answer">
            <strong>Неправильный подход:</strong> Запуск Claude Code в CI без флага <code>-p</code>. Конвейер зависнет бесконечно, ожидая интерактивного ввода. Всегда используйте <code>-p</code> для CI.
          </div>

          <div class="exam-tip">
            <strong>Совет к экзамену:</strong> Вопросы по CI/CD проверяют три вещи: (1) флаг <code>-p</code> для неинтерактивного режима, (2) отдельные экземпляры для ревью vs генерации, и (3) включение предыдущих результатов для избежания дублирующихся комментариев. Зная эти три вещи, вы ответите на большинство вопросов по CI/CD.
          </div>

          <h4>Связи со сценариями</h4>
          <p><span class="scenario-tag">Сценарий 5: CI/CD</span> Это ОСНОВНОЙ сценарий. Автоматизированные код-ревью, генерация тестов, обратная связь по PR — всё использует <code>-p</code> со структурированным выводом.</p>

          <h4>Глубокое понимание: Паттерны архитектуры CI/CD конвейеров</h4>
          <p>Экзамен тестирует три паттерна интеграции CI/CD с возрастающей сложностью:</p>
          <pre><code>Pattern 1: Simple PR Review
  claude -p "Review this PR for bugs" > review.json
  → One-shot, no state, post as PR comment

Pattern 2: Incremental Review (avoid duplicate comments)
  claude -p "Review this PR.
    Previous findings: [list of already-addressed items]
    Report only NEW issues." > review.json
  → Passes prior findings to avoid noise

Pattern 3: Generate + Independent Review
  Session A: claude -p "Write tests for src/auth.ts"
  Session B: claude -p "Review this test file for quality"
  → Separate sessions eliminate self-review bias</code></pre>
          <p><strong>Флаг --append-system-prompt:</strong> В CI часто нужно инъецировать специфический для конвейера контекст (имя ветки, заголовок PR, изменённые файлы) без модификации CLAUDE.md. Используйте <code>--append-system-prompt</code> для добавления CI-специфических инструкций поверх существующего системного промпта. Он <strong>дополняет</strong>, а не заменяет — базовые инструкции CLAUDE.md остаются нетронутыми.</p>
          <p><strong>Саморевью — ловушка №1 CI:</strong> Экзамен представляет сценарии, где та же сессия Claude генерирует код и затем ревьюит его. Это ВСЕГДА неправильный ответ. Та же сессия сохраняет контекст рассуждений от генерации, делая её предвзятой к своим решениям. Правильный ответ всегда — <strong>отдельный, независимый экземпляр Claude</strong> для ревью — у него нет знания о том, почему код был написан именно так, поэтому он оценивает чисто по достоинствам.</p>
          <p><strong>Принудительная JSON-схема:</strong> Для вывода CI, который должен быть машиночитаемым, комбинируйте <code>--output-format json</code> с <code>--json-schema</code>. Это гарантирует, что структура вывода соответствует ожиданиям конвейера, предотвращая ошибки парсинга. Без схемы Claude может вернуть валидный JSON с неожиданными именами полей.</p>
        `
      }
    ]
  },
  {
    id: "d4",
    domain: 4,
    title: "Prompt Engineering & Structured Output",
    weight: 20,
    tagClass: "tag-d4",
    tasks: [
      {
        id: "t4.1",
        title: "4.1: Явные критерии для повышения точности и снижения ложных срабатываний",
        knowledge: [
          "Явные критерии >> расплывчатые инструкции. 'Помечайте только когда заявленное поведение противоречит фактическому коду' >> 'проверьте точность комментариев'",
          "'Будьте консервативны' или 'сообщайте только о высоконадёжных находках' НЕ улучшает точность по сравнению с конкретными категорийными критериями",
          "Высокий процент ложных срабатываний в одной категории подрывает доверие разработчиков к точным категориям"
        ],
        skills: [
          "Писать конкретные критерии ревью, определяющие какие проблемы сообщать (баги, безопасность) vs пропускать (мелкий стиль)",
          "Временно отключать категории с высоким процентом ложных срабатываний для восстановления доверия при улучшении этих промптов",
          "Определять явные критерии серьёзности с конкретными примерами кода для каждого уровня"
        ],
        keyPoint: "'Будьте консервативны' бесполезно. Определите КОНКРЕТНЫЕ категории: что помечать, что пропускать, с конкретными примерами для каждого уровня серьёзности.",
        details: `
          <h4>Проблема расплывчатых инструкций</h4>
          <p>Рассмотрим два промпта для агента проверки кода:</p>
          <pre><code>// РАСПЛЫВЧАТО — не улучшает точность
"Be conservative. Only report high-confidence findings."

// ЯВНО — действительно улучшает точность
"Report these issues:
  - CRITICAL: SQL injection, XSS, authentication bypass
  - WARNING: Null pointer risks, resource leaks, race conditions
  
DO NOT report:
  - Minor style differences (spacing, naming preferences)
  - Local patterns that differ from project conventions but work correctly
  - Missing comments on self-explanatory code"</code></pre>
          <p>"Будьте консервативны" и "сообщайте только о высоконадёжных находках" — субъективны: Claude интерпретирует их по-разному каждый раз. Конкретные категории дают Claude конкретную рамку принятия решений.</p>

          <h4>Почему ложные срабатывания разрушают доверие</h4>
          <p>Если ваш агент проверки кода имеет 50% ложных срабатываний по стилевым вопросам, разработчики будут:</p>
          <ol>
            <li>Начнут игнорировать ВСЕ находки (включая реальные баги)</li>
            <li>Потеряют доверие и к точным категориям тоже</li>
            <li>В конце концов полностью отключат инструмент</li>
          </ol>
          <p>Высокий процент ложных срабатываний в ОДНОЙ категории подрывает уверенность во ВСЕХ категориях.</p>

          <h4>Исправление: Управление на уровне категорий</h4>
          <pre><code>Strategy 1: Define explicit categories
  "Flag: bugs, security vulnerabilities, data loss risks
   Skip: style preferences, naming conventions, comment quality"

Strategy 2: Temporarily disable problematic categories
  If "comment accuracy" has 60% false positives:
  → Disable that category entirely
  → Fix the prompt/examples for that category
  → Re-enable once false positive rate drops

Strategy 3: Severity criteria with concrete examples
  "CRITICAL (must fix):
    Example: user input passed directly to SQL query
  WARNING (should fix):
    Example: catch block swallows exception without logging
  INFO (consider):
    Example: function over 50 lines could be split"</code></pre>

          <div class="wrong-answer">
            <strong>Подходы, которые НЕ работают:</strong>
            <ul>
              <li>"Будьте более консервативны" — всё ещё расплывчато, не меняет поведение предсказуемо</li>
              <li>"Сообщайте только о высоконадёжных находках" — Claude не может надёжно самостоятельно оценить уверенность</li>
              <li>"Сообщайте меньше ложных срабатываний" — замкнутая инструкция без конкретных критериев</li>
            </ul>
          </div>

          <div class="exam-tip">
            <strong>Совет к экзамену:</strong> Если в вопросе описывается инструмент проверки кода с высоким процентом ложных срабатываний и спрашивается как это исправить, ответ НИКОГДА не будет "добавить порог уверенности" или "сказать быть консервативнее". Ответ всегда — явные категорийные критерии с конкретными примерами, определяющими что помечать и что пропускать.
          </div>

          <h4>Связь со сценариями</h4>
          <p><span class="scenario-tag">Сценарий 5: CI/CD Code Review</span> Это основной сценарий. Автоматические ревью PR требуют точных критериев для получения действенной обратной связи без утопления разработчиков в ложных срабатываниях.</p>

          <h4>Глубокое понимание: Каскадный эффект ложных срабатываний</h4>
          <p>Экзамен проверяет понимание того, почему процент ложных срабатываний важен помимо просто раздражения:</p>
          <pre><code>False Positive Cascade:
  1. Tool reports 20 findings per PR
  2. 10 are false positives (50% FP rate)
  3. Developers start ignoring ALL findings
  4. Real bugs slip through because of "cry wolf" effect
  5. Team disables the tool entirely
  6. Zero automated coverage → worse than before

The Fix (category-level triage):
  1. Track FP rate per CATEGORY (not aggregate)
  2. "Comment accuracy" has 60% FP → DISABLE temporarily
  3. "SQL injection" has 2% FP → keep enabled
  4. Fix the "comment accuracy" prompt/examples
  5. Re-enable once FP rate drops below threshold</code></pre>
          <p><strong>Сигнальные слова экзамена:</strong> Когда в вопросе упоминается "доверие разработчиков", "принятие снизилось" или "команда перестала использовать инструмент", первопричина — всегда ложные срабатывания. Исправление — всегда явные категорийные критерии, никогда не "будьте консервативнее" или "добавьте порог уверенности".</p>
          <p><strong>Иерархия серьёзности имеет значение:</strong> Определяйте уровни серьёзности с КОНКРЕТНЫМИ примерами кода, а не абстрактными описаниями. "CRITICAL: пользовательский ввод передаётся напрямую в SQL-запрос без санитизации" — конкретно. "CRITICAL: очень серьёзные проблемы безопасности" — нет. Экзамен проверяет, понимаете ли вы, что серьёзность должна быть привязана к конкретным паттернам, а не к ощущениям.</p>
        `
      },
      {
        id: "t4.2",
        title: "4.2: Few-shot промптинг для консистентности и качества",
        knowledge: [
          "Few-shot примеры — НАИБОЛЕЕ эффективная техника для консистентного, действенного вывода когда одни инструкции не справляются",
          "Few-shot примеры демонстрируют обработку неоднозначных случаев и позволяют обобщать на новые паттерны",
          "Эффективны для снижения галлюцинаций в задачах извлечения (неформальные измерения, разнообразные структуры документов)"
        ],
        skills: [
          "Создавать 2-4 целевых примера для неоднозначных сценариев, показывающих рассуждение для выбранного действия",
          "Включать примеры, демонстрирующие желаемый формат вывода (расположение, проблема, серьёзность, исправление)",
          "Предоставлять примеры, различающие допустимые паттерны от настоящих проблем",
          "Использовать примеры для разнообразных структур документов (внутритекстовые цитаты vs библиографии)",
          "Добавлять примеры правильного извлечения из разнообразных форматов для исправления пустого/null извлечения"
        ],
        keyPoint: "Когда детальные инструкции дают непоследовательные результаты, 2-4 целевых few-shot примера — самое эффективное исправление.",
        details: `
          <h4>Почему few-shot примеры работают</h4>
          <p>Когда Claude выдаёт непоследовательный вывод несмотря на детальные инструкции, проблема обычно в <strong>неоднозначности</strong> — инструкции можно интерпретировать несколькими способами. Few-shot примеры решают это, показывая Claude точно, что вы хотите:</p>
          <pre><code>// Instructions alone (ambiguous):
"Extract measurements from documents. Handle informal formats."

// With few-shot examples (unambiguous):
Example 1:
  Input: "The room is about 12 by 15 feet"
  Output: { width: 12, length: 15, unit: "feet", precision: "approximate" }

Example 2:
  Input: "Ceiling height: 8'"
  Output: { height: 8, unit: "feet", precision: "exact" }

Example 3:
  Input: "The property spans roughly 2 acres"
  Output: { area: 2, unit: "acres", precision: "approximate" }</code></pre>
          <p>Обратите внимание, как примеры учат Claude обрабатывать неформальные измерения, сокращения и приблизительные значения — вещи, которые трудно описать прозой.</p>

          <h4>Что демонстрируют хорошие few-shot примеры</h4>
          <ul>
            <li><strong>Обработка неоднозначных случаев:</strong> Как решить между двумя правдоподобными действиями</li>
            <li><strong>Формат вывода:</strong> Точная структура (поля, типы, форматирование)</li>
            <li><strong>Граничные случаи:</strong> Что считается "допустимым" vs "настоящей проблемой"</li>
            <li><strong>Рассуждение:</strong> ПОЧЕМУ было выбрано одно действие, а не альтернативы</li>
          </ul>

          <h4>Few-shot для проверки кода</h4>
          <pre><code>Example: Acceptable pattern (don't flag)
  Code: try { await db.save(user); } catch (e) { return null; }
  Decision: SKIP — intentional error swallowing in this codebase
  Reasoning: Project convention uses null returns for save failures

Example: Genuine issue (flag)
  Code: try { await processPayment(amount); } catch (e) { }
  Decision: FLAG — WARNING severity
  Reasoning: Payment errors silently swallowed, customer charged 
             but no record saved. Financial impact.</code></pre>
          <p>Эти примеры учат Claude различать допустимые паттерны и настоящие проблемы — то, что одни инструкции часто не могут передать.</p>

          <h4>Few-shot для извлечения данных</h4>
          <p>Особенно эффективны для снижения галлюцинаций при разнообразных структурах документов:</p>
          <pre><code>Example: Inline citation format
  Input: "Smith (2023) found that 67% of participants..."
  Output: { author: "Smith", year: 2023, stat: "67%", type: "inline" }

Example: Bibliography format
  Input: "[12] J. Smith, 'Study Results', Journal of X, 2023"
  Output: { author: "J. Smith", year: 2023, title: "Study Results", 
            type: "bibliography", ref_num: 12 }

Example: Missing information (don't fabricate!)
  Input: "The study found significant improvement"
  Output: { stat: null, type: "qualitative", 
            note: "No numerical value provided" }</code></pre>

          <h4>Сколько примеров нужно?</h4>
          <p><strong>2-4 целевых примера</strong> — оптимальное количество. Слишком мало — Claude не увидит паттерн. Слишком много — потратите токены контекста. Фокусируйтесь на неоднозначных случаях — примеры для очевидных случаев не нужны.</p>

          <div class="wrong-answer">
            <strong>Неправильный подход:</strong> Реагировать на непоследовательный вывод добавлением БОЛЕЕ детальных инструкций. Если детальные инструкции уже не помогли, добавление ещё слов не поможет. Переключитесь на конкретные примеры.
          </div>

          <div class="exam-tip">
            <strong>Совет к экзамену:</strong> Few-shot примеры — ответ всегда, когда в вопросе упоминается "непоследовательный вывод", "галлюцинации при извлечении", "неоднозначные случаи" или "модель ненадёжно следует формату". Это НАИБОЛЕЕ эффективная техника для консистентности.
          </div>

          <h4>Связь со сценариями</h4>
          <p><span class="scenario-tag">Сценарий 6: Data Extraction</span> Few-shot примеры критически важны для обработки разнообразных форматов документов — внутритекстовые цитаты vs библиографии, неформальные измерения и неполные данные.</p>
          <p><span class="scenario-tag">Сценарий 5: CI/CD Code Review</span> Примеры, показывающие какие паттерны кода помечать vs пропускать, значительно улучшают точность ревью.</p>

          <h4>Глубокое понимание: Принципы проектирования few-shot примеров</h4>
          <p>Экзамен проверяет не только КОГДА использовать few-shot примеры, но и КАК их эффективно проектировать:</p>
          <pre><code>Principle 1: Show the HARD cases, not the easy ones
  ❌ Example: "Invoice #123" → { number: "123" }  (trivial)
  ✅ Example: "Inv. No.: 00123-A" → { number: "00123-A" }  (edge case)

Principle 2: Show the REASONING, not just I/O
  ❌ Input: "about 12 feet" → Output: { value: 12, precision: "approximate" }
  ✅ Input: "about 12 feet" → Output: { value: 12, precision: "approximate" }
     Reasoning: "about" indicates approximate measurement

Principle 3: Show NEGATIVE examples (what NOT to extract)
  ✅ Input: "The study found significant improvement"
     Output: { stat: null, note: "No numerical value — don't fabricate" }

Principle 4: Match your actual data distribution
  If 80% of your documents are invoices, have 2+ invoice examples
  If 5% are handwritten, still include 1 handwritten example
  (because that's where errors concentrate)</code></pre>
          <p><strong>Ловушка "слишком много примеров":</strong> Более 4-5 примеров обычно тратят токены контекста без улучшения качества. Экзамен может предложить вариант "добавить 20 примеров для покрытия всех граничных случаев". Это неправильно — 2-4 целевых примера, покрывающих неоднозначные случаи, — оптимальное количество. После 4-5 вы получаете убывающую отдачу и трату контекста.</p>
          <p><strong>Точка принятия решения few-shot vs инструкции:</strong> Начните с инструкций. Если Claude даёт консистентный вывод → готово. Если Claude даёт непоследовательный вывод → добавьте few-shot примеры. Если Claude всё ещё не справляется → задача может требовать tool_use с JSON-схемами для структурного принуждения. Это <strong>лестница эскалации</strong>, которую экзамен ожидает от вас.</p>
        `
      },
      {
        id: "t4.3",
        title: "4.3: Принудительный структурированный вывод через tool_use и JSON-схемы",
        knowledge: [
          "tool_use с JSON-схемами = самый надёжный способ для гарантированного вывода, соответствующего схеме (устраняет синтаксические ошибки)",
          "tool_choice: 'auto' (может вернуть текст), 'any' (обязан вызвать какой-то инструмент), forced (обязан вызвать конкретный инструмент)",
          "Строгие схемы устраняют СИНТАКСИЧЕСКИЕ ошибки, но НЕ семантические (строки не суммируются к итогу)",
          "Дизайн схемы: обязательные vs опциональные поля, enum + 'other' + строка описания для расширяемости"
        ],
        skills: [
          "Определять инструменты извлечения с JSON-схемами в качестве входных параметров",
          "Устанавливать tool_choice: 'any' когда существует несколько схем и тип документа неизвестен",
          "Принудительно указывать конкретный инструмент через tool_choice: {type: 'tool', name: '...'} для обязательного извлечения",
          "Проектировать опциональные/nullable поля когда источник может не содержать информации (предотвращает фабрикацию)",
          "Добавлять значения enum 'unclear' для неоднозначных случаев, 'other' + описание для расширяемых категорий",
          "Включать правила нормализации формата наряду со строгими схемами вывода"
        ],
        keyPoint: "tool_use + JSON-схема = нет синтаксических ошибок. Делайте поля nullable когда информации может не быть, чтобы предотвратить фабрикацию.",
        details: `
          <h4>Зачем tool_use для структурированного вывода?</h4>
          <p>Когда вам нужно, чтобы Claude возвращал данные в конкретном JSON-формате, <code>tool_use</code> с JSON-схемой — самый надёжный подход. Он <strong>полностью устраняет синтаксические ошибки JSON</strong> — нет пропущенных запятых, незакрытых скобок, повреждённых строк.</p>
          <pre><code>// Define an extraction "tool" with a JSON schema
{
  name: "extract_invoice",
  description: "Extract structured data from an invoice",
  input_schema: {
    type: "object",
    properties: {
      vendor: { type: "string" },
      invoice_number: { type: "string" },
      total: { type: "number" },
      line_items: {
        type: "array",
        items: {
          type: "object",
          properties: {
            description: { type: "string" },
            amount: { type: "number" }
          }
        }
      }
    },
    required: ["vendor", "invoice_number", "total"]
  }
}</code></pre>
          <p>Ответ Claude всегда будет валидным JSON, соответствующим этой схеме. Никаких ошибок парсинга.</p>

          <h4>tool_choice: Три варианта (повторение)</h4>
          <p>Это тестируется так часто, что стоит повторить в данном контексте:</p>
          <pre><code>"auto"   → Claude МОЖЕТ вернуть текст вместо вызова инструмента
           Use when: tool use is optional

"any"    → Claude ОБЯЗАН вызвать инструмент (выбирает какой)
           Use when: you have multiple extraction schemas
           and don't know which document type you're processing

forced   → Claude ОБЯЗАН вызвать ЭТОТ конкретный инструмент
{ type: "tool", name: "extract_invoice" }
           Use when: a specific extraction must happen first</code></pre>

          <h4>Синтаксические ошибки vs семантические ошибки</h4>
          <p>Критическое различие, которое тестирует экзамен:</p>
          <pre><code>JSON Schema устраняет:          JSON Schema НЕ предотвращает:
  ✅ Пропущенные скобки            ❌ Строки не суммируются к итогу
  ✅ Пропущенные запятые            ❌ Значения в неправильных полях
  ✅ Невалидный синтаксис JSON      ❌ Фабрикованные значения
  ✅ Неправильные типы данных       ❌ Неверные суммы/даты
  ✅ Отсутствующие обязательные поля ❌ Противоречивые данные</code></pre>
          <p>Валидация схемы обрабатывает <em>структуру</em>. Вам всё ещё нужна <em>семантическая</em> валидация (проверки бизнес-логики) отдельно.</p>

          <h4>Nullable/опциональные поля — предотвращение фабрикации</h4>
          <p>Это <strong>критический принцип проектирования</strong>: если поле может не существовать в исходном документе, сделайте его nullable:</p>
          <pre><code>// BAD — required field forces fabrication
{ "fax_number": { type: "string" } }  // required!
// Claude might fabricate a fax number if the document doesn't have one

// GOOD — nullable field allows honest "no data"
{ "fax_number": { type: ["string", "null"] } }
// Claude returns null when no fax number is in the document</code></pre>

          <h4>Расширяемые enum</h4>
          <p>Для полей категоризации используйте enum + "other" + строку описания:</p>
          <pre><code>{
  "document_type": {
    "type": "string",
    "enum": ["invoice", "receipt", "contract", "other"]
  },
  "document_type_detail": {
    "type": ["string", "null"],
    "description": "If type is 'other', describe the document type"
  },
  "confidence": {
    "type": "string",
    "enum": ["high", "medium", "low", "unclear"]
  }
}</code></pre>
          <p>Паттерн "other" + описание изящно обрабатывает граничные случаи. Значение enum "unclear" даёт Claude честный выход для неоднозначных случаев.</p>

          <div class="wrong-answer">
            <strong>Неправильный подход:</strong> Делать все поля обязательными когда исходные документы могут не содержать этой информации. Это заставляет Claude фабриковать данные для удовлетворения схемы. Делайте поля nullable/опциональными для предотвращения галлюцинаций.
          </div>

          <div class="exam-tip">
            <strong>Совет к экзамену:</strong> Если в вопросе описывается Claude, фабрикующий значения для полей, которых нет в документе, ответ — сделать эти поля опциональными/nullable. Если описываются синтаксические ошибки JSON, ответ — tool_use с JSON-схемой. Это разные проблемы с разными решениями.
          </div>

          <h4>Связь со сценариями</h4>
          <p><span class="scenario-tag">Сценарий 6: Data Extraction</span> Это ОСНОВНОЙ сценарий. Структурированное извлечение из счетов, контрактов и неструктурированных документов с использованием tool_use + JSON-схем.</p>
          <p><span class="scenario-tag">Сценарий 5: CI/CD</span> <code>--json-schema</code> в CI обеспечивает структурированный вывод, который можно автоматически распарсить для комментариев к PR.</p>

          <h4>Глубокое понимание: Паттерн Best-of-N</h4>
          <p>Паттерн <strong>Best-of-N</strong> генерирует несколько независимых выводов и выбирает лучший. Это техника качества/надёжности для высокоответственных извлечений:</p>
          <pre><code>// Best-of-N workflow:
1. Generate N independent extractions (e.g., N=3)
   - Each extraction uses the SAME prompt + document
   - Each runs in a SEPARATE context (no shared state)
2. Compare outputs:
   - If all N agree → high confidence, use the result
   - If N-1 agree → use majority, flag the outlier
   - If no agreement → escalate to human review
3. Select the best based on agreement + validation</code></pre>
          <p><strong>Когда использовать Best-of-N:</strong></p>
          <ul>
            <li>Извлечение из высокоценных документов, где ошибки дорогостоящи (финансовые, юридические, медицинские)</li>
            <li>Неоднозначные исходные документы, где один проход может пропустить или неправильно интерпретировать информацию</li>
            <li>Валидация трудно автоматизируема (нет эталона для сверки)</li>
          </ul>
          <p><strong>Когда НЕ использовать:</strong></p>
          <ul>
            <li>Простые, хорошо структурированные документы (трата токенов при минимальном выигрыше качества)</li>
            <li>Когда программная валидация может ловить ошибки (retry-with-feedback дешевле)</li>
            <li>Приложения, чувствительные к задержке (N параллельных вызовов = N× стоимость)</li>
          </ul>
          <div class="exam-tip">
            <strong>Совет к экзамену:</strong> Best-of-N — ответ когда в сценарии описывается "высокоответственное извлечение где валидация затруднена" или "нужна уверенность без эталона". Это НЕ ответ для простых ошибок формата (используйте retry-with-feedback) или когда есть правила валидации (используйте программные проверки).
          </div>
        `
      },
      {
        id: "t4.4",
        title: "4.4: Валидация, повторные попытки и циклы обратной связи",
        knowledge: [
          "Retry-with-error-feedback: добавить конкретные ошибки валидации к промпту при повторной попытке",
          "Повторные попытки неэффективны когда информация отсутствует в источнике (vs ошибки формата/структуры где повторные попытки работают)",
          "Циклы обратной связи: отслеживать поле detected_pattern для анализа паттернов отклонений",
          "Семантическая валидация (значения не суммируются) vs синтаксические ошибки схемы (устраняются через tool_use)"
        ],
        skills: [
          "Реализовать повторные запросы с: исходным документом + неудачным извлечением + конкретными ошибками валидации",
          "Определять когда повторные попытки не помогут (информация отсутствует в источнике) vs помогут (несовпадения формата, структурные ошибки)",
          "Добавить detected_pattern к находкам для анализа паттернов ложных срабатываний",
          "Проектировать самокоррекцию: извлекать calculated_total наряду с stated_total для обнаружения расхождений"
        ],
        keyPoint: "Повторные попытки работают для ошибок формата/структуры. Повторные попытки НЕ работают когда информации просто нет в документе.",
        details: `
          <h4>Паттерн Retry-with-Error-Feedback</h4>
          <p>Когда извлечение Claude не проходит валидацию, паттерн повторной попытки:</p>
          <pre><code>1. Send document + extraction prompt → get initial output
2. Validate output → find specific errors
3. Send: original document + failed output + specific errors
4. Claude self-corrects based on the error feedback
5. Re-validate → repeat if needed</code></pre>
          <p>Ключевое: включить <strong>конкретные ошибки валидации</strong> в промпт повторной попытки, а не просто "попробуйте ещё раз".</p>
          <pre><code>// BAD retry prompt
"The extraction was wrong. Try again."

// GOOD retry prompt
"Your extraction had these errors:
  1. line_items total ($1,234) doesn't match stated_total ($1,244)
  2. vendor_address is missing the ZIP code — it's on page 2
  3. invoice_date format should be YYYY-MM-DD, you returned 'Jan 5'
  
Here is the original document again: [document]
Here is your previous extraction: [extraction]
Please correct these specific issues."</code></pre>

          <h4>Когда повторные попытки работают vs не работают</h4>
          <pre><code>Повторные попытки РАБОТАЮТ для:    Повторные попытки НЕ РАБОТАЮТ для:
  ✅ Несовпадения формата              ❌ Информация не в документе
  ✅ Структурные ошибки вывода         ❌ Данные во внешнем документе
  ✅ Математические ошибки             ❌ Действительно неоднозначный источник
  ✅ Пропущенные данные на другой стр  ❌ Противоречивые исходные данные
  ✅ Неправильный формат даты          ❌ Затертые/нечитаемые секции</code></pre>
          <p>Критический инсайт: если информация просто не существует в документе, повторная попытка не поможет — Claude не может извлечь то, чего нет. Повторные попытки работают когда данные ЕСТЬ, но были неправильно обработаны.</p>

          <h4>Проектирование цикла обратной связи</h4>
          <p>Для постоянного улучшения качества отслеживайте паттерны в ошибках модели:</p>
          <pre><code>{
  "finding": "Potential null pointer on line 42",
  "severity": "warning",
  "detected_pattern": "unchecked_optional_access",
  "confidence": "medium"
}

// When developers dismiss findings, analyze by detected_pattern:
// "unchecked_optional_access" dismissed 80% of the time
// → This pattern has too many false positives
// → Improve the prompt/examples for this pattern</code></pre>
          <p>Поле <code>detected_pattern</code> позволяет систематически анализировать, какие паттерны дают ложные срабатывания.</p>

          <h4>Семантическая самокоррекция</h4>
          <p>Проектируйте извлечение для ловли собственных ошибок:</p>
          <pre><code>{
  "stated_total": 1244.00,     // What the document says
  "calculated_total": 1234.00, // Sum of extracted line items
  "totals_match": false,       // Flag discrepancy
  "conflict_detected": true    // Boolean for downstream processing
}</code></pre>
          <p>Извлечение и заявленного значения, И вычисленного значения позволяет автоматически обнаруживать расхождения.</p>

          <div class="wrong-answer">
            <strong>Неправильный подход:</strong> Бесконечные повторные попытки когда информации нет в документе. Если в документе нет номера факса, 5 повторных попыток его не создадут. Возвращайте null/empty для отсутствующих данных.
          </div>

          <div class="exam-tip">
            <strong>Совет к экзамену:</strong> Ключевое тестируемое различие: повторные попытки работают для ошибок обработки (неправильный формат, пропущенные данные которые ЕСТЬ). Повторные попытки НЕ работают для ошибок отсутствия (данных нет в документе). Если в вопросе описывается повторная попытка для отсутствующих данных — это неправильный ответ.
          </div>

          <h4>Связь со сценариями</h4>
          <p><span class="scenario-tag">Сценарий 6: Data Extraction</span> Retry-with-feedback — ядро конвейеров извлечения. Валидировать, определить конкретные ошибки, повторить с целевой обратной связью.</p>
          <p><span class="scenario-tag">Сценарий 5: CI/CD</span> Поле <code>detected_pattern</code> позволяет отслеживать процент ложных срабатываний со временем для улучшения промптов ревью.</p>

          <h4>Глубокое понимание: Дерево решений для повторных попыток</h4>
          <p>Экзамен проверяет, можете ли вы быстро определить, поможет ли повторная попытка. Используйте это дерево решений:</p>
          <pre><code>Extraction failed validation →
  Q: Is the required data IN the document?
    YES → Q: Was it a format/structure error?
      YES → RETRY with specific error feedback ✅
      NO → Q: Was it a math/logic error?
        YES → RETRY asking model to recalculate ✅
        NO → INVESTIGATE — may need different approach
    NO → DON'T RETRY ❌
      → Return null/empty for that field
      → Move on to next document</code></pre>
          <p><strong>Паттерн "вычисленное vs заявленное":</strong> Для финансовых документов всегда извлекайте И заявленный итог (что утверждает документ), И вычисленный итог (сумму позиций). Если они не совпадают, помечайте расхождение. Это механизм самокоррекции — модель ловит свои собственные ошибки извлечения И подлинные ошибки документа (счёт с неправильным итогом). Экзамен любит этот паттерн, потому что он демонстрирует семантическую валидацию помимо соответствия схеме.</p>
          <p><strong>Структура промпта повторной попытки:</strong> Экзамен проверяет, включает ли промпт повторной попытки все три компонента: (1) исходный документ, (2) неудачное извлечение и (3) конкретные ошибки валидации. Отсутствие любого из них делает повторную попытку менее эффективной. Самая частая ловушка — повторная попытка которая говорит "попробуйте ещё раз" без указания конкретных ошибок — это заставляет модель гадать что пошло не так.</p>
        `
      },
      {
        id: "t4.5",
        title: "4.5: Стратегии пакетной обработки",
        knowledge: [
          "Message Batches API: экономия 50%, до 24 часов обработки, нет SLA по задержке",
          "Подходит для: ночных отчётов, еженедельных аудитов, ночной генерации тестов. НЕ для: блокирующих проверок перед мержем",
          "Batch API НЕ поддерживает многоходовой вызов инструментов в рамках одного запроса",
          "Поля custom_id связывают пары запрос/ответ в пакете"
        ],
        skills: [
          "Сопоставлять API с рабочим процессом: синхронный для блокирующих проверок перед мержем, пакетный для ночных/еженедельных",
          "Рассчитывать частоту отправки на основе ограничений SLA (например, 4-часовые окна для 30-часового SLA)",
          "Обрабатывать ошибки пакета: повторно отправлять только неудачные документы по custom_id с модификациями",
          "Уточнять промпты на выборочном наборе перед пакетной обработкой больших объёмов"
        ],
        keyPoint: "Batch API = на 50% дешевле, но ожидание до 24ч. Никогда не используйте для блокирующих рабочих процессов. Используйте custom_id для отслеживания и повторной отправки ошибок.",
        details: `
          <h4>Обзор Message Batches API</h4>
          <p>Batch API предлагает <strong>экономию 50%</strong> в обмен на <strong>отсутствие гарантии задержки</strong> (до 24 часов обработки):</p>
          <pre><code>Synchronous API:          Message Batches API:
  ✅ Immediate response      ✅ 50% cost savings
  ✅ Guaranteed latency      ❌ Up to 24-hour processing
  ✅ Multi-turn tool use     ❌ No multi-turn tool calling
  ❌ Full price              ✅ Bulk processing</code></pre>

          <h4>Когда использовать каждый API</h4>
          <pre><code>Synchronous API:                    Batch API:
  ✅ Pre-merge checks (blocking)       ✅ Overnight code quality reports
  ✅ Interactive code review            ✅ Weekly security audits
  ✅ Real-time user interactions        ✅ Nightly test generation
  ✅ Anything needing immediate results ✅ Monthly compliance scans
                                        ✅ Large dataset extraction</code></pre>
          <div class="wrong-answer">
            <strong>Критически неправильный ответ:</strong> Использовать Batch API для проверок перед мержем. Это БЛОКИРУЮЩИЕ рабочие процессы — PR не может быть смержен пока проверка не завершится. При времени обработки до 24 часов разработчики ждали бы весь день для проверки PR. Всегда используйте синхронный API для блокирующих рабочих процессов.
          </div>

          <h4>Нет многоходового вызова инструментов</h4>
          <p>Batch API <strong>не может</strong> выполнять инструменты в середине запроса и возвращать результаты. Каждый запрос в пакете — одноходовое взаимодействие. Если вашему рабочему процессу нужно, чтобы Claude вызвал инструмент, увидел результат, затем вызвал другой инструмент, вы должны использовать синхронный API.</p>

          <h4>custom_id для отслеживания</h4>
          <p>Каждый запрос в пакете имеет <code>custom_id</code>, который связывает запрос с ответом:</p>
          <pre><code>// Batch submission
[
  { custom_id: "invoice-001", messages: [...] },
  { custom_id: "invoice-002", messages: [...] },
  { custom_id: "invoice-003", messages: [...] }
]

// Batch results
[
  { custom_id: "invoice-001", result: { ... }, status: "success" },
  { custom_id: "invoice-002", result: { ... }, status: "success" },
  { custom_id: "invoice-003", status: "error", error: "context_exceeded" }
]

// Resubmit only failures
[
  { custom_id: "invoice-003", messages: [chunked_version] }
]</code></pre>

          <h4>Расчёт времени отправки пакета</h4>
          <p>Рассчитывайте частоту отправки на основе ограничений SLA:</p>
          <pre><code>If your SLA says "results within 30 hours"
And batch processing takes UP TO 24 hours
Then submit every 6 hours maximum
(6h wait + 24h processing = 30h worst case)</code></pre>

          <h4>Уточнение промпта перед пакетом</h4>
          <p>Всегда уточняйте промпт на <strong>небольшом выборочном наборе</strong> перед запуском большого пакета. Если в промпте есть проблемы, вы потратите весь запуск пакета (и деньги) на плохие извлечения:</p>
          <pre><code>1. Test prompt on 10-20 documents
2. Check quality, fix issues
3. Re-test on sample
4. Once satisfied, submit full batch of 10,000 documents</code></pre>

          <div class="exam-tip">
            <strong>Совет к экзамену:</strong> Вопросы о Batch API почти всегда проверяют: (1) он НИКОГДА не для блокирующих рабочих процессов, (2) он не поддерживает многоходовой вызов инструментов, и (3) используйте custom_id для повторной отправки ошибок. Если дистрактор предлагает использовать Batch для проверок перед мержем — это всегда неправильно.
          </div>

          <h4>Связь со сценариями</h4>
          <p><span class="scenario-tag">Сценарий 6: Data Extraction</span> Обработка 10,000 счетов за ночь → идеальный случай для пакетного использования. Используйте custom_id для отслеживания и повторной отправки ошибок.</p>
          <p><span class="scenario-tag">Сценарий 5: CI/CD</span> Проверки перед мержем должны использовать синхронный API. Ночные отчёты о качестве кода могут использовать пакетный API.</p>

          <h4>Глубокое понимание: Ограничения и граничные случаи Batch API</h4>
          <p>Экзамен проверяет нюансное понимание ограничений Batch API:</p>
          <pre><code>Batch API МОЖЕТ:
  ✅ Single-turn message processing
  ✅ Tool use (single-turn only — model calls tool, you get result)
  ✅ System prompts and JSON schemas
  ✅ Different prompts per request in same batch
  ✅ Mixed model versions within a batch

Batch API НЕ МОЖЕТ:
  ❌ Multi-turn conversations within a request
  ❌ Agentic loops (tool_use → result → next tool_use)
  ❌ Streaming responses
  ❌ Guaranteed completion time (up to 24h)
  ❌ Cancel individual requests (only entire batch)</code></pre>
          <p><strong>Ловушка расчёта времени:</strong> Если ваш SLA говорит "результаты в течение 30 часов" и пакетная обработка занимает до 24 часов, максимальный интервал отправки составляет <code>SLA - batch_max = 30 - 24 = 6 часов</code>. Экзамен может представить это как математическую задачу, где один дистрактор использует 30 часов (игнорируя время обработки), а другой — 24 часа (путая SLA и обработку).</p>
          <p><strong>Принцип "уточнение промпта перед пакетом":</strong> Всегда тестируйте промпт извлечения на 10-20 репрезентативных документах ПЕРЕД отправкой пакета из 10,000 документов. Если у промпта 15% ошибок на выборке, вы потратите весь запуск пакета на обработку 10,000 документов с плохим промптом. Уточняйте итеративно на малых выборках, ЗАТЕМ пакет. Экзамен проверяет это предлагая "отправить полный пакет и исправить ошибки потом" как дистрактор.</p>
          <p><strong>Повторная отправка по custom_id:</strong> Когда пакет завершается с некоторыми ошибками, повторно отправляйте ТОЛЬКО неудачные запросы по их custom_id — не перезапускайте весь пакет. Это и дешевле, и быстрее.</p>
        `
      },
      {
        id: "t4.6",
        title: "4.6: Мульти-инстансное и многопроходное ревью",
        knowledge: [
          "Ограничение саморевью: модель сохраняет контекст рассуждений, менее склонна ставить под вопрос свои решения в той же сессии",
          "Независимые экземпляры ревью (без предшествующего контекста рассуждений) более эффективны для обнаружения тонких проблем",
          "Многопроходное: анализ по файлам + интеграционные проходы между файлами избегают рассеивания внимания"
        ],
        skills: [
          "Использовать второй независимый экземпляр Claude для ревью сгенерированного кода без контекста рассуждений генератора",
          "Разделять большие многофайловые ревью на проходы по файлам + отдельные интеграционные проходы для межфайловых потоков данных",
          "Запускать проходы верификации где модель сообщает уверенность рядом с каждой находкой"
        ],
        keyPoint: "Саморевью слабое потому что модель сохраняет рассуждения генерации. Используйте ОТДЕЛЬНЫЙ экземпляр для проверки кода.",
        details: `
          <h4>Проблема саморевью</h4>
          <p>Когда Claude генерирует код и затем ревьюит его в той же сессии, он сохраняет <strong>контекст рассуждений</strong> от генерации. Он "помнит" почему принял каждое решение, что делает его менее склонным ставить под вопрос эти решения:</p>
          <pre><code>Session A:
  Claude: "I'll use a recursive approach because [reasoning]"
  Claude: *generates recursive code*
  You:    "Now review your code for issues"
  Claude: "The code looks good. The recursive approach is 
           appropriate because [same reasoning as before]"
  // Claude is biased — it won't question its own reasoning</code></pre>

          <h4>Решение: Независимый экземпляр ревью</h4>
          <pre><code>Session A (Generator):
  Claude: *generates code*

Session B (Reviewer — NO knowledge of Session A):
  Claude: "I see several issues with this code:
    1. Recursive approach has O(2^n) complexity for n > 30
    2. No base case guard for negative inputs
    3. Stack overflow risk for large inputs"
  // Session B has no bias — it evaluates the code on its merits</code></pre>
          <p>Ревьюер не имеет доступа к рассуждениям генератора, поэтому оценивает код чисто по тому, что видит.</p>

          <h4>Архитектура многопроходного ревью</h4>
          <p>Для больших многофайловых изменений разделяйте ревью на фокусированные проходы:</p>
          <pre><code>Pass 1: Per-file local analysis
  ├── Review file_a.py for local issues
  ├── Review file_b.py for local issues
  └── Review file_c.py for local issues
  Each file reviewed in isolation — full attention on local issues

Pass 2: Cross-file integration analysis
  └── Review how files A, B, C interact
      - Data flow across modules
      - API contracts between services
      - Shared state consistency
      - Import chain correctness</code></pre>
          <p>Почему разделять? <strong>Рассеивание внимания.</strong> Ревью 10 файлов за один проход означает, что Claude уделяет меньше внимания каждому. Проходы по файлам обеспечивают тщательный локальный анализ. Интеграционный проход ловит сквозные проблемы.</p>

          <h4>Маршрутизация ревью на основе уверенности</h4>
          <p>Пусть модель сообщает уверенность рядом с каждой находкой:</p>
          <pre><code>{
  "finding": "Potential SQL injection on line 42",
  "severity": "critical",
  "confidence": "high",
  "reasoning": "User input passed directly to query without sanitization"
}

{
  "finding": "Possible performance issue in loop",
  "severity": "warning",
  "confidence": "low",
  "reasoning": "Unclear if this loop runs on large datasets"
}</code></pre>
          <p>Направляйте находки с высокой уверенностью напрямую разработчикам. Направляйте находки с низкой уверенностью человеку-ревьюеру для сортировки. Это оптимизирует ограниченную пропускную способность ревьюера.</p>

          <div class="wrong-answer">
            <strong>Неправильные подходы к саморевью:</strong>
            <ul>
              <li>"Использовать extended thinking для саморевью" — всё та же сессия, тот же bias</li>
              <li>"Добавить 'ревьюируйте код критически' к промпту" — инструкция не устраняет bias рассуждений</li>
              <li>"Использовать temperature 0 для ревью" — температура не решает проблему контекста рассуждений</li>
            </ul>
          </div>

          <div class="exam-tip">
            <strong>Совет к экзамену:</strong> Вопросы о саморевью всегда имеют один ответ: использовать ОТДЕЛЬНЫЙ, НЕЗАВИСИМЫЙ экземпляр без контекста рассуждений генератора. Любой ответ, пытающийся улучшить саморевью в рамках той же сессии (extended thinking, критические промпты, изменения температуры) — неправильный.
          </div>

          <h4>Связь со сценариями</h4>
          <p><span class="scenario-tag">Сценарий 5: CI/CD</span> CI-конвейеры должны использовать отдельный экземпляр Claude для ревью — не ту же сессию, которая генерировала код.</p>
          <p><span class="scenario-tag">Сценарий 2: Code Generation</span> После того как Claude сгенерировал код, используйте отдельный экземпляр для проверки качества перед мержем.</p>

          <h4>Глубокое понимание: Почему саморевью проваливается, а независимое ревью работает</h4>
          <p>Экзамен тестирует это на более глубоком уровне чем "используйте отдельную сессию". Понимание ПОЧЕМУ помогает справляться с вариативными вопросами:</p>
          <pre><code>Self-review fails because of ANCHORING BIAS:
  Session A generates: recursive fibonacci
  Session A reviews:   "Recursion is appropriate here because..."
  → The model is anchored to its own reasoning chain
  → It will defend its decisions, not challenge them

Independent review succeeds because of FRESH EVALUATION:
  Session B sees:      recursive fibonacci (no reasoning context)
  Session B reviews:   "This has O(2^n) complexity. Consider memoization."
  → No prior commitment to the recursive approach
  → Evaluates purely on code quality</code></pre>
          <p><strong>Extended thinking не исправляет саморевью:</strong> Ловушка экзамена предлагает "включить extended thinking для более тщательного саморевью". Звучит правдоподобно, но неправильно — extended thinking всё ещё в той же сессии, поэтому anchoring bias сохраняется. Больше времени на размышление не помогает когда отправная точка предвзята.</p>
          <p><strong>Связь с Best-of-N:</strong> Мульти-инстансное ревью связано с паттерном Best-of-N (t4.3 Deep Insight). Сгенерируйте N независимых решений, затем используйте независимого оценщика для выбора лучшего. Оценщик должен быть отделён от всех генераторов — тот же принцип, что и независимое ревью кода.</p>
          <p><strong>Архитектура многопроходного ревью:</strong> Проходы по файлам должны выполняться как независимые экземпляры (каждый файл ревьюируется без знания результатов ревью других файлов). Это предотвращает перекрёстное загрязнение, когда находка в файле A влияет на ревью файла B. Интеграционный проход между файлами — ЕДИНСТВЕННОЕ место, где находки из всех файлов объединяются.</p>
        `
      }
    ]
  },
  {
    id: "d5",
    domain: 5,
    title: "Context Management & Reliability",
    weight: 15,
    tagClass: "tag-d5",
    tasks: [
      {
        id: "t5.1",
        title: "5.1: Сохранение критической информации в длительных взаимодействиях",
        knowledge: [
          "Прогрессивное суммирование рискованно: сжатие сумм, дат, процентов в расплывчатые резюме",
          "Эффект 'потеря в середине': модели надёжно обрабатывают начало и конец, могут пропускать средние секции",
          "Результаты инструментов накапливаются и потребляют токены непропорционально (40+ полей когда релевантны только 5)",
          "Полная история разговора должна передаваться в последующих API-запросах для связности"
        ],
        skills: [
          "Извлекать транзакционные факты (суммы, даты, ID, статусы) в постоянный блок 'факты дела' вне суммированной истории",
          "Обрезать многословные результаты инструментов до только релевантных полей перед их накоплением",
          "Размещать ключевые находки в начале агрегированных входных данных; использовать явные заголовки секций",
          "Требовать от субагентов включения метаданных (даты, источники, методы) в структурированные выводы",
          "Модифицировать вышестоящих агентов для возврата структурированных данных (ключевые факты, цитаты, баллы) вместо многословного контента"
        ],
        keyPoint: "Извлеките точные значения в блок 'факты дела'. Обрежьте результаты инструментов до релевантных полей. Ключевые находки — в начало, чтобы избежать 'потери в середине'.",
        details: `
          <h4>Проблема сохранения контекста</h4>
          <p>В длительных разговорах критическая информация теряется. Три механизма вызывают это:</p>
          <ol>
            <li><strong>Прогрессивное суммирование:</strong> Когда история разговора суммируется, точные значения (суммы, даты, ID) сжимаются в расплывчатые фразы типа "недавний заказ клиента" вместо "Заказ #ORD-67890 на $723.50, оформлен 2024-01-15"</li>
            <li><strong>Эффект "потеря в середине":</strong> Claude надёжно обрабатывает начало и конец длинных входных данных, но может пропускать информацию, закопанную в средних секциях</li>
            <li><strong>Накопление токенов:</strong> Результаты инструментов копятся — поиск заказа возвращает 40+ полей когда нужны только 5. Контекст заполняется нерелевантными данными.</li>
          </ol>

          <h4>Решение 1: Блок фактов дела</h4>
          <p>Извлеките транзакционные факты в постоянный блок, который остаётся в КАЖДОМ промпте, вне суммированной истории:</p>
          <pre><code>== CASE FACTS (do not summarize) ==
Customer: Jane Smith (C-12345)
Issue 1: Order #ORD-67890, $723.50, shipped 2024-01-15
  Status: Delivered damaged, return initiated
Issue 2: Order #ORD-12345, $89.99, shipped 2024-01-10
  Status: Wrong item, replacement requested
Verification: ID verified via security questions
================================</code></pre>
          <p>Даже если история разговора суммируется, эти точные значения сохраняются. Никакого расплывчатого "заказ клиента" — всегда точная сумма, дата и ID.</p>

          <h4>Решение 2: Обрезка результатов инструментов</h4>
          <p>Перед тем как результаты инструментов попадут в разговор, убирайте нерелевантные поля:</p>
          <pre><code>// RAW tool result (40+ fields)
{
  order_id: "ORD-67890",
  customer_id: "C-12345",
  created_at: "2024-01-15T10:30:00Z",
  items: [...],
  shipping_method: "standard",
  tracking_number: "1Z999AA10123456784",
  internal_sku: "SKU-789",
  warehouse_id: "WH-03",
  picking_zone: "B-14",
  pack_station: "PS-07",
  ... 30 more internal fields
}

// TRIMMED result (5 relevant fields)
{
  order_id: "ORD-67890",
  total: 723.50,
  status: "delivered",
  shipping_date: "2024-01-15",
  return_eligible: true
}</code></pre>

          <h4>Решение 3: Ключевые находки — в начало</h4>
          <p>Для борьбы с "потерей в середине" размещайте самую важную информацию в <strong>начале</strong> агрегированных входных данных:</p>
          <pre><code>== KEY FINDINGS (most important) ==
1. Revenue declined 12% QoQ
2. Three data sources show conflicting market share numbers
3. Regulatory filing deadline is January 31

== DETAILED RESULTS ==
[Section 1: Revenue analysis...]
[Section 2: Market share data...]
[Section 3: Regulatory requirements...]</code></pre>
          <p>Используйте явные заголовки секций и помещайте резюме наверх. Не закапывайте критические находки на странице 5 из 10-страничной агрегации.</p>

          <h4>Требования к выводу субагентов</h4>
          <p>Требуйте от субагентов включения метаданных в структурированные выводы:</p>
          <pre><code>{
  "finding": "Revenue grew 23% YoY",
  "source": "Q4 Financial Report",
  "date": "2024-01-15",
  "page": 12,
  "method": "Compared Q4 2023 vs Q4 2022 revenue figures"
}</code></pre>
          <p>Без этих метаданных координатор не может верифицировать утверждения, атрибутировать источники или оценить надёжность.</p>

          <div class="wrong-answer">
            <strong>Неправильный подход:</strong> Позволять результатам инструментов накапливаться без фильтрации. После 10 поисков заказов с 40+ полями каждый, вы потратили тысячи токенов на преимущественно нерелевантные данные. Всегда обрезайте перед вставкой в контекст.
          </div>

          <div class="exam-tip">
            <strong>Совет к экзамену:</strong> Вопросы по управлению контекстом часто описывают сценарий, где агент "забывает" важные детали в ходе разговора. Ответ включает: (1) блок фактов дела для точных значений, (2) обрезку результатов инструментов и/или (3) размещение ключевых находок в начале. Никогда не "добавить больше контекста" — управляйте тем, что уже есть.
          </div>

          <h4>Связь со сценариями</h4>
          <p><span class="scenario-tag">Сценарий 1: Customer Support</span> Многопроблемные сессии поддержки нуждаются в блоках фактов дела для сохранения деталей каждой проблемы (номера заказов, суммы, статусы).</p>
          <p><span class="scenario-tag">Сценарий 3: Multi-Agent Research</span> Вышестоящие агенты возвращают структурированные данные (ключевые факты, цитаты, баллы) вместо многословного контента, уважая бюджеты контекста нижестоящих.</p>

          <h4>Глубокое понимание: Эффект "потеря в середине"</h4>
          <p>Исследования показывают, что LLM имеют <strong>U-образную кривую внимания</strong> — они надёжно обрабатывают <strong>начало</strong> и <strong>конец</strong> длинных контекстов, но <strong>внимание деградирует для контента в середине</strong>:</p>
          <pre><code>Attention Level:
  HIGH ████████░░░░░░░░░░░░░░████████
       ↑ Beginning         Middle ↓         End ↑
       Strong attention   WEAK attention   Strong attention</code></pre>
          <p><strong>Практические последствия:</strong></p>
          <ul>
            <li>Критические инструкции, факты дела и ключевые находки должны быть в <strong>начале</strong> контекста</li>
            <li>Текущее сообщение пользователя естественно оказывается в <strong>конце</strong> (тоже высокое внимание)</li>
            <li>Многословные результаты инструментов, исторические ходы разговора и вспомогательные детали могут быть в <strong>середине</strong> (где внимание наименьшее)</li>
          </ul>
          <p><strong>Стратегии смягчения:</strong></p>
          <ol>
            <li><strong>Размещение по приоритету:</strong> System prompt → блок фактов дела → ключевые находки в самом начале</li>
            <li><strong>Явные заголовки секций:</strong> Разбивайте длинные средние секции чёткими разделителями (<code>== НАЗВАНИЕ СЕКЦИИ ==</code>) для создания якорей внимания</li>
            <li><strong>Дублирование критических данных:</strong> Повторяйте самые важные факты и в начале, И в текущем запросе</li>
            <li><strong>Агрессивная обрезка:</strong> Уменьшайте объём средних секций — меньше контента в середине означает меньше возможностей для потери внимания</li>
          </ol>
          <div class="exam-tip">
            <strong>Совет к экзамену:</strong> Если в вопросе описывается агент, "пропускающий" или "забывающий" информацию, которая ЕСТЬ в контексте (не была суммирована), вероятная причина — "потеря в середине" — и исправление — перемещение этой информации в начало. Это отличается от переполнения контекста (где информация действительно пропала). "Потеря в середине" означает что информация <em>присутствует, но не получает внимания</em>.
          </div>
          <div class="wrong-answer">
            <strong>Неправильное исправление:</strong> "Увеличить размер окна контекста" не исправляет "потерю в середине" — это делает её ХУЖЕ, добавляя больше контента в середину. Исправление — реструктуризация, не расширение.
          </div>
        `
      },
      {
        id: "t5.2",
        title: "5.2: Паттерны эскалации и разрешения неоднозначности",
        knowledge: [
          "Триггеры эскалации: клиент запрашивает человека, исключения/пробелы в политике, невозможность продвижения",
          "Эскалировать немедленно когда клиент явно требует; предлагать решение когда проблема простая",
          "Эскалация на основе сентимента и самооценка уверенности — НЕНАДЁЖНЫЕ прокси для сложности",
          "Несколько совпадений клиентов → запросить дополнительные идентификаторы, не выбирать эвристически"
        ],
        skills: [
          "Добавить явные критерии эскалации с few-shot примерами в системный промпт",
          "Выполнять явные запросы клиентов на оператора-человека немедленно без предварительного исследования",
          "Признавать фрустрацию предлагая решение в рамках возможностей; эскалировать если клиент повторяет",
          "Эскалировать когда политика неоднозначна или молчит по конкретному запросу",
          "Запрашивать дополнительные идентификаторы при нескольких совпадениях, не угадывать"
        ],
        keyPoint: "Клиент говорит 'дайте мне человека' → эскалировать немедленно. Анализ сентимента и баллы уверенности ненадёжны для решений об эскалации.",
        details: `
          <h4>Триггеры эскалации</h4>
          <p>Когда агент должен эскалировать к человеку? Есть три чётких триггера:</p>
          <ol>
            <li><strong>Клиент явно запрашивает человека</strong> — Эскалировать НЕМЕДЛЕННО. Не пытайтесь сначала разобраться.</li>
            <li><strong>Исключение или пробел в политике</strong> — Запрос клиента не покрывается существующей политикой.</li>
            <li><strong>Невозможность продвижения</strong> — Агент застрял и не может решить проблему.</li>
          </ol>

          <h4>Явный запрос = немедленная эскалация</h4>
          <pre><code>Customer: "I want to speak to a manager."

// WRONG
Agent: "Let me first look into your issue to see if I can help..."
// This ignores the customer's explicit request!

// RIGHT
Agent: "I'll connect you with a supervisor right away. Let me 
       compile a summary of your issue so they can help you quickly."
// Honors the request, prepares a handoff summary</code></pre>
          <p>Это безусловно. Если клиент явно просит человека — эскалируйте. Не исследуйте сначала.</p>

          <h4>Нюанс с фрустрацией</h4>
          <p>Когда клиент расстроен, но явно не просил человека:</p>
          <pre><code>Customer: "This is ridiculous! I've been waiting for a week!"

Agent: "I completely understand your frustration — a week is 
       too long to wait. Let me look into this right now and 
       see what I can do to resolve it."
// Acknowledge frustration, offer resolution

Customer: "No, I just want to talk to someone!"

Agent: "Absolutely, I'll connect you with a supervisor now."
// NOW escalate — customer reiterated preference</code></pre>
          <p>Паттерн: Признать фрустрацию → Предложить решение → Если клиент повторяет → Эскалировать.</p>

          <h4>Что НЕ использовать для решений об эскалации</h4>
          <div class="wrong-answer">
            <strong>Ненадёжные сигналы эскалации:</strong>
            <ul>
              <li><strong>Анализ сентимента:</strong> Рассерженные клиенты могут иметь простые проблемы. Спокойные клиенты могут иметь сложные. Сентимент не предсказывает сложность кейса.</li>
              <li><strong>Самооценка уверенности:</strong> Уверенность модели в собственной оценке — ненадёжный прокси для сложности кейса или необходимости эскалации.</li>
            </ul>
          </div>

          <h4>Эскалация из-за пробела в политике</h4>
          <pre><code>Customer: "Your competitor offers price matching. Can you match it?"

// Agent checks policy → policy only covers own-site price adjustments
// Policy is SILENT on competitor price matching

Agent: "I don't have the authority to match competitor prices, but 
       let me connect you with a specialist who can review this 
       with you."
// Escalate because policy doesn't cover this case</code></pre>
          <p>Когда политика неоднозначна или молчит по конкретному запросу, эскалируйте а не гадайте.</p>

          <h4>Множественные совпадения клиентов</h4>
          <pre><code>// Tool returns 3 customers named "John Smith"
// WRONG: Pick the one with the most recent order
// RIGHT: Ask for additional identifiers

Agent: "I found several accounts under that name. Could you 
       provide your email address or the last 4 digits of your 
       phone number so I can pull up the right account?"</code></pre>

          <div class="exam-tip">
            <strong>Совет к экзамену:</strong> Вопросы об эскалации имеют предсказуемые паттерны ответов: (1) "Клиент просит человека" → эскалировать немедленно, (2) "Эскалация на основе сентимента" → неправильный ответ, (3) "Эскалация на основе уверенности" → неправильный ответ, (4) "Политика не покрывает этот случай" → эскалировать.
          </div>

          <h4>Связь со сценариями</h4>
          <p><span class="scenario-tag">Сценарий 1: Customer Support</span> Это ОСНОВНОЙ сценарий. Агент поддержки должен балансировать разрешение при первом контакте (цель 80%) со знанием когда эскалировать.</p>

          <h4>Глубокое понимание: Блок-схема решения об эскалации</h4>
          <p>Экзамен проверяет решения об эскалации в нюансных сценариях. Используйте эту полную блок-схему:</p>
          <pre><code>Customer message received →
  Q: Did customer explicitly request a human?
    YES → ESCALATE IMMEDIATELY (don't investigate first)
    NO → Q: Is the request within policy?
      YES → Q: Can the agent resolve it?
        YES → RESOLVE (first-contact resolution)
        NO → Q: Is progress being made?
          YES → Continue attempting
          NO → ESCALATE (inability to progress)
      NO → Q: Is policy silent or ambiguous?
        YES → ESCALATE (policy gap)
        NO → DECLINE per policy, explain why

  At ANY point:
    Customer says "I want a human" → ESCALATE IMMEDIATELY
    Customer reiterates frustration after offer → ESCALATE</code></pre>
          <p><strong>Ловушка "сначала разобраться":</strong> Когда клиент говорит "дайте менеджера", НЕПРАВИЛЬНЫЙ ответ — "позвольте мне сначала изучить вашу проблему, может я смогу помочь". Даже если агент МОЖЕТ решить проблему, клиент явно попросил человека. Исследование сначала игнорирует заявленное предпочтение клиента и делает взаимодействие хуже. Всегда выполняйте явные запросы немедленно.</p>
          <p><strong>Паттерн признания фрустрации:</strong> Клиент расстроен но не просил человека → признать фрустрацию + предложить решение. Клиент повторяет "просто дайте мне человека" → эскалировать. Экзамен проверяет именно этот двухшаговый паттерн. Неправильный ответ эскалирует при первом признаке фрустрации (чрезмерная реакция). Другой неправильный ответ продолжает предлагать решение после того как клиент повторил (игнорирование клиента).</p>
          <p><strong>Множественные совпадения клиентов:</strong> Никогда не выбирайте клиента эвристически (например, "самый последний заказ"). Всегда запрашивайте дополнительные идентификаторы. Экзамен представляет это как соблазнительный шорткат, который создаёт реальный риск — действие с неправильным аккаунтом может раскрыть данные другого клиента.</p>
        `
      },
      {
        id: "t5.3",
        title: "5.3: Распространение ошибок в мультиагентных системах",
        knowledge: [
          "Структурированный контекст ошибки (тип отказа, попытанный запрос, частичные результаты, альтернативы) позволяет интеллектуальное восстановление",
          "Различать отказы доступа (таймауты) от валидных пустых результатов (нет совпадений)",
          "Общее 'поиск недоступен' скрывает ценный контекст от координатора",
          "Анти-паттерны: тихое подавление ошибок ИЛИ прекращение всего рабочего процесса при одном отказе"
        ],
        skills: [
          "Возвращать структурированный контекст ошибки: тип отказа, что было попытано, частичные результаты, альтернативы",
          "Различать отказы доступа от валидных пустых результатов в отчётах об ошибках",
          "Субагенты обрабатывают локальное восстановление для транзиентных отказов; передают только нерешаемые ошибки с частичными результатами",
          "Аннотировать синтез с пробелами покрытия из-за недоступных источников"
        ],
        keyPoint: "Никогда не подавляйте ошибки молча. Никогда не прекращайте весь рабочий процесс из-за одного отказа. Возвращайте структурированный контекст ошибки для интеллектуального восстановления.",
        details: `
          <h4>Распространение ошибок в мультиагентных системах</h4>
          <p>Когда субагент встречает ошибку, то как он сообщает эту ошибку координатору определяет способность системы к восстановлению.</p>

          <h4>Два анти-паттерна</h4>
          <div class="wrong-answer">
            <strong>Анти-паттерн 1: Тихое подавление ошибок</strong>
            <pre><code>// Subagent encounters database timeout
// Returns empty results as if query succeeded
return { results: [], status: "success" }
// Coordinator thinks "no results found" — WRONG!
// Actually: database was down</code></pre>
          </div>
          <div class="wrong-answer">
            <strong>Анти-паттерн 2: Прекращение всего рабочего процесса</strong>
            <pre><code>// One subagent fails
// Entire research pipeline stops
throw new Error("Web search failed — aborting all research")
// Lost: partial results from other subagents that DID succeed</code></pre>
          </div>

          <h4>Правильный паттерн: Структурированный контекст ошибки</h4>
          <pre><code>// Subagent returns structured error information
{
  status: "partial_failure",
  error: {
    type: "transient",
    source: "web_search",
    attempted: "Search for 'AI market trends 2024'",
    message: "Search API timeout after 30s",
    isRetryable: true
  },
  partial_results: [
    { title: "AI Market Report", url: "...", snippet: "..." }
    // One result retrieved before timeout
  ],
  alternatives: [
    "Retry with simplified query",
    "Use cached results from previous search",
    "Try alternative search provider"
  ]
}</code></pre>
          <p>С этим структурированным контекстом координатор может принимать интеллектуальные решения: повторить поиск, использовать частичные результаты, попробовать альтернативу или обойти пробел.</p>

          <h4>Отказ доступа vs валидный пустой результат (повторение)</h4>
          <p>Это различие тестируется многократно по нескольким задачам потому что это так важно:</p>
          <pre><code>// Access failure
{ isError: true, type: "transient", 
  message: "Database connection refused" }
// → Coordinator should retry or use alternative source

// Valid empty result
{ isError: false, results: [], 
  message: "Query returned 0 matching records" }
// → Coordinator should report "no data available" (not retry)</code></pre>

          <h4>Локальное восстановление перед передачей</h4>
          <p>Субагенты должны обрабатывать локальное восстановление для транзиентных отказов ПЕРЕД передачей координатору:</p>
          <pre><code>Subagent error handling:
1. Attempt operation
2. If transient failure → retry 2-3 times locally
3. If still failing → propagate to coordinator with:
   - What was attempted
   - Number of retries
   - Partial results (if any)
   - Suggested alternatives
4. Only propagate UNRESOLVABLE errors</code></pre>

          <h4>Аннотация пробелов покрытия</h4>
          <p>При синтезе результатов, когда некоторые источники были недоступны, аннотируйте пробелы:</p>
          <pre><code>== Research Synthesis ==
Section 1: Market Analysis [Complete]
  Sources: Bloomberg, Reuters, SEC filings

Section 2: Competitor Analysis [PARTIAL — gap noted]
  Sources: Public filings only
  Gap: Unable to access Gartner report (API timeout)
  Impact: Market share estimates may be incomplete

Section 3: Regulatory Outlook [Complete]
  Sources: Federal Register, industry publications</code></pre>

          <div class="exam-tip">
            <strong>Совет к экзамену:</strong> Вопросы о распространении ошибок всегда имеют одну и ту же структуру ответа: (1) возвращать структурированный контекст ошибки, (2) включать что было попытано + частичные результаты + альтернативы, (3) никогда не подавлять молча, (4) никогда не прекращать весь рабочий процесс. Если дистрактор упоминает "вернуть пустые результаты" или "прервать конвейер" — это неправильно.
          </div>

          <h4>Связь со сценариями</h4>
          <p><span class="scenario-tag">Сценарий 3: Multi-Agent Research</span> Субагент веб-поиска таймаутится → возвращает частичные результаты + структурированную ошибку → координатор решает повторить или работать с тем что есть.</p>
          <p><span class="scenario-tag">Сценарий 1: Customer Support</span> Бэкенд-сервис недоступен → агент сообщает о проблеме клиенту и эскалирует вместо молчаливого возврата неверных данных.</p>

          <h4>Глубокое понимание: Выбор стратегии восстановления при ошибках</h4>
          <p>Экзамен проверяет какую стратегию восстановления использовать в зависимости от типа ошибки и состояния системы:</p>
          <pre><code>Error Type → Recovery Strategy:

Transient (timeout, 503):
  1. Subagent retries locally (2-3 attempts)
  2. If still failing → propagate with partial results
  3. Coordinator: retry with simplified query OR use alternative source

Validation (bad input):
  1. Subagent does NOT retry (same input = same failure)
  2. Propagate with error details + the invalid input
  3. Coordinator: fix input and resubmit to same subagent

Business (policy violation):
  1. Subagent does NOT retry
  2. Propagate with limit/threshold that was exceeded
  3. Coordinator: try alternative workflow or escalate

Permission (access denied):
  1. Subagent does NOT retry
  2. Propagate immediately
  3. Coordinator: escalate to human or use alternative source</code></pre>
          <p><strong>Принцип частичных результатов:</strong> Даже когда субагент терпит неудачу, он должен включать любые данные, которые удалось собрать до отказа. "Я обыскал 3 из 5 баз данных до таймаута, вот результаты из 3 успешных" намного полезнее чем "поиск не удался". Координатор может решить, достаточны ли частичные данные или отсутствующие источники критичны.</p>
          <p><strong>Аннотация пробелов покрытия:</strong> Когда синтез продолжается с неполными данными (потому что источник был недоступен), финальный отчёт ДОЛЖЕН аннотировать какие секции имеют пробелы покрытия и почему. "Секция 2: ЧАСТИЧНО — отчёт Gartner недоступен из-за таймаута API. Оценки доли рынка основаны только на публичных документах." Без этой аннотации потребитель отчёта не знает какие выводы хорошо обоснованы, а какие основаны на неполных данных.</p>
        `
      },
      {
        id: "t5.4",
        title: "5.4: Управление контекстом при исследовании больших кодовых баз",
        knowledge: [
          "Деградация контекста: модель даёт непоследовательные ответы и ссылается на 'типичные паттерны' вместо конкретных обнаруженных классов",
          "Файлы-блокноты (scratchpad) сохраняют ключевые находки через границы контекста",
          "Делегирование субагентам изолирует многословное исследование; главный агент координирует высокоуровневое понимание",
          "Структурированное сохранение состояния для восстановления после сбоев: агенты экспортируют состояние, координатор загружает манифест при возобновлении"
        ],
        skills: [
          "Порождать субагентов для конкретных расследований пока главный агент сохраняет координацию",
          "Поддерживать файлы-блокноты записывающие ключевые находки; ссылаться на них для последующих вопросов",
          "Суммировать находки одной фазы перед порождением субагентов для следующей, инъецируя резюме",
          "Проектировать восстановление после сбоев используя структурированные экспорты состояния агентов (манифесты)",
          "Использовать /compact для сокращения контекста во время длительных сессий исследования"
        ],
        keyPoint: "Длинная сессия даёт непоследовательные ответы? Используйте файлы-блокноты для сохранения. Используйте /compact. Делегируйте многословное исследование субагентам.",
        details: `
          <h4>Признаки деградации контекста</h4>
          <p>Как узнать, что ваш контекст исчерпан?</p>
          <ul>
            <li>Claude даёт <strong>непоследовательные ответы</strong> о вещах, которые он правильно определил ранее</li>
            <li>Claude ссылается на <strong>"типичные паттерны"</strong> вместо конкретных классов/функций, которые он уже обнаружил</li>
            <li>Claude <strong>перечитывает файлы</strong>, которые уже читал, или спрашивает о том, что уже было установлено</li>
            <li>Качество ответов <strong>постепенно деградирует</strong> по ходу сессии</li>
          </ul>

          <h4>Решение 1: Файлы-блокноты (Scratchpad)</h4>
          <p>Записывайте ключевые находки в файл-блокнот, который сохраняется через границы контекста:</p>
          <pre><code>// scratchpad.md
# Codebase Investigation Notes

## Architecture
- Entry point: src/index.ts
- Router: src/routes/index.ts (express-based)
- Auth: src/middleware/auth.ts (JWT, 30min expiry)

## Key Findings
- Database: PostgreSQL via Prisma ORM
- User model at src/models/user.ts (47 fields!)
- Payment processing: src/services/payment.ts
  - Uses Stripe API
  - Webhook handler at src/routes/webhooks.ts

## Open Questions
- How does the cache invalidation work?
- What triggers the nightly batch job?</code></pre>
          <p>Когда контекст деградирует или вы используете <code>/compact</code>, ссылайтесь на блокнот для восстановления ключевого контекста.</p>

          <h4>Решение 2: Команда /compact</h4>
          <p><code>/compact</code> сжимает контекст разговора во время длительных сессий. Используйте её проактивно когда замечаете, что сессия становится длинной, ДО начала деградации. После сжатия ссылайтесь на блокнот чтобы ключевые находки не потерялись.</p>

          <h4>Решение 3: Делегирование субагентам</h4>
          <p>Самый мощный паттерн: делегируйте многословное исследование субагентам, пока главный агент фокусируется на координации:</p>
          <pre><code>Main Agent (coordinator — clean context):
  "Investigate the auth system"
    → Spawns subagent for auth investigation
    ← Receives structured summary: "JWT-based, 30min expiry, 
       refresh tokens in Redis, middleware at auth.ts:45"

  "Investigate the payment system"  
    → Spawns subagent for payment investigation
    ← Receives structured summary: "Stripe integration, 
       webhook handler at webhooks.ts, retry logic at payment.ts:120"

  Main agent retains SUMMARIES (clean, small)
  Subagents handled the verbose file reading (isolated context)</code></pre>

          <h4>Структурированное состояние для восстановления после сбоев</h4>
          <p>Для критических длительных расследований проектируйте восстановление через структурированные экспорты состояния:</p>
          <pre><code>{
  "phase": "analysis",
  "completed_modules": ["auth", "payment", "user"],
  "pending_modules": ["notification", "logging"],
  "key_findings": { ... },
  "open_questions": [ ... ],
  "timestamp": "2024-01-15T10:30:00Z"
}
// Save to manifest.json → coordinator loads on resume</code></pre>

          <h4>Инкрементальная стратегия</h4>
          <pre><code>1. Explore with subagents (isolated verbose context)
2. Record findings in scratchpad (persistent file)
3. Use /compact when context gets long
4. Reference scratchpad after compaction
5. Continue investigation with clean context</code></pre>

          <div class="wrong-answer">
            <strong>Неправильный подход:</strong> Продолжать длительную сессию исследования без какого-либо управления контекстом. После чтения 50+ файлов контекст исчерпан и Claude начинает давать расплывчатые, непоследовательные ответы. Используйте блокноты, /compact и делегирование субагентам проактивно.
          </div>

          <div class="exam-tip">
            <strong>Совет к экзамену:</strong> Если в сценарии описывается "Claude даёт непоследовательные ответы" или "ссылается на типичные паттерны вместо конкретных находок", ответ включает управление контекстом: файлы-блокноты, /compact или делегирование субагентам. Это три инструмента для борьбы с деградацией контекста.
          </div>

          <h4>Связь со сценариями</h4>
          <p><span class="scenario-tag">Сценарий 4: Developer Productivity</span> Это ОСНОВНОЙ сценарий. Длительные сессии исследования кодовой базы нуждаются в блокнотах, /compact и делегировании субагентам для сохранения эффективности.</p>
          <p><span class="scenario-tag">Сценарий 3: Multi-Agent Research</span> Координатор делегирует многословные исследования субагентам, сохраняя чистый координационный контекст.</p>

          <h4>Глубокое понимание: Продвинутые техники управления контекстом</h4>
          <p><strong>Паттерн HANDOFF.md:</strong> Перед завершением сессии или когда контекст почти заполнен, попросите Claude написать файл HANDOFF.md, суммирующий: текущий прогресс, испробованные подходы (что сработало и не сработало), открытые вопросы и рекомендуемые следующие шаги. Следующая сессия читает этот файл вместо того чтобы полагаться на качество сжатия. Это гораздо надёжнее чем <code>--resume</code> с устаревшими результатами инструментов.</p>
          <p><strong>Ловушка сжатия:</strong> По умолчанию <code>/compact</code> теряет архитектурные решения, потому что приоритизирует "перечитываемый" контент (результаты инструментов, код). Исправление: добавьте секцию <strong>Compact Instructions</strong> в CLAUDE.md, указывающую что сохранять (архитектурные решения, выбор дизайна API, обнаруженные режимы отказа).</p>
          <p><strong>Команда /context:</strong> Используйте <code>/context</code> чтобы увидеть фактическую разбивку бюджета токенов — это выявляет скрытые расходы вроде определений инструментов MCP, съедающих 10-20K токенов. Необходимо для диагностики ситуаций "почему мой контекст полон?".</p>
          <p><strong>Пятиуровневая стратегия контекста:</strong></p>
          <ul>
            <li><strong>Всегда резидентный</strong> (CLAUDE.md) — контракт проекта, жёсткие ограничения</li>
            <li><strong>Загружаемый по пути</strong> (.claude/rules/) — специфичный для языка/типа файла</li>
            <li><strong>По запросу</strong> (Skills) — рабочие процессы, загружаются только при активации</li>
            <li><strong>Изолированный</strong> (Субагенты) — массовое исследование, возвращаются только резюме</li>
            <li><strong>Вне контекста</strong> (Hooks) — детерминированные скрипты, нулевая стоимость токенов</li>
          </ul>
        `
      },
      {
        id: "t5.5",
        title: "5.5: Рабочие процессы ревью человеком и калибровка уверенности",
        knowledge: [
          "97% общая точность может маскировать плохую производительность на конкретных типах документов или полях",
          "Стратифицированная случайная выборка измеряет процент ошибок в извлечениях с высокой уверенностью",
          "Баллы уверенности на уровне полей калибруются с использованием размеченных валидационных наборов",
          "Проверить точность по типу документа И по полю перед автоматизацией"
        ],
        skills: [
          "Реализовать стратифицированную случайную выборку извлечений с высокой уверенностью для постоянного измерения ошибок",
          "Анализировать точность по типу документа и полю для проверки консистентной производительности перед снижением ревью",
          "Выводить баллы уверенности на уровне полей; калибровать пороги ревью с использованием размеченных валидационных наборов",
          "Направлять извлечения с низкой уверенностью или неоднозначные на ревью человеком, приоритизируя ограниченную пропускную способность ревьюера"
        ],
        keyPoint: "Не доверяйте агрегированной точности (97%). Разбивайте по типу документа и полю. Используйте стратифицированную выборку для обнаружения скрытых паттернов ошибок.",
        details: `
          <h4>Ловушка агрегированной точности</h4>
          <p>Ваша система извлечения показывает 97% общую точность. Звучит отлично, правда? Но что если:</p>
          <pre><code>Accuracy by document type:
  Invoices:    99.5%  ← Great!
  Receipts:    98.0%  ← Good
  Contracts:   97.0%  ← Okay
  Handwritten: 71.0%  ← Terrible!
  
Aggregate: 97% (because invoices are 80% of volume)
But handwritten documents are completely unreliable!</code></pre>
          <p>97% агрегат маскирует серьёзную проблему. Вы бы автоматизировали весь конвейер думая что он работает, а потом обнаружили катастрофические отказы на определённых типах документов.</p>

          <h4>Стратифицированный анализ: Тип документа И поле</h4>
          <p>Разбивайте точность по двум измерениям:</p>
          <pre><code>             | vendor | amount | date  | items |
─────────────┼────────┼────────┼───────┼───────┤
Invoices     | 99.9%  | 99.5%  | 99.0% | 98.0% |
Receipts     | 98.0%  | 97.0%  | 95.0% | 90.0% |
Contracts    | 97.0%  | N/A    | 94.0% | N/A   |
Handwritten  | 85.0%  | 71.0%  | 68.0% | 55.0% |</code></pre>
          <p>Теперь видно: извлечение элементов из рукописных документов имеет точность только 55%. Вы бы направляли их на ревью человеком, автоматизируя остальное.</p>

          <h4>Стратифицированная случайная выборка</h4>
          <p>Даже для извлечений с высокой уверенностью, выбирайте регулярно для обнаружения дрейфа:</p>
          <pre><code>Sampling strategy:
  1. Sample 5% of ALL extractions randomly
  2. Sample 20% of "edge case" document types
  3. Sample 50% of low-confidence extractions
  4. Human reviewer checks each sample
  5. Track error rates over time by stratum</code></pre>
          <p>Это ловит проблемы, которые могут развиться со временем (изменение форматов документов, новые граничные случаи) прежде чем они затронут большой объём.</p>

          <h4>Баллы уверенности на уровне полей</h4>
          <pre><code>{
  "vendor": { "value": "Acme Corp", "confidence": 0.98 },
  "amount": { "value": 1234.50, "confidence": 0.95 },
  "date":   { "value": "2024-01-15", "confidence": 0.92 },
  "fax":    { "value": "555-0100", "confidence": 0.31 }
}
// Route to human review when ANY field drops below threshold
// Calibrate thresholds using labeled validation sets</code></pre>
          <p>Калибруйте пороги уверенности используя размеченные валидационные наборы — не доверяйте самооценке уверенности модели напрямую.</p>

          <h4>Маршрутизация ревью человеком</h4>
          <pre><code>High confidence (all fields > 0.9):
  → Auto-accept with spot-check sampling

Mixed confidence (some fields < 0.9):
  → Route to human for low-confidence fields only

Low confidence (multiple fields < 0.7):
  → Full human review

Document type never seen before:
  → Always route to human until accuracy validated</code></pre>
          <p>Это оптимизирует ограниченную пропускную способность ревьюера — люди фокусируются на случаях, наиболее вероятно содержащих ошибки.</p>

          <div class="wrong-answer">
            <strong>Неправильный подход:</strong> Использовать агрегированную точность (97%) для обоснования автоматизации всего извлечения. Всегда проверяйте точность по типу документа И по полю перед снижением ревью человеком. 97% агрегат может скрывать 71% точность на конкретных типах.
          </div>

          <div class="exam-tip">
            <strong>Совет к экзамену:</strong> Если в вопросе упоминается "97% точность" и спрашивается о снижении ревью человеком, правильный ответ включает разбивку по типу документа и полю сначала. Любой ответ говорящий "97% достаточно для полной автоматизации" — неправильный.
          </div>

          <h4>Связь со сценариями</h4>
          <p><span class="scenario-tag">Сценарий 6: Data Extraction</span> Это ОСНОВНОЙ сценарий. Системы извлечения должны валидировать точность на гранулярных уровнях перед автоматизацией ревью человеком.</p>

          <h4>Глубокое понимание: Лестница уверенности для автоматизации</h4>
          <p>Экзамен проверяет когда безопасно снижать ревью человеком. Используйте эту лестницу:</p>
          <pre><code>Level 1: Full human review (new system, unknown accuracy)
  → ALL extractions reviewed by humans
  → Build labeled validation set (500+ documents)

Level 2: Stratified review (accuracy validated by type)
  → High-accuracy types (invoices 99.5%): spot-check 5%
  → Medium-accuracy types (receipts 95%): review 20%
  → Low-accuracy types (handwritten 71%): review 100%

Level 3: Confidence-based routing (calibrated thresholds)
  → High confidence (all fields > 0.9): auto-accept + 5% sampling
  → Mixed confidence (some < 0.9): route low-confidence fields only
  → Low confidence (multiple < 0.7): full human review

Level 4: Autonomous with monitoring (mature system)
  → Ongoing stratified random sampling (5% of all)
  → Drift detection: alert if error rate increases
  → New document types: ALWAYS start at Level 1</code></pre>
          <p><strong>Ловушка "97%" — самый частый паттерн экзамена в D5:</strong> В вопросе утверждается "система достигает 97% общей точности." Неправильный ответ говорит что этого достаточно для полной автоматизации. Правильный ответ говорит что нужно разбить по типу документа И по полю. 97% агрегат может маскировать 71% точность на конкретных типах. Экзамен использует этот точный паттерн многократно.</p>
          <p><strong>Калибровка уверенности:</strong> Баллы уверенности на уровне полей должны быть калиброваны по размеченному валидационному набору — нельзя доверять самооценке уверенности модели напрямую. Если модель говорит 95% уверенности, но правильна только 80% времени при этом пороге, порог некалиброван. Калибровка требует периодической перевалидации по мере смещения распределений документов.</p>
          <p><strong>Новые типы документов сбрасываются на Уровень 1:</strong> Даже если система обрабатывает счета с 99.5%, новый тип документа (например, заказы на закупку) должен начинать с полного ревью человеком пока точность не будет независимо валидирована для этого типа.</p>
        `
      },
      {
        id: "t5.6",
        title: "5.6: Сохранение происхождения и обработка неопределённости в мультиисточниковом синтезе",
        knowledge: [
          "Атрибуция источников теряется когда суммирование сжимает находки без маппинга утверждение-источник",
          "Структурированные маппинги утверждение-источник должны сохраняться и мержиться через этапы синтеза",
          "Противоречивая статистика из надёжных источников: аннотировать конфликты с атрибуцией, не выбирать произвольно один",
          "Требовать даты публикации/сбора для предотвращения временной неверной интерпретации"
        ],
        skills: [
          "Требовать от субагентов вывода маппингов утверждение-источник (URL, имена документов, выдержки), сохраняемых через синтез",
          "Структурировать отчёты различая хорошо установленные находки от оспариваемых",
          "Включать противоречивые значения с явными аннотациями; позволять координатору согласовать перед синтезом",
          "Требовать даты публикации в выводах для корректной временной интерпретации",
          "Отображать разные типы контента соответствующим образом (финансовые как таблицы, новости как прозу, технические как списки)"
        ],
        keyPoint: "Никогда не теряйте атрибуцию источников при синтезе. Сохраняйте противоречивые данные с аннотациями. Всегда включайте даты публикации.",
        details: `
          <h4>Проблема атрибуции</h4>
          <p>Когда мультиагентная система синтезирует находки из нескольких источников, атрибуция источников легко теряется:</p>
          <pre><code>// Subagent 1 returns:
"Revenue grew 23% (Bloomberg, Jan 2024)"
"Revenue grew 19% (Reuters, Dec 2023)"

// Bad synthesis (attribution lost):
"Revenue grew approximately 20%"
// Which source? What date? Why the discrepancy? All lost.

// Good synthesis (attribution preserved):
"Revenue growth estimates vary by source:
  - Bloomberg (Jan 2024): 23% YoY growth
  - Reuters (Dec 2023): 19% YoY growth
  Note: Discrepancy may reflect different reporting periods"</code></pre>

          <h4>Маппинги утверждение-источник</h4>
          <p>Требуйте от субагентов вывода структурированных маппингов утверждение-источник, которые переживают конвейер синтеза:</p>
          <pre><code>// Subagent output format
{
  "claims": [
    {
      "claim": "Revenue grew 23% YoY",
      "source": "Bloomberg Terminal",
      "url": "https://bloomberg.com/...",
      "date": "2024-01-15",
      "excerpt": "Full-year revenue reached $4.2B, up 23% from..."
    },
    {
      "claim": "Market share reached 34%",
      "source": "Gartner Report Q4 2023",
      "url": null,
      "date": "2023-12-20",
      "excerpt": "Company X maintained leading position with 34%..."
    }
  ]
}</code></pre>
          <p>Эти маппинги должны <strong>сохраняться и мержиться</strong> через каждый этап синтеза — не сворачиваться в неатрибутированную прозу.</p>

          <h4>Обработка противоречивых данных</h4>
          <p>Когда надёжные источники противоречат друг другу, НЕ выбирайте произвольно один:</p>
          <pre><code>// WRONG — picked one source arbitrarily
"Market share is 34%"

// RIGHT — annotated conflict with attribution
"Market share estimates:
  Gartner (Q4 2023): 34%
  IDC (Q4 2023): 31%
  Note: Discrepancy likely due to different market 
  definitions (Gartner includes cloud; IDC does not)"</code></pre>
          <p>Координатор должен согласовать или аннотировать конфликты перед финальным синтезом, не заметать их под ковёр.</p>

          <h4>Даты публикации критически важны</h4>
          <p>Без дат временная неверная интерпретация неизбежна:</p>
          <pre><code>// Without dates — misleading
"Revenue is $4.2B and growing 23%"
// Is this current? From 2022? From a forecast?

// With dates — clear
"Revenue was $4.2B in FY2023 (Bloomberg, Jan 2024), 
representing 23% growth over FY2022"</code></pre>
          <p>Всегда требуйте даты публикации/сбора в выводах субагентов. Это предотвращает представление устаревших данных как текущих.</p>

          <h4>Рендеринг по типу контента</h4>
          <p>Разные типы контента должны отображаться соответствующим образом:</p>
          <pre><code>Financial data  → Tables with precise numbers
News/events     → Chronological prose with dates
Technical specs → Structured lists with values
Market analysis → Comparison tables with source attribution
Mixed content   → Appropriate format for each section</code></pre>

          <h4>Хорошо установленные vs оспариваемые находки</h4>
          <pre><code>== Well-Established (multiple sources agree) ==
- Company revenue: $4.2B (Bloomberg, Reuters, SEC filing — all agree)
- Employee count: ~12,000 (annual report)

== Contested (sources disagree) ==
- Market share: 31-34% (see source comparison above)
- Growth forecast: 15-25% (analyst estimates vary widely)</code></pre>
          <p>Различайте находки, где источники согласны, от тех, где они расходятся. Это строит доверие и помогает читателям калибровать свою уверенность.</p>

          <div class="wrong-answer">
            <strong>Неправильные подходы:</strong>
            <ul>
              <li>Суммировать находки без источников ("Выручка выросла примерно на 20%")</li>
              <li>Выбирать один противоречащий источник без объяснения</li>
              <li>Опускать даты из синтезированных утверждений</li>
              <li>Смешивать текущие и устаревшие данные без меток</li>
            </ul>
          </div>

          <div class="exam-tip">
            <strong>Совет к экзамену:</strong> Вопросы о происхождении проверяют три вещи: (1) сохранять маппинги утверждение-источник через синтез, (2) аннотировать конфликты а не выбирать произвольно, (3) всегда включать даты публикации. Если дистрактор предлагает "суммировать находки в чистый абзац" — это неправильно — теряется атрибуция.
          </div>

          <h4>Связь со сценариями</h4>
          <p><span class="scenario-tag">Сценарий 3: Multi-Agent Research</span> Это ОСНОВНОЙ сценарий. Исследовательские отчёты должны сохранять атрибуцию источников, прозрачно обрабатывать конфликты и включать даты для каждого утверждения.</p>

          <h4>Глубокое понимание: Конвейер сохранения атрибуции</h4>
          <p>Экзамен проверяет как атрибуция выживает через мультиагентный конвейер синтеза:</p>
          <pre><code>Stage 1: Research subagent outputs
  { claim: "Revenue $4.2B", source: "Bloomberg", date: "2024-01-15",
    url: "https://...", excerpt: "Full-year revenue reached..." }

Stage 2: Coordinator aggregates (attribution preserved)
  findings = [subagent_1.claims, subagent_2.claims, ...]
  // Each claim still has its source mapping

Stage 3: Synthesis subagent receives attributed findings
  prompt: "Synthesize these findings. PRESERVE source attribution.
           For each claim, include [Source, Date]."

Stage 4: Final report (attribution visible to reader)
  "Revenue was $4.2B (Bloomberg, Jan 2024), representing
   23% growth (Reuters, Dec 2023)."

ANTI-PATTERN (attribution lost at Stage 3):
  prompt: "Summarize these research findings into a clean paragraph."
  output: "Revenue grew approximately 20%"
  // Source? Date? Precision? All lost.</code></pre>
          <p><strong>Ловушка "чистого абзаца":</strong> Когда экзамен описывает промпт синтеза, просящий "чистое" или "отполированное" резюме — это часто неправильный ответ. Чистые резюме обычно теряют атрибуцию источников, смешивают противоречивые данные в средние значения и опускают даты. Правильный подход сохраняет беспорядочную реальность: конфликты, несколько источников, диапазоны дат.</p>
          <p><strong>Правило обработки противоречивых данных:</strong> Когда Bloomberg говорит 23% а Reuters говорит 19%, НИКОГДА не усредняйте (21%) и не выбирайте один. Всегда включайте оба с атрибуцией и аннотацией, объясняющей вероятную причину расхождения (разные периоды отчётности, разные определения рынка, разные методологии). Экзамен проверяет это предлагая "сообщить среднее" как правдоподобный дистрактор.</p>
          <p><strong>Даты публикации предотвращают временную путаницу:</strong> "Выручка составляет $4.2B" опасно без даты — читатели предполагают что это текущее. "Выручка составила $4.2B в FY2023 (отчёт январь 2024)" — ясно. Экзамен специально проверяет требуете ли вы даты в выводах субагентов для предотвращения представления устаревших данных как текущих.</p>
        `
      }
    ]
  }
];
